.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
orchestrator/state.sqlite

# Secrets — Slack chat tokens live here (documented .env fallback; OneCLI
# handles every other credential). Never commit real tokens.
.env
research/.env

# rdagent writes CWD-relative log traces + selector.log on every invocation
/log/
selector.log
# rdagent ui/server_ui default CWD-relative state dir (production points
# UI_TRACE_FOLDER/UI_STATIC_PATH outside the repo; this guards manual runs)
/git_ignore_folder/

