# Ralph Progress Log
Started: Sat Aug 15 12:50:59 PM UTC 2026
---
## Codebase Patterns
- `make check` = ruff + pyright + pytest (~2 min). Run it before every commit; the tree can carry PRE-EXISTING failures from prior commits — fix them minimally (they block your commit too).
- StateStore._connect sets WAL + timeout=30; keep any new sqlite connection on the same settings.
- Schema changes to an EXISTING table need `CREATE ... IF NOT EXISTS` in _SCHEMA plus a guarded ALTER in migrate() (see runs.universe_tickers).
- pyright venv is .venv; torch is not installed (docker-only) — suppress with `# pyright: ignore[reportMissingImports]` for container-side scripts.

## 2026-08-15 12:55 UTC - US-001
- Implemented WAL mode + 30s busy timeout for orchestrator/state.sqlite: `StateStore._connect` now opens with `sqlite3.connect(path, timeout=30)` (= busy_timeout 30000ms) and executes `PRAGMA journal_mode = WAL` per connection (persistent in the file, cheap no-op after first switch). Module docstring + orchestrator/CLAUDE.md updated.
- Tests added (tests/test_state.py): PRAGMA assertions (wal + busy_timeout >= 30000), pre-seeded delete-mode DB converts to WAL with rows intact, 4 threads x 25 concurrent writes complete with zero errors and 100 rows landed.
- Files changed: orchestrator/state.py, tests/test_state.py, orchestrator/CLAUDE.md; drive-by fixes for pre-existing check failures committed broken in b308db6: execution/pred_refresh.py (E501 line wrap), ops/gpu_trace.py + execution/pred_refresh_predict.py (pyright suppressions).
- Verified: full `make check` green (ruff, pyright, 1063 passed / 3 skipped).
- **Learnings for future iterations:**
  - main was committed with failing checks (b308db6): E501 in pred_refresh.py + 2 pyright errors in gpu_trace.py / pred_refresh_predict.py. Fixed here; don't be surprised if other pre-existing failures surface.
  - `PRAGMA journal_mode = DELETE` in a test is how you simulate the legacy live DB; journal_mode is a property of the FILE, not the connection.
  - sqlite3 `timeout=` connect kwarg IS the busy timeout (maps to busy_timeout ms) — no separate PRAGMA needed, but assert via `PRAGMA busy_timeout`.
  - Live-box note: the on-disk orchestrator/state.sqlite flips to WAL the first time the deployed code opens it; remember rdq-orchestrator needs a restart after deploy for this to take effect (memory: deploy-vs-running-process).
---
## 2026-08-15 13:10 UTC - US-002
- Fixed the GPU billing watchdog PATH bug: ops/rdq-gpu-watchdog.service now sets Environment=PATH=%h/.local/bin:/usr/local/bin:/usr/bin:/bin (doctl lives in ~/.local/bin; user-manager default PATH omits it). ops/gpu_watchdog.py gains a shutil.which("doctl") guard that posts a :rotating_light: Slack notice and exits 1 instead of dying on FileNotFoundError before any notify.
- Tests: tests/test_services.py new TestGpuWatchdogUnits (unit+timer exist, PATH line, systemd-analyze verify); tests/test_gpu_backend.py test_launch_injects_path_for_doctl asserts the --setenv=PATH injection on pipeline transient units; tests/test_gpu_watchdog.py test_missing_doctl_is_loud_failure (+ fake_doctl now patches shutil.which so tests stay hermetic).
- Files changed: ops/rdq-gpu-watchdog.service, ops/gpu_watchdog.py, tests/test_services.py, tests/test_gpu_backend.py, tests/test_gpu_watchdog.py, ops/CLAUDE.md.
- Verified: make check green (1068 passed / 3 skipped). On-box: daemon-reload + systemctl --user start rdq-gpu-watchdog exits 0 (ExecMainStatus=0, Environment shows the new PATH). Droplet-state check exercised under the unit's exact PATH via a temp state file (doctl ran, bogus droplet detected gone, state cleaned); guard verified with PATH=/usr/bin:/bin -> notice + exit 1. No real worker.env existed, nothing live touched.
- **Learnings for future iterations:**
  - The unit files in ops/ are SYMLINKED into ~/.config/systemd/user — editing the repo file + daemon-reload is enough, no reinstall.
  - systemctl --user from agent shells needs XDG_RUNTIME_DIR=/run/user/$(id -u) exported first.
  - `systemctl --user show <unit> -p Environment` reflects per-unit Environment= lines after daemon-reload (unlike show-environment).
  - To safely exercise the watchdog's doctl path on-box, point --state-file at a temp worker.env with a bogus DROPLET_ID: doctl get fails -> stale-state cleanup path, no real resources touched. Real state lives at ~/rdq-runs/gpu_worker/worker.env (absent today).
---
## 2026-08-15 13:30 UTC - US-003
- Restricted token-bearing env files and wired regression detection: `.env` and `research/.env` chmod 600 on-box (were 644). ops/health.sh gains section 4 "Env file permission audit": fails when mode has any group/other bit or an owner execute bit (i.e. more permissive than 600), passes absent files ("nothing to leak"), and honors an RDQ_ENV_FILES colon-separated override so tests are hermetic.
- Tests (tests/test_health.py): stub box now creates 600-mode env files + RDQ_ENV_FILES; new cases 644 fails w/ chmod hint, 700 (owner exec) fails, 400 passes, absent passes. 23/23 pass; shellcheck clean.
- Files changed: ops/health.sh, tests/test_health.py, ops/CLAUDE.md.
- Verified: make check green (1072 passed / 3 skipped). On-box `bash ops/health.sh` shows `PASS env mode .env (mode 600)` and `PASS env mode research/.env (mode 600)`.
- **Learnings for future iterations:**
  - ON-BOX FINDING (pre-existing, not from this story): health.sh fails on `tailscale serve :8443 -> http://127.0.0.1:3200` — that's the user's separate jph-master-tracker node app (tailnet-only, up 17 days), same category as the pre-existing 443/3100 mappings but added after the PLAN.md §1 allowlist. Deliberately NOT allowlisted autonomously (exposure-policy = operator decision). US-019 (health timer) will make this page Slack daily — resolve before/at that story: either operator adds 8443->3200 to ALLOWED_SERVE + PLAN.md §1, or removes the mapping.
  - health.sh tests are hermetic via env-var path overrides (RDQ_ENV_FILES pattern) — prefer that over letting the script read real repo files in tests.
  - Octal mode test in bash: `(( (8#$mode & 8#177) != 0 ))` = any group/other bit or owner-x; stricter modes (400) still pass.
---
## 2026-08-15 13:50 UTC - US-004
- Added the absolute store-staleness bound to execution/signal.py assert_fresh: new store_calendar_end() reads the calendar's final entry; assert_fresh fails when it trails as_of by more than max_store_lag_days CALENDAR days (default 5 via DEFAULT_MAX_STORE_LAG_DAYS, keyword-configurable + --max-store-lag-days CLI flag, plumbed through extract_targets). Failure messages now distinguish "store stale relative to today" from "predictions stale relative to store". Calendar parsing refactored into _read_calendar (shared by last_trading_day/store_calendar_end; empty calendar now raises cleanly). pred_refresh.py needed NO edit: both its call sites (short-circuit probe + post-docker self-check) call signal.assert_fresh directly, so the bound applies there automatically — covered by a new test.
- Tests: tests/test_signal.py — store_calendar_end, boundary at exactly 5 days passes / 6 fails even when pred == store's own last trading day (the self-certify case), max_store_lag_days configurability, extract_targets frozen-store abort; tests/test_pred_refresh.py — self-check fails "store stale relative to today" when as_of is 9+ days past the store end.
- Files changed: execution/signal.py, execution/CLAUDE.md, tests/test_signal.py, tests/test_pred_refresh.py.
- Verified: make check green (1077 passed / 3 skipped). Live-store sanity check on-box (read-only): store end 2026-08-13, lag 2 days -> passes the new bound; synthetic as_of +10d -> trips with the store-stale message. Tomorrow's real rebalance is safe.
- **Learnings for future iterations:**
  - assert_fresh is THE single freshness gate: rebalance (via extract_targets) and pred_refresh (2 direct call sites) all route through it — extend it there, never fork the logic per caller.
  - tests/test_signal.py fixture_workspace writes the calendar FROM the pred rows' dates (calendar end == latest pred day), handy for frozen-store scenarios; test_pred_refresh.py promoted_env pins CALENDAR ending 2026-07-16 with AS_OF 2026-07-17 (lag 1 — safely inside the new bound).
  - Bound semantics chosen: CALENDAR days (not trading days) vs the calendar FILE's final entry; exactly-at-bound passes, strictly-over fails (same boundary convention as gate/breaker limits).
## 2026-08-15 14:15 UTC - US-005
- Added the append-only promotion_history table to orchestrator/state.py: workspace_path, config JSON, promoted_at, source (CHECK constrained to auto_gate/conversation/cli), gate_verdict JSON nullable, replaced_workspace nullable. set_promoted_strategy gains source= (default 'cli') + gate_verdict= kwargs, validates source, and appends the history row IN THE SAME TRANSACTION as the single-row pointer upsert (reads the previous pointer first for replaced_workspace). New list_promotion_history(limit=None) reader, newest first (US-006 rollback will consume it). migrate() backfills a pre-history DB's promoted row as history row 1 (source 'cli'), guarded on the history table being empty — idempotent because history is append-only. Call sites tagged: orchestrator/promotion.py confirm_promotion -> source='conversation'; ops/promote_fetched.py -> source='cli'.
- Files changed: orchestrator/state.py, orchestrator/promotion.py, ops/promote_fetched.py, tests/test_state.py, orchestrator/CLAUDE.md.
- Verified: make check green (1085 passed / 3 skipped). 8 new tests: history append w/ verdict roundtrip, replaced_workspace chain, default source, unknown-source ValueError (nothing written), limit, legacy-DB backfill, backfill idempotent across re-migrations + after later promotions, fresh-DB no-op. E2E: ran the migration on a COPY of the live state.sqlite — the real promoted row (workspace c9587797..., promoted 2026-08-14) backfilled as history row 1 source 'cli'; re-migrate added nothing; copy deleted.
- **Learnings for future iterations:**
  - promotion_history + pointer flip commit atomically because _connect wraps the whole method body in one `with conn:` transaction — read-then-two-writes inside a single _connect block is the pattern for any future audit-trail write.
  - The backfill guard is "history table empty", NOT a schema-version flag — safe only because the table is append-only and never emptied. Don't add a delete path to promotion_history (rollback in US-006 must APPEND a new row, source 'cli').
  - Live-DB behaviors are cheap to verify safely: cp state.sqlite to /tmp, open StateStore on the copy (migration runs in __init__), assert, delete. Remember the real backfill only happens on the box when rdq-orchestrator restarts after deploy (memory: deploy-vs-running-process).
  - US-011/US-012 hooks are ready: pass source='auto_gate' + gate_verdict=<verdict JSON> from the gate path; --force recording (US-012) can ride inside gate_verdict.
---
## 2026-08-15 13:40 UTC - US-006
- Added `python -m ops.rollback_promotion` (US-006): re-promotes the previous promotion_history entry (or a named one via --to) using that entry's RECORDED config (never re-derived from the workspace conf), appends a new history row (source 'cli', gate_verdict = {action: rollback, restored_history_id, rolled_back_from}), and re-runs snapshot_pred_refresh BEFORE the pointer flip so a snapshot failure leaves the current promotion untouched. Safety rails mirror promote_fetched: dry-run default (--yes), never creates state.sqlite, refuses when the target workspace dir is gone ("likely swept"), refuses empty history / target==current / never-promoted --to targets. New --keep-snapshot flag preserves an operator-pinned conf_pred_refresh.yaml (frozen *_promoted_* universes would otherwise be regenerated from docker logs and lose the pin).
- ops/sweep.py: new recent_promotion_workspaces() (guarded like promoted_workspace: absent DB = empty set, unreadable DB = SweepError) protects the workspaces of the last RECENT_PROMOTIONS_KEPT=3 promotion_history entries; report reason precedence promoted > "recent promotion" > SOTA.
- Files changed: ops/rollback_promotion.py (new), ops/sweep.py, ops/CLAUDE.md, tests/test_rollback_promotion.py (new, 15 tests), tests/test_sweep.py (+4 tests).
- Verified: make check green (1103 passed / 3 skipped). E2E on a COPY of the live DB: real single-entry history correctly refuses ("nothing to roll back to"); after seeding a synthetic prior promotion, `--yes --keep-snapshot` flipped the pointer, kept the pinned conf, and appended history row source cli with the rollback marker + replaced_workspace = c9587797. On-box `ops.sweep --dry-run` exits 0 (5 deletable / 55 protected / 369 kept, nothing deleted).
- **Learnings for future iterations:**
  - Running any StateStore-opening tool from the NEW working tree against the live DB triggers migrate() — my on-box sweep dry-run applied the US-005 history backfill to the real state.sqlite (intended, idempotent, append-only; would have happened at deploy anyway). Be aware read-ish CLIs can carry pending migrations.
  - Fetched GPU workspaces live under ~/rdq-runs/gpu_worker/results/us_quant/workspace — NOT matched by sweep's `*/workspace` glob (run_root depth 1), so today's promoted/rollback workspaces are outside sweep scope entirely; the last-3 protection matters when candidates land under swept roots.
  - Rollback restores the history entry's config verbatim — this is the point of storing config in history (a rolled-back-to workspace may no longer parse cleanly, and re-derivation could disagree with what was actually traded).
  - The current live promotion (c9587797) rides a pinned frozen universe (us_liquid_promoted_30) in its snapshot conf — any future rollback ONTO it must use --keep-snapshot or re-apply the pin (same caveat promote_fetched prints).
---
## 2026-08-15 14:55 UTC - US-007
- New ops/promotion_gate.py: PURE evaluate_gate(candidate, incumbent, config) -> GateVerdict. Parity layer (test window, market, instrument-list hash, topk, n_drop, cost params — mismatch OR a missing input on either side fails parity and the gate, naming the field). Criteria layer from the new promotion_gate: section in orchestrator/config.yaml (load_gate_config; defaults ir_margin 1.05 / mdd_tolerance 1.25 / min_ic 0 / allow_first false): IR strict > incumbent×margin, |MDD| <= incumbent|MDD|×tolerance (at-limit passes, magnitude compare so the negative sign convention can't flip a verdict), IC strict > min_ic. No incumbent -> fail unless allow_first (which waives the comparisons, NOT the IC requirement). Verdict: to_json() (the promotion_history.gate_verdict payload, includes the config used) + slack_text() (per-criterion pass/fail with both values). IO half: load_metric_bundle reuses gpu_trace.workspace_metrics (METRIC_SPECS labelling) / workspace_window, summary.compute_sharpe, signal.load_market/load_strategy_params, and new signal.load_cost_params (exchange_kwargs open/close/min cost, scan-all-must-agree like load_strategy_params); every field degrades independently. hash_instruments = canonical sorted/deduped sha256[:16] universe hash — US-008 must reuse it.
- Files changed: ops/promotion_gate.py (new), execution/signal.py (+load_cost_params/COST_PARAM_KEYS), orchestrator/config.yaml (promotion_gate: section), tests/test_promotion_gate.py (new, 24 tests), tests/test_signal.py (write_conf costs kwarg + 5 load_cost_params tests), ops/CLAUDE.md, execution/CLAUDE.md.
- Verified: make check green (1138 passed / 3 skipped). E2E on-box (read-only): load_metric_bundle on the live promoted workspace c9587797 returns full metrics/window/market/topk-n_drop/costs/380 daily returns; evaluate_gate(e05ad9b4 as candidate vs c9587797, repo config) renders an honest FAIL — IR passes (1.7988 > 1.6391×1.05), MDD fails (-26.65% vs limit -20.15%), parity all-match with a shared demo hash.
- **Learnings for future iterations:**
  - The previous promoted workspace e05ad9b4 lives under ~/rdq-runs/server_ui/workspace/ (NOT the gpu_worker results tree) — useful as a second real workspace for comparison tests/demos.
  - Real workspaces load cleanly through load_metric_bundle, including conf_pred_refresh.yaml agreement — the snapshot conf carries the same topk/costs as the run conf, so scan-all-must-agree holds in practice.
  - Instrument hash is NOT derivable from a workspace (conf names the market; the resolved ticker list lives in the store) — US-008 must record hash_instruments(list) in pipeline_status.json at launch, and US-011 must fetch the incumbent's hash from its own launch record (promotion_history config is the natural home).
  - pyright chokes on pd.bdate_range(...)[-1] (.date() on the union type) — wrap with pd.Timestamp(x) + pyright ignore[reportArgumentType], same as gpu_trace.py.
  - US-011 wiring notes: gate fail must never fail the run; snapshot failure blocks auto-promote (do NOT reuse orchestrator/promotion.py's warn-and-promote); kill-switch key promotion_gate.auto_promote is NOT yet in config.yaml (add it in US-011, load_gate_config currently ignores unknown keys).
---
---
## 2026-08-15 15:35 UTC - US-008
- Rolling TEST_END with reserved confirmation window: ops/gpu_pipeline.py new compute_run_dates() (RDQ_TEST_END = store calendar end minus confirm_days TRADING days; default 42 via --confirm-days / RDQ_CONFIRM_DAYS) and resolve_instrument_hash() (promotion_gate.hash_instruments over the store's resolved instrument list — span-format rows dedup to symbols). Both computed at the TOP of run_pipeline, before provisioning, so a broken store/universe fails at $0. Status file (pipeline_status.json) records test_end, confirmation_window=[confirm_start, store_end], confirm_days, instrument_hash; new pure build_run_args()/format_run_start() carry --test-end to gpu_worker.sh and state the window+hash in the run-start Slack post. gpu_worker.sh run gains --test-end (ISO-validated at parse time, before load_state) exporting RDQ_TEST_END in the launch script. run_us_quant.sh new check_test_end_lag (MAX_TEST_END_LAG_DAYS=90, calendar days vs store calendar end, strictly-over fails) wired into BOTH check_mode and run_mode; hardcoded fallback kept.
- Files changed: ops/gpu_pipeline.py, ops/gpu_worker/gpu_worker.sh, ops/run_us_quant.sh, tests/test_gpu_pipeline.py, tests/test_gpu_worker.py, tests/test_run_us_quant.py, tests/test_services.py, ops/CLAUDE.md.
- Verified: make check green (1154 passed / 3 skipped); shellcheck clean on both scripts. E2E on the live store (read-only): compute_run_dates -> test_end 2026-06-12, confirmation window 2026-06-15 → 2026-08-13 (store end), us_liquid hash 6fbafedc13ed9a52 (581 names); ops/run_us_quant.sh --check passes with fallback 2026-07-10 (lag 34d) and FAILS with RDQ_TEST_END=2026-01-02 ("223 days behind the store calendar end"); StatusFile round-trip shows all four new fields surviving later stage updates.
- **Learnings for future iterations:**
  - The status-file fields persist across updates because StatusFile.update() MERGES (dict.update) — set launch-time facts once, they survive to stage=finished. US-010/US-011 read test_end/confirmation_window/instrument_hash from pipeline_status.json (never re-derive; the hash is not derivable from a workspace).
  - gpu_worker.sh cmd_run parses args BEFORE load_state — new-arg validation is testable offline (run_script("run", "--test-end", "bogus") with an empty state dir).
  - run_us_quant.sh check-mode tests that fail before the onecli leg don't need the onecli skipif; assert on specific stderr text, and for "passes this leg" assertions check the PASS line + absence of the failure text (later legs may fail for unrelated env reasons).
  - execution.signal._read_calendar is the shared calendar parser (validates ISO, raises on empty) — reuse it rather than re-reading day.txt; entries needed as strings via .isoformat().
  - The computed rolling TEST_END (~60 calendar days behind store end at confirm_days=42) sits well inside the 90-day guard — the guard only bites rotted hardcoded fallbacks or frozen stores.
---
## 2026-08-15 15:20 UTC - US-009
- New ops/confirm_window.py: confirmation_returns(workspace, window_start, window_end) drives the exact-weights re-predict (US-049 docker machinery byte-for-byte: snapshot env + test_end override + fresh predict-script copy + docker_command) and simulates TopkDropoutStrategy over the window's trading days -> WindowReturns (net + gross daily returns, evaluated window, repredicted flag). Docker is SKIPPED when the workspace's newest pred.pkl already covers every needed signal day (deterministic inference => covering pred is the same answer; the incumbent's daily refresh usually spares its run, a fresh candidate always re-predicts). Simulation: day d's return goes to the book selected from pred d-1 (live-rebalancer timing), close-to-close on ADJUSTED store closes, equal-weight, open/close_cost charged on traded fractions, min_cost ignored (absolute-dollar, identical both sides). Every gap raises ConfirmWindowError (missing snapshot files, window outside store calendar, docker failure, pred still not covering, missing prices) — never a silent None. annualized_ir (mean/std·sqrt(252), compute_sharpe convention) is the US-010 criterion metric.
- Files changed: ops/confirm_window.py (new), tests/test_confirm_window.py (new, 17 tests), ops/CLAUDE.md, scripts/ralph/prd.json.
- Verified: make check green (1171 passed / 3 skipped). E2E on-box: (1) read-only on the live promoted c9587797 — 11 trading days 2026-07-30->08-13, repredicted=False, plausible net returns; (2) forced REAL docker re-predict on e05ad9b4 (~7 min container) — returns computed, and the re-predicted pred reproduces that workspace's original backtested pred EXACTLY (spearman 1.0000, top-50 overlap 50/50 on common days), validating the whole chain.
- **Learnings for future iterations:**
  - INCIDENT FOUND (pre-existing, NOT from this change; Slack alert posted 2026-08-15): the currently promoted c9587797's snapshot re-predict is DEGENERATE — its conf_pred_refresh.yaml is the combined-factors variant whose features come from StaticDataLoader over combined_factors_df.parquet, and the refreshed pred collapses to 37 unique values across 581 names (419 exactly ~0; spearman vs the backtested pred ~0.08, top-50 overlap 6-10/50). locate_pred picks that refresh run (newest mtime), so the 2026-08-14 rebalance traded it and Monday 2026-08-17 will too unless fixed/rolled back. e05ad9b4 (snapshot conf without parquet deps — features recomputed from the store) reproduces EXACTLY, so US-049 machinery is sound for expression-based confs; the parquet-based snapshot path is what's broken.
  - Implication for US-010/US-011: a confirmation evaluation is only meaningful if the re-predict REPRODUCES the strategy — add a reproduction check (rank-correlate re-predicted scores vs the workspace's original pred on overlap days; require ~1) before trusting confirmation returns, else a degenerate re-predict silently passes "technically successful" evaluation.
  - qlib ret.pkl semantics: headline IR/ARR in qlib_res are EXCESS-vs-benchmark (return - bench - cost annualized ~1.64 for c9587797) while raw net compounds much higher — don't compare my absolute annualized_ir against qlib's IR number; US-010 compares candidate-vs-incumbent through the SAME evaluator so this stays internally consistent.
  - My sim deliberately differs from qlib backtest: equal-weight daily re-weighting (qlib lets winners drift, only trades adds/drops) and 1-day-earlier signal timing (live-rebalancer convention). On a healthy workspace book overlap with qlib positions is limited mainly by the drift difference — comparability across strategies is preserved because both sides run the same code.
  - The store bins' element 0 is a calendar-index OFFSET header (real symbols have nonzero offsets — tests' write_bins uses 0); returns must use ADJUSTED closes directly (raw = close/factor is for order pricing, not returns).
  - 2026-07-30 was a genuine monster-dispersion day in the store (MSFT +15.5%, MU +18.4%, CHRW -15.4%) — not a data bug; don't "fix" it.
## 2026-08-15 16:30 UTC - US-010
- Confirmation-window criterion in the gate: evaluate_gate gains `confirmation: ConfirmationEvidence | None` — REQUIRED whenever an incumbent exists (None, evidence.error, or a degenerate IR all yield a failing "confirmation" criterion whose reason starts confirmation_unavailable; a technical failure can never read as a pass). Pass leg: candidate confirmation-window IR strictly > incumbent's × confirm_ir_margin (new GateConfig field + promotion_gate: config key, default 1.0 = no margin). No-incumbent path unchanged (allow_first waives comparisons, confirmation included). GateVerdict gains confirmation + window fields (both in to_json); slack_text now shows the test window on the parity line and a "confirmation window a → b: candidate `tag` IR x (Nd, re-predicted), incumbent ..." detail line. New IO half load_confirmation_evidence(cand, inc, start, end, **confirm_kwargs): incumbent FIRST (docker usually skipped there — cheap failures before the candidate's ~7-min re-predict), ConfirmWindowError lands in evidence.error naming the side, never raises.
- Reproduction check (the US-009 incident follow-up): ops/confirm_window.confirmation_returns now verifies the pred it uses — whenever that pred is NOT the workspace's original backtested pred (oldest mlruns/**/pred.pkl by mtime), mean spearman on sampled overlap days (up to 10, >=3 common names) must be >= min_reproduction (0.98, new kwarg) or ConfirmWindowError. Catches BOTH degenerate re-predicts and degenerate cached daily-refresh preds (the c9587797 shape); constant scores -> NaN spearman -> "cannot verify" error (loud, not a pass). WindowReturns gains reproduction field.
- Files changed: ops/promotion_gate.py, ops/confirm_window.py, orchestrator/config.yaml (confirm_ir_margin: 1.0), tests/test_promotion_gate.py (+10 tests, existing updated for the required-evidence contract), tests/test_confirm_window.py (+4 tests), ops/CLAUDE.md.
- Verified: make check green (1185 passed / 3 skipped). E2E on-box, fully read-only (runner that refuses docker): (1) load_confirmation_evidence(e05ad9b4 candidate, c9587797 incumbent, 2026-06-15→2026-08-13) → incumbent's cached refresh pred FAILS reproduction (mean spearman 0.127 vs 0.98) → evidence.error names incumbent → gate FAIL with confirmation_unavailable — the live incident is now mechanically caught; (2) healthy e05ad9b4 self-pair → full evidence (42 trading days, IR 1.6581, reproduction 0.9999972, repredicted=False), verdict honestly fails strict-> on equal IRs, Slack block carries both windows + both strategies, JSON round-trips.
- **Learnings for future iterations:**
  - evaluate_gate's contract CHANGED: with an incumbent, callers MUST pass ConfirmationEvidence (US-011's pipeline must call load_confirmation_evidence and pass the result; the kill-switch/report-only mode still gets an honest verdict). Window inputs come from pipeline_status.json confirmation_window (US-008) — never re-derive.
  - The incumbent's evidence leg is where the current live incident bites: until c9587797's parquet-conf snapshot is fixed or rolled back, EVERY gate run will fail confirmation_unavailable on the incumbent side (reproduction 0.127). That is correct behavior — do not "fix" it by lowering min_reproduction; fix the snapshot (US-011 may want to surface this state distinctly in Slack).
  - Reproduction reference = OLDEST mlruns pred.pkl by mtime (path tiebreak). Fetched workspaces keep worker mtimes (rsync -a), so the original backtested pred stays oldest even after refreshes/re-predicts on this box.
  - scipy's spearman warns (ConstantInputWarning) and returns NaN on constant input — wrap in warnings.catch_warnings and treat NaN as "unverifiable = error", never as pass.
  - pyright: tuple(x) on a tuple[str, str] | None widens to tuple[str, ...] — rebuild as (x[0], x[1]) to keep the pair type.
---
## 2026-08-15 17:30 UTC - US-011
- Auto-promotion on gate pass: new ops/gpu_pipeline.gate_and_promote runs as the pipeline's LAST stage (after chart + Notion write-up, so both still describe the pre-promotion world) and NEVER raises. Flow: load_gate_config (new auto_promote kill-switch field on GateConfig + config.yaml key, default true) → load_metric_bundle on candidate (launch hash) and incumbent (hash from its promotion record's universe_tickers via hash_instruments — the only place the incumbent's resolved list survives) → load_confirmation_evidence over the launch-reserved window (RunDates.confirm_start → store_end) → evaluate_gate. Verdict block + outcome line posted as one Slack message; status file gains gate (verdict dict or {"error": ...}) and auto_promoted fields. On PASS + auto_promote: promotes via new ops/promote_fetched.promote_workspace — the extracted shared write path (validate → tickers → db guard → snapshot_pred_refresh → set_promoted_strategy) — with source='auto_gate' and verdict.to_dict() in promotion_history; success post names the replaced workspace and the exact rollback command. Snapshot failure raises BEFORE the pointer flip → :rotating_light: post, run finalizes UNPROMOTED, incumbent untouched. Gate fail posts "failing criteria: ..."; gate error posts a warning; neither fails the run. Kill-switch → report-only post with the manual promote command.
- Files changed: ops/gpu_pipeline.py, ops/promote_fetched.py (promote_workspace + PromotionResult; main rewired through it), ops/promotion_gate.py (GateConfig.auto_promote), orchestrator/config.yaml, tests/test_gpu_pipeline.py (+6 TestGateAndPromote), tests/test_promotion_gate.py (+1 config test), ops/CLAUDE.md, scripts/ralph/prd.json.
- Verified: make check green (1192 passed / 3 skipped). Scenario tests cover promote-on-pass (REAL promote_workspace + real snapshot on the promote_fetched fixture; history row source=auto_gate with verdict + replaced_workspace), no-promote-on-fail, parity-mismatch (via launch-hash disagreement), snapshot-failure (pointer + history untouched), kill-switch report-only, gate-error-never-raises. E2E on-box against a COPY of the live DB with real loaders: e05ad9b4-as-candidate vs live c9587797 → parity ALL MATCH (recorded-tickers hash == launch hash 6fbafedc13ed9a52 — the plumbing works on real records), IR/IC pass, MDD fail, confirmation fails confirmation_unavailable on the real degenerate-refresh incident → honest FAIL, promoted=False, DB copy byte-identical semantics (pointer + history unchanged), no exception.
- **Learnings for future iterations:**
  - promote_workspace is now THE promotion write path — US-012's Decision Log + --force work should extend it (or wrap it), not fork it; its ordering (snapshot before pointer flip) is load-bearing for the snapshot-failure-blocks-promotion guarantee.
  - The incumbent's parity hash comes from promotion config universe_tickers, NOT from any workspace file — on the real c9587797 record those are the full 581-name us_liquid list (the frozen us_liquid_promoted_30 pin lives only in its snapshot conf), so parity against a new us_liquid run genuinely matches.
  - Cross-test-module imports are the repo convention (from tests.test_promote_fetched import make_workspace) — reuse fixtures instead of cloning them.
  - gate_and_promote's loaders are imported INSIDE the function from ops.promotion_gate, so tests monkeypatch ops.promotion_gate.load_metric_bundle / load_confirmation_evidence and the real evaluate_gate still runs — good pattern for testing IO-wrapped pure cores.
  - The live gate outcome today: any auto-promotion attempt will fail confirmation_unavailable until the c9587797 degenerate snapshot is fixed or rolled back (that is CORRECT behavior; the Slack verdict names the exact failing pred path).
  - RecordingSlack(SlackThread) subclass with enabled=False + overridden post() satisfies pyright where a duck-typed fake would not.
---
## 2026-08-15 18:20 UTC - US-012
- Audit trail on all promotion paths: (1) NEW ops/promotion_decision.py — Decision Log rows for ops-side promotions: build_payload (JSON-safe, from PromotionResult) + decision_title/details + write_decision (routes through NotionRecorder.record_decision so the one-writer-per-DB convention holds; refuses to CREATE state.sqlite) + record_via_onecli (subprocess hop `onecli run --agent rdq-orchestrator -- python -m ops.promotion_decision --payload <tmp json>`, the notion_writeup pattern — the Notion bearer only injects under that identity). (2) promote_fetched: runs the gate in ADVISORY mode (evaluate_advisory_gate — candidate hash from the CURRENT store list, confirmation window re-derived via gpu_pipeline.compute_run_dates, candidate snapshotted pre-gate only on --yes when missing; returns (verdict|None, error), never raises); non-PASS blocks --yes unless --force; the stored history gate_verdict carries "forced": true/false ({"error":..., "forced": true} when the gate itself errored); Decision Log written post-promotion (--no-notion skips; failure falls back to the manual reminder). (3) gpu_pipeline._auto_promote writes the same Decision Log row (source auto_gate, thread_ts threaded through gate_and_promote from options), guarded so bookkeeping can never fail the run. (4) orchestrator/promotion.py: confirmation text now shows the incumbent's own IC/ARR/MDD + test window (_incumbent_context via ops.gpu_trace, degrades to "unavailable"); Decision Log details append GATE_NOT_EVALUATED. (5) promotion_gate: gate_summary_line(verdict_dict, forced=, error=) + GATE_NOT_EVALUATED shared by all three sources.
- Files changed: ops/promotion_decision.py (new), ops/promote_fetched.py, ops/gpu_pipeline.py, ops/promotion_gate.py, orchestrator/promotion.py, tests/test_promotion_decision.py (new, 16 tests), tests/test_promote_fetched.py (+advisory/force/decision tests, autouse record_via_onecli guard), tests/test_promotion.py (+incumbent-context, history-source, gate-line asserts), tests/test_gpu_pipeline.py (autouse decision capture + payload asserts), ops/CLAUDE.md, orchestrator/CLAUDE.md, scripts/ralph/prd.json.
- Verified: make check green (1218 passed / 3 skipped). E2E on-box: (1) real dry-run `ops.promote_fetched --workspace e05ad9b4… --no-slack` against the LIVE DB/store — advisory verdict rendered (parity ALL MATCH incl. both hash derivations, IR pass, MDD fail, confirmation_unavailable on the real c9587797 degenerate refresh), --force hint printed, nothing written; (2) real Decision Log row written via the exact record_via_onecli command (page 3bd9b1a4-36cf-81d3-83df-e0d6d6a5a991, marked US012-VERIFY), then archived via NotionClient.update_page(archived=True).
- **Learnings for future iterations:**
  - Notion writes from ops-side processes MUST hop through `onecli run --agent rdq-orchestrator` (only that identity has the bearer); ops/promotion_decision.record_via_onecli is the reusable hop — and any test that reaches a promotion path must monkeypatch it (on this box onecli EXISTS, so an unpatched call writes REAL Notion rows from pytest; test_promote_fetched/test_gpu_pipeline now have autouse guards).
  - Archived Notion pages are the sanctioned cleanup for verify rows (invisible to queries); NotionClient.update_page(page_id, archived=True) works under the same identity.
  - KNOWN GAP (pre-existing, US-011): the pipeline's gate never snapshots the candidate, and confirm_window refuses workspaces without snapshot files — a FRESH candidate (never promoted) will fail confirmation_unavailable in the AUTO gate even when healthy. promote_fetched --yes now snapshots pre-gate (same files promote_workspace rewrites); gate_and_promote needs the same call (one guarded snapshot_pred_refresh before load_confirmation_evidence) if auto-promotion should ever actually fire on a fresh candidate.
  - PromotedStrategy.config["universe_tickers"] → hash_instruments is the ONLY parity-hash source for anything promoted (used by both the pipeline and evaluate_advisory_gate); a promotion with no pinned tickers yields hash None → parity honestly fails.
  - test_promotion.py history fixture note: StateStore fixtures there predate US-005 backfill semantics — list_promotion_history() on the `store` fixture is empty until a promotion happens in-test (no backfill because promoted_strategy starts empty).
---
## 2026-08-15 19:05 UTC - US-013
- Machine-readable run_summary on Strategy Notes: ops/notion_summary.py gains RUN_SUMMARY_SCHEMA_VERSION=1, loop_outcome (decision True/False/None -> SOTA/rejected/failed), build_run_summary(context) (directive, run status, universe name+instrument_hash, windows {test, test_end, confirmation}, per-hypothesis outcomes+metrics, winner incl. IR), run_summary_blocks (ONE fenced `json` code block; rich_text chunked at 1900 chars, split into extra blocks past Notion's 100-elements-per-block cap), and parse_run_summary (reader: reassembles all json code blocks, handles both write shape text.content and API-read shape plain_text, returns None on anything unparseable). create_summary_page appends the summary block after the prose. ops/gpu_pipeline.py: context building extracted to pure build_notion_context with new fields (loops, run_status via new run_status() helper — also reused in finalize_run_row — instrument_hash, test_end, confirmation_window); notion_writeup passes dates/hash/exit_code from run_pipeline.
- Files changed: ops/notion_summary.py, ops/gpu_pipeline.py, tests/test_notion_summary.py (+9 TestRunSummary tests, create-page test now round-trips the block), tests/test_gpu_pipeline.py (+TestNotionContext, 3 tests), ops/CLAUDE.md, scripts/ralph/prd.json.
- Verified: make check green (1230 passed / 3 skipped). E2E against REAL Notion under `onecli run --agent rdq-orchestrator`: created a Strategy Notes row with a ~7k-char run_summary (4 rich_text chunks in one code block), read the page children back via GET /v1/blocks/{id}/children, parse_run_summary == the written summary exactly (schema_version 1), page archived (id 3bd9b1a4-36cf-81cc-b07f-c19800eaf034).
- **Learnings for future iterations:**
  - Notion ACCEPTS multi-element rich_text in a `code` block and returns it re-readable; the API read shape has BOTH text.content and plain_text — parse_run_summary prefers text.content, falls back to plain_text.
  - The write-up subprocess sees ONLY the context JSON file — new run_summary fields must be added in gpu_pipeline.build_notion_context, never read from disk inside notion_summary.
  - US-014 reads these: query strategy_notes DB, GET each page's block children (client._request GET /v1/blocks/{page_id}/children?page_size=100 — no public wrapper for generic children yet), parse_run_summary, degrade on None. Winner metrics carry IR (workspace_metrics labels via METRIC_SPECS include IR).
  - Verify-row cleanup: NotionClient.update_page(page_id, archived=True) (US-012 pattern); title 'US013-VERIFY' marks it if archive ever fails.
---
## 2026-08-15 20:00 UTC - US-014
- Run-history digest builder: new orchestrator/run_memory.py build_digest(db_path, client, max_rows=10, max_chars=4000, notion_budget=15.0, clock=...) -> str. Notion side: query strategy-notes DB (sorts Run Date descending, newest 10), fetch each page's block children via NEW NotionClient.list_block_children (paginating GET /v1/blocks/{id}/children), parse_run_summary per row -> rich entry (header [date | status | universe] + directive, winner + metrics line, per-hypothesis outcome list with SOTA/rejected/failed counts). Incumbent section from local state: promoted pointer + workspace artifacts (metrics/window/factors via gpu_trace, model via NEW gpu_trace.workspace_model reading task.model.class, sota-conf preferred). Fallbacks: any Notion failure (client build, config, query) -> local runs+directives entries (directive + status only); a single row without parseable JSON degrades just that row (status matched from local runs); budget deadline checked before the query and before each children fetch; store opened only when the file exists (never creates state.sqlite); outermost try returns NO_PRIOR_RUNS. Deterministic output, truncation drops OLDEST entries first with an "(+N older runs omitted)" marker, hard clamp at max_chars.
- Files changed: orchestrator/run_memory.py (new), orchestrator/notion_client.py (+list_block_children), ops/gpu_trace.py (+workspace_model), tests/test_run_memory.py (new, 16 tests), tests/test_gpu_trace.py (+4), tests/test_notion_client.py (+2), orchestrator/CLAUDE.md, ops/CLAUDE.md, scripts/ralph/prd.json.
- Verified: make check green (1251 passed / 3 skipped). E2E on-box: (1) under onecli rdq-orchestrator against REAL Notion + live DB -> 5 strategy-note rows newest-first, incumbent section fully populated (GeneralPTNN, 4 downside factors, IR 1.6391, window 2025-01-02→2026-07-10), 2.9s; all real rows predate US-013 so each honestly degrades to directive+status ("no run summary available") — statuses correctly matched from local runs; (2) bare python (no bearer -> Notion 401) -> full local fallback, 10 runs with directives+statuses, 2.2s, never raised.
- **Learnings for future iterations:**
  - LIVE DATA GOTCHA: the Notion Strategy Notes row's Directive property is the FULL run instruction (clipped by Notion at 2000 chars) while the local directives.objective is often a shorter PREFIX of it — exact directive equality never matches across the two stores; run_memory matches on mutual whitespace-normalized prefix. Anything else correlating Notion rows with local runs has the same problem (no thread_ts on Notion rows — consider adding one to note_properties someday).
  - No real Notion row carries run_summary JSON yet (all predate US-013's deploy) — the first post-deploy GPU run will be the first rich digest entry. US-015 can proceed regardless; the degraded entries still carry directive+status.
  - StubNotion pattern: mock at the client-method boundary (query_db/list_block_children) and serve children built by the REAL writer (build_run_summary + run_summary_blocks) so the reader/writer contract is tested end-to-end without HTTP.
  - build_digest's default NotionClient is deliberately snappy (timeout 5s, max_retries 1): the 15s budget only checks BETWEEN requests, so per-request bounds are what actually caps a stall.
  - tests/test_gpu_trace.py write_metrics/write_ret/write_factors are reusable workspace-artifact fixtures (cross-module import per repo convention).
  - US-015 wiring notes: call build_digest() in ConversationCore.start_research/GpuBackend.launch (orchestrator process has the Notion connector; no onecli hop needed); count entries for the "N prior runs included" Slack line — entries are separated by blank lines after the incumbent block, or better, expose a count by refactoring _build if needed.
---
## 2026-08-15 20:45 UTC - US-015
- Digest + incumbent injected into RDQ_USER_INSTRUCTION: orchestrator/run_memory.py gains Digest(text, runs) + build_digest_details (build_digest is now a thin wrapper; _assemble returns the kept-entry count so `runs` honestly reflects post-truncation entries), compose_instruction(directive, digest, max_chars=8000) — directive ALWAYS first, only the digest is trimmed to fit (an oversized directive ships intact with the digest dropped; empty/NO_PRIOR_RUNS digest composes to the bare directive) — and split_instruction (partition on MEMORY_DELIMITER). ConversationCore: new digest_builder= ctor param (None = no injection, same opt-in safety pattern as gpu=), start_research gains include_memory (default true; false skips building the digest at all), composed instruction goes to GpuBackend.launch unchanged, and format_run_started + the tool result state "N prior run(s) included as context" (or "not included (clean-slate run)"). app.py wires digest_builder=lambda: build_digest_details(store.db_path). gpu_pipeline.build_notion_context now records the BARE directive via split_instruction so digest text never compounds into future digests (Notion run_summary directive = directive only; prefix matching in run_memory keeps working).
- Files changed: orchestrator/run_memory.py, orchestrator/conversation.py, orchestrator/app.py, ops/gpu_pipeline.py, tests/test_run_memory.py (+10), tests/test_conversation.py (+3, make_core digest_builder param), tests/test_gpu_backend.py (+1 passthrough), tests/test_gpu_pipeline.py (+1 strip test), orchestrator/CLAUDE.md, ops/CLAUDE.md, scripts/ralph/prd.json.
- Verified: make check green (1266 passed / 3 skipped). E2E on-box under onecli rdq-orchestrator against real Notion + live DB: build_digest_details -> runs=5 in 3.1s (1738 chars), compose_instruction output starts with the directive, delimiter present, split_instruction round-trips exactly; composed instruction 1899 chars (well inside the 8000 budget and the base64 argv path).
- **Learnings for future iterations:**
  - The instruction can now be directive + MEMORY_DELIMITER + digest — ANY new consumer that records or matches on the instruction must strip via run_memory.split_instruction first (build_notion_context does; the worker-side RDQ_USER_INSTRUCTION deliberately keeps the full composition — that's the point).
  - Digest.runs counts entries AFTER oldest-first truncation, so the Slack line never overstates what the model actually saw.
  - ConversationCore's None-default digest_builder keeps every existing test hermetic (a real default would hit api.notion.com from pytest with a 5s timeout per call); format_run_started(run) without memory_runs renders the "not included" line, so old equality asserts kept passing unchanged.
  - Deploy note: the live bot needs an rdq-orchestrator restart to pick up the digest wiring (memory: deploy-vs-running-process); no real Notion row carries run_summary JSON yet, so first digests will be degraded directive+status entries until a post-deploy run writes one.
---
## 2026-08-15 21:35 UTC - US-016
- New execution/divergence.py (US-016): daily live-vs-backtest divergence tracker. Pure `evaluate_divergence(daily_returns, equities, stats, config)` — realized = SUM of trailing window_days (20) daily returns, expectation = window_days × mu_adj where mu_adj = haircut (0.5) × backtest net daily mean, z = (realized − expected)/(σ·√window_days); drawdown since promotion = peak-to-trough on the equity series (promotion-day close as baseline). Escalation: z < warn_z (−2) → :warning: Slack post with realized/expected/drawdown; z < halt_z (−3) OR drawdown strictly over backtest |MDD| × mdd_tolerance (1.25) → writes the breaker halt file (execution/breaker.py DEFAULT_HALT_FILE — verified through a real Breaker in tests) + :rotating_light: notice naming trigger(s) and the clear procedure (resume_trading tool / delete the file). write_halt_file NEVER overwrites an existing halt (operator note wins; notice says "already present"). Warmup (<20 realized days) and nothing-promoted are SILENT exit 0 (daily job; only warn/halt/failure post). `load_backtest_stats` is deliberately loud (raises on missing ret.pkl / zero variance — degrade would silently disarm the halt); backtest MDD from qlib_res.csv key 1day.excess_return_with_cost.max_drawdown, fallback derived from the compounded ret.pkl curve. Config: new `divergence:` section in orchestrator/config.yaml (haircut/warn_z/halt_z/mdd_tolerance/window_days; loader mirrors load_gate_config, validates halt_z <= warn_z). Alpaca via `PortfolioReader` protocol (get_portfolio_history only); realized daily returns = profit_loss_pct of entries strictly after the promotion date; period picked by choose_period (3M/1A/all).
- Files changed: execution/divergence.py (new), orchestrator/config.yaml (divergence: section), tests/test_divergence.py (new, 34 tests), execution/CLAUDE.md.
- Verified: make check green (1300 passed / 3 skipped). E2E on-box under onecli rdq-exec-paper: (1) live DB → honest warmup (c9587797 promoted 2026-08-14, 0 realized days, exit 0, nothing posted/written); (2) DB COPY with promoted_at backdated to 2026-07-01 → real Alpaca history, z = 0.14, drawdown 2.83% vs limit 20.15% (= |−16.12%| × 1.25, matches the gate's incumbent MDD exactly) → ok, exit 0; (3) same copy + temp config (halt_z 0.2) → halt file written in /tmp with the trigger note, rotating_light notice with numbers + clear procedure. All temp artifacts removed; the real ~/rdq-data/breaker/halt was never touched.
- **Learnings for future iterations:**
  - pandas `.std()` of a CONSTANT series returns float residue (~1e-19), not 0.0 — guard degenerate distributions with `sigma < 1e-12`, not `== 0`.
  - StateStore stamps promoted_at itself; runner tests that need "promoted N days ago" derive promoted_date from the row and shift `as_of` forward instead of trying to pin the timestamp (or UPDATE a DB copy for on-box E2E, as done here).
  - tests/test_summary.py exports write_qlib_res_csv/write_ret_pkl fixtures, but hand-rolled deterministic returns (alternating ±1% → mean exactly 0) make z assertions exact — prefer that for math-heavy tests.
  - pyright covers tests/ — accept fakes via a Protocol (PortfolioReader, like conversation.py's BrokerReader) rather than typing the concrete AlpacaClient.
  - US-017 wiring notes: unit should run `python -m execution.divergence` as rdq-exec-paper-style identity (needs Alpaca creds via onecli) with NO_PROXY=slack.com; exit 0 covers ok/warn/halt/warmup, exit 1 = check failure (OnFailure notifier from US-018 would double-report — divergence already posts its own FAILED notice).
---
## 2026-08-15 16:30 UTC - US-017
- Divergence tracker systemd units: new ops/rdq-divergence.service (Type=oneshot, no [Install], runs `python -m execution.divergence` under `onecli run --agent rdq-exec-paper` with NO_PROXY=slack.com, After=network-online.target rdq-rebalance.service) + ops/rdq-divergence.timer (OnCalendar=Mon..Fri 16:30 America/New_York, Persistent=true, AccuracySec=1min). Persistent=true chosen deliberately: divergence is a safety MONITOR, so a check missed while the box was down should fire on boot (contrast rebalance's Persistent=false — trading is a scheduled act). 16:30 ET = post-close so Alpaca portfolio history already includes today's session. Wired into install_services.sh UNITS and health.sh TIMERS/ONESHOTS.
- Tests: tests/test_services.py new TestDivergenceUnits (exist, exec-paper identity + oneshot conventions, NO_PROXY line, After=rdq-rebalance.service, timer calendar/timezone/Persistent, rebalance-before-divergence timer ordering, systemd-analyze verify); install-script assertions extended; tests/test_health.py TIMERS/ONESHOTS mirrors updated (cross-check test enforces health.sh == install UNITS).
- Files changed: ops/rdq-divergence.service (new), ops/rdq-divergence.timer (new), ops/install_services.sh, ops/health.sh, tests/test_services.py, tests/test_health.py, ops/CLAUDE.md.
- Verified: make check green (1307 passed / 3 skipped); shellcheck clean. On-box: install_services.sh linked both units, `systemctl --user enable --now rdq-divergence.timer` -> active, next elapse Mon 2026-08-17 20:30 UTC (= 16:30 ET); manual `systemctl --user start rdq-divergence.service` exit 0, ExecMainStatus=0, journal shows the real computed result: "warmup — 0 realized trading day(s) since promotion (2026-08-14), need 20" (honest warmup; nothing posted to Slack, as designed). ops/health.sh now PASSes both new entries.
- **Learnings for future iterations:**
  - The unit ran fine against the LIVE DB from this working tree (divergence only reads promoted_strategy; migrations were already applied by earlier stories' on-box runs).
  - onecli logs a "No Anthropic credentials configured" warning for rdq-exec-paper — harmless noise (the agent makes no LLM calls), don't chase it.
  - `systemctl --user list-timers <timer>` prints NEXT in UTC — 20:30 UTC == 16:30 ET in August (EDT); don't misread it as a timezone bug.
  - US-018 note: divergence posts its OWN failure notice (exit 1 after Slack post) — when adding OnFailure=rdq-notify-failure@%n.service to every service, expect double-reporting on this unit (acceptable per US-016 notes, but consider it when writing the notifier).
  - health.sh's timer check fails until the timer is ENABLED on-box — enabling the timer is part of the story, do it before running health.sh or the daily health run (US-019) would page.
---
## 2026-08-15 16:40 UTC - US-018
- OnFailure Slack notifier for all rdq units: new templated ops/rdq-notify-failure@.service runs `python -m ops.notify_failure %i` (new module) — posts ":rotating_light: unit <name> failed" + the failed unit's last 10 journal lines (journalctl --user -u, short-iso; oldest-first truncation at 3500 chars) via execution.rebalance.slack_notifier (repo .env token, gpu_watchdog pattern — Slack never transits the OneCLI proxy, so no onecli wrapper despite the PRD's loose "through the OneCLI proxy" phrasing). journal_tail never raises (missing/failed journalctl → placeholder, headline still posts); Slack post failure → stderr + exit 1 (visible in journal only — by design, nothing left to notify with). Template has NO OnFailure (no recursion) and no [Install] (activated only via OnFailure=). All 8 rdq-*.service files gained `OnFailure=rdq-notify-failure@%n.service` in [Unit]; template added to install_services.sh UNITS.
- Files changed: ops/notify_failure.py (new), ops/rdq-notify-failure@.service (new), ops/rdq-{orchestrator,research,data-refresh,pred-refresh,rebalance,sweep,gpu-watchdog,divergence}.service, ops/install_services.sh, tests/test_notify_failure.py (new, 10 tests), tests/test_services.py (TestNotifyFailureUnits incl. a glob test asserting OnFailure on every non-template rdq-*.service), ops/CLAUDE.md.
- Verified: make check green (1323 passed / 3 skipped). On-box E2E: install_services.sh linked the template; `systemd-run --user --unit=rdq-notify-test -p Type=oneshot -p OnFailure='rdq-notify-failure@rdq-notify-test.service.service' /bin/false` → notifier instance ran, exited 0, and the real Slack post (headline + fenced journal tail) confirmed in #quant-research via the Slack API; cleaned up with reset-failed (unit gone). `list-units 'rdq-notify-*' --all` shows a loaded/inactive notifier instance per rdq service = every OnFailure reference resolves.
- **Learnings for future iterations:**
  - OnFailure=...@%n.service yields double-suffix instance names (rdq-notify-failure@rdq-rebalance.service.service) — valid and standard; %i inside = "rdq-rebalance.service", directly usable as journalctl -u argument.
  - systemd-analyze --user verify works fine on template unit FILES (ops/rdq-notify-failure@.service) — no instance needed; and verifying sibling units resolves OnFailure refs from the same directory, so the repo tree verifies before install.
  - The health.sh/install cross-check regexes (rdq-[a-z-]+\.(service|timer)) don't match "@" template names — template units need no health.sh list entry and don't break test_health_covers_every_installed_unit.
  - NOTE for US-026 (server_ui decommission): rdq-research.service now carries the OnFailure line; when deleting it, TestNotifyFailureUnits' >=8 services assertion drops to 7 — lower that bound in the same commit.
  - The channel currently carries a live incident discussion (c9587797 degenerate refresh; Claude-in-Slack recommended rollback to e05ad9b4, halt is on) — unrelated to this story, operator-owned; don't act on it from ralph iterations.
---
## 2026-08-15 17:00 UTC - US-019
- Scheduled reconcile + health timers. ops/reconcile.py gains: `--update` (repairs a field_mismatch ONLY when every differing field is fill-state — Status/Filled Qty/Filled Avg Price, the fill-poll-timeout case — patching broker truth via expected_ledger_fields/ledger_status; identity mismatches, orphans, duplicates, missing rows, and failed patches stay unresolved -> exit 1; fully-repaired run exits 0 with a FIXED line), `--notify` (one-line Slack summary via execution.rebalance.slack_notifier, posted ONLY when mismatches were found), `--lookback N` (start = end − N days; conflicts with --start). New helpers: diff_fields (compare_fields now formats from it), apply_fill_updates, _fill_update_properties (patches exactly the differing fields; {"number": None} clears a stale value). New units: rdq-reconcile.{service,timer} (Mon..Fri 16:15 America/New_York, After=rdq-rebalance.service, rdq-exec-paper + NO_PROXY=slack.com, runs `--update --notify --lookback 4`, Persistent=true — lookback makes catch-up cover missed days) and rdq-health.{service,timer} (daily 09:00 America/New_York incl. weekends, bare bash ops/health.sh, NO direct Slack — findings fail the unit and page via the US-018 OnFailure notifier, Persistent=true). Wired into install UNITS + health.sh TIMERS/ONESHOTS; ledger.py/CLAUDE.md "sole writer" wording amended (reconcile --update is the one other sanctioned Trade Ledger writer, broker truth only).
- Files changed: ops/reconcile.py, ops/rdq-reconcile.{service,timer} (new), ops/rdq-health.{service,timer} (new), ops/install_services.sh, ops/health.sh, execution/ledger.py (docstring), tests/test_reconcile.py (+9), tests/test_services.py (TestReconcileUnits, TestHealthUnits, install asserts), tests/test_health.py (list mirrors), ops/CLAUDE.md, execution/CLAUDE.md.
- Verified: make check green (1344 passed / 3 skipped). On-box E2E: install linked all units; both timers enabled+active (health next Sun 13:00 UTC = 09:00 EDT, reconcile Mon 20:15 UTC = 16:15 EDT). Read-only reconcile --lookback 4 found the REAL systemic staleness: 66/66 orders' rows stuck at submitted/qty 0 (pre-open day orders fill after the poll window — every row was stale). Manual `systemctl --user start rdq-reconcile.service` exit 0 / Result=success: journal shows 66 UPDATED + "FIXED ... 66 updated, 0 unresolved"; the :mag: one-line Slack summary confirmed in #quant-research via the Slack API; re-run now reports OK 66/66. bash ops/health.sh run directly: all 22 checks PASS except the pre-existing tailscale :8443 finding.
- **Learnings for future iterations:**
  - EVERY rebalance day's ledger rows are born stale (pre-open submission, fills happen at the open, poll times out first) — reconcile --update at 16:15 ET is what makes the ledger converge daily; don't "fix" record_final to block until the open.
  - OPERATOR-PENDING: the daily rdq-health timer WILL page #quant-research every morning (first fire Sun 2026-08-16 09:00 ET) about `tailscale serve :8443 -> 127.0.0.1:3200` (jph-master-tracker, flagged since US-003) until the operator either adds it to health.sh ALLOWED_SERVE + PLAN.md §1 or removes the mapping. Deliberately NOT allowlisted autonomously (exposure policy = operator decision, consistent with US-003).
  - A freshly enabled Persistent=true timer does NOT fire immediately (no stamp file = nothing missed); first run is the next scheduled elapse.
  - reconcile exit contract is now: 0 = matched OR fully repaired, 1 = unresolved mismatches, 2 = couldn't run. OnFailure pages only on exit 1/2 residue — the flatten.py caveat (liquidation orders have no ledger rows) would surface as missing_ledger_row exit 1, which is correct and intended to page.
  - Notion PATCH with {"number": null} clears a stale number property — used so a resolved mismatch can never leave a leftover value; select_property/number_property shapes come from orchestrator/notion_recorder (same as the ledger writer).
---
## 2026-08-15 18:10 UTC - US-020
- Global GPU run mutual exclusion: new ops/run_lock.py — atomic (O_CREAT|O_EXCL) lock at ~/rdq-runs/gpu_worker/run.lock recording unit + thread_ts + pid + acquired_at. Staleness = owning unit inactive AND pid gone (manual CLI runs have no real unit, the pid keeps their lock live); a stale/corrupt lock is broken automatically and the break is reported by the caller. run_lock.unit_name is now THE transient-unit naming convention (gpu_backend._unit_name aliases it). gpu_pipeline: acquire_pipeline_lock runs before ANYTHING (a refusal posts :no_entry: naming the holder, sets stage=refused, finalizes the thread's run row, and returns 1 WITHOUT running teardown — teardown would destroy the ACTIVE run's droplet); release in the existing finally, ownership-checked so a re-acquired lock is never deleted by the old owner. GpuBackend gains active_run_lock() (unit-active probe rides the injected runner; lock_file ctor param). ConversationCore: start_research refuses while any thread holds the lock, naming the owning thread; check_research_status's false "queued behind it" text replaced ("runs are never queued"); stop_run's GPU branch refuses unless the requesting thread owns the lock (cancel kills THE shared worker's tmux session); both paths post a :broom: note when they break a stale lock.
- Files changed: ops/run_lock.py (new), ops/gpu_pipeline.py, orchestrator/gpu_backend.py, orchestrator/conversation.py, tests/test_run_lock.py (new, 15 tests), tests/test_gpu_pipeline.py (TestPipelineLock, 6 tests), tests/test_gpu_backend.py (+3), tests/test_conversation.py (StubGpu lock fields, +4 tests), ops/CLAUDE.md, orchestrator/CLAUDE.md, scripts/ralph/prd.json.
- Verified: make check green (1372 passed / 3 skipped). E2E on-box (no run/lock/worker existed): (1) planted a live-owner lock (pid 1) at the REAL path and ran `python -m ops.gpu_pipeline --no-slack --reuse-worker` → honest refusal (exit 1, :no_entry: naming thread 9999.0001, status stage=refused, holder's lock untouched, no worker_sh calls); (2) real GpuBackend().active_run_lock() broke a stale lock (unit inactive + pid None → removed, broken info returned) and reported a live one (own pid) without touching it. Lock file cleaned up after.
- **Learnings for future iterations:**
  - The refusal path in run_pipeline is deliberately BEFORE the try/finally: the finally's `worker_sh destroy --force` operates on the shared worker.env, i.e. the ACTIVE run's droplet. Never move lock acquisition inside the try.
  - run_pipeline is E2E-testable offline: monkeypatch ops.gpu_pipeline.worker_sh (+ compute_run_dates to raise) and use thread_ts=None so finalize_run_row never opens the real state.sqlite (it guards on DEFAULT_DB_PATH existing — which on this box it DOES).
  - `--reuse-worker` is the safe harness for on-box pipeline E2E: even if the code under test proceeded past the point you expect, there is no provisioning leg to bill.
  - Liveness probes in tests: a "live" pid = os.getpid() (or pid 1 on-box), a "dead" owner = pid None; is_active is injectable everywhere (never rely on systemctl state in unit tests).
  - StubGpu.active_run_lock consumes broken_lock once (mirrors the real one-shot break) — set gpu.lock for a holder, gpu.broken_lock for a stale-break scenario.
  - US-021 (stale run-row reaper) hook: a reaped unit's lock will read stale here and self-clean on the next start_research/stop_run/pipeline acquire — the reaper does NOT need to touch run.lock.
---
## 2026-08-15 19:05 UTC - US-021
- Stale GPU run-row reaper: new orchestrator/run_reaper.py (GpuRunReaper) runs as a third background thread in app.py (ApprovalsBridge pattern, 5-min ticks). Each tick: list_runs(status='running') filtered to backend='gpu'; a run whose rdq-gpu-run-<ts> unit is inactive starts an in-memory grace clock (default 15 min, injectable clock); past grace it posts a :headstone: note in the run's Slack thread (names the unit + journalctl pointer + "start_research can launch a fresh run") and flips the row to 'failed'. Note posts BEFORE the status flip (poller completion convention) so a Slack failure leaves the row running and the whole reap retries next tick; per-run try/except so one bad probe can't block the rest; unit recovery during grace resets the clock. app.py passes gpu.unit_active (the injected-runner probe) and starts reaper after poller/approvals.
- Thread unbricking: start_research now treats ONLY status='failed' as non-blocking (completed/stopped still refuse — promotion reads their session_path). StateStore.create_run gains replace_failed=True: DELETE ... WHERE status='failed' + INSERT in ONE transaction, so a concurrent live row still raises DuplicateRunError. Run-lock needs nothing: a reaped unit's lock reads stale and self-cleans on the next acquire (US-020 hook confirmed).
- Files changed: orchestrator/run_reaper.py (new), orchestrator/state.py (create_run replace_failed), orchestrator/conversation.py (start_research failed-row bypass + replace_failed=True), orchestrator/app.py (wiring), tests/test_run_reaper.py (new, 7 tests), tests/test_state.py (+3), tests/test_conversation.py (+2 incl. the full reap->relaunch loop), orchestrator/CLAUDE.md.
- Verified: make check green (1384 passed / 3 skipped). E2E on-box with the REAL systemctl --user probe (temp DB, unit rdq-gpu-run-9999-000123 nonexistent, grace 2s): tick 1 starts grace, tick 2 reaps (note rendered, row failed), create_run(replace_failed=True) then succeeds — thread relaunched. Deploy note: rdq-orchestrator restart required for the reaper thread to exist (memory: deploy-vs-running-process).
- **Learnings for future iterations:**
  - StateStore.delete_run exists but is production-unused (only a test touches it) — replace_failed was added instead because delete-then-create across two transactions would race a concurrent start; single-transaction DELETE(status='failed')+INSERT keeps the PK dedup guarantee.
  - The one-run-per-thread refusal in start_research is now STATUS-SENSITIVE: failed frees the thread, completed/stopped/running block. US-027/US-028 (poller/server_ui removal) must keep that distinction when touching duplicate_run_message.
  - StateStore(db_path=...) does NOT mkdir parents — tests using tmp_path subdirs must mkdir first (sqlite 'unable to open database file' otherwise).
  - Background-thread modules follow the ApprovalsBridge shape (start idempotent / _stop Event / daemon thread / tick() separated for tests) — the reaper copies it; tick() returning the acted-on ids makes assertions trivial.
---
---
## 2026-08-15 18:20 UTC - US-022
- GPU base-snapshot auto-use with rebake on drift: new ops/gpu_snapshot.py owns the worker-inputs hash (sha256[:12] over research/PINNED_COMMIT, ops/gpu_worker/gpu_worker.sh, the Makefile's venv/install target slice, and a STRUCTURAL store marker — feature field names + calendar file names, never content, so the daily data refresh can't churn it), BaseImage listing via doctl JSON, select_snapshot (newest image matching hash AND region), newest_in_region (manual-provision fallback), and prune_snapshots (keep newest 2, failed deletes skipped). CLI: python -m ops.gpu_snapshot hash|select|prune. Image name: rdq-gpu-base-<hash>-<YYYYmmdd-HHMM>; legacy unhashed names parse to inputs_hash None and never match.
- gpu_pipeline: PipelineOptions.snapshot (bool) → snapshot_mode ("auto" default / "bake" / "off" via --no-snapshot); provision_with_fallback picks the boot image PER size-plan region (match → snapshot id, no match → stock DEFAULT_GPU_IMAGE explicitly — never a stale-hash snapshot) and returns booted_from_snapshot; run-start Slack message gains a Worker: line ("booted from snapshot" / "full bootstrap (inputs changed — rebaking ... at teardown)"); rebake_needed + bake_base_snapshot run in the finally block BEFORE destroy (bake failure = Slack warning, never a run failure; reused workers never auto-rebake; kill conditions: off-mode, no hash, keep-worker, unbootstrapped). The old bake-after-check --snapshot stage is gone. main() split into build_options + run_pipeline call so tests can parse a real GpuBackend.launch argv (asserted → snapshot_mode auto, i.e. Slack runs get this by default with zero backend changes).
- gpu_worker.sh: latest_snapshot_id now delegates to `python -m ops.gpu_snapshot select --region "${RDQ_GPU_REGION}" [--hash ...]` (region-aware; any failure degrades to empty = default image); cmd_snapshot names the image with the hash (env RDQ_GPU_SNAPSHOT_HASH, else computes via the python module) and delegates pruning (keep 2) to the module.
- Files changed: ops/gpu_snapshot.py (new), ops/gpu_pipeline.py, ops/gpu_worker/gpu_worker.sh, ops/gpu_worker/README.md, ops/runbook.md, ops/CLAUDE.md, tests/test_gpu_snapshot.py (new, 20 tests), tests/test_gpu_pipeline.py (+9 TestSnapshotBootFlow), tests/test_gpu_backend.py (+1 launch-argv plumbing), tests/test_gpu_worker.py (+2), scripts/ralph/prd.json.
- Verified: make check green (1416 passed / 3 skipped); shellcheck clean. E2E vs REAL doctl (read-only): account has 0 rdq-gpu-base images; `select --region tor1` (with and without --hash) prints empty exit 0; `prune` no-op exit 0; real worker-inputs hash 07b012d45126 — so the next research run boots the stock image, full-bootstraps, and bakes the first hash-tagged snapshot at teardown automatically.
- **Learnings for future iterations:**
  - Selection/prune logic lives ONLY in ops/gpu_snapshot.py; gpu_worker.sh delegates to the python CLI (`(cd "${REPO_ROOT}" && .venv/bin/python -m ops.gpu_snapshot ...)`). Never re-grow doctl output parsing in the shell — the whole point is one offline-testable implementation.
  - Python default args bind at def time: `def f(runner=subprocess.run)` ignores later monkeypatching of subprocess.run — inject the fake runner explicitly (FakeDoctl pattern in tests/test_gpu_snapshot.py).
  - The store marker must be STRUCTURAL (field/calendar file NAMES). Content-based markers would rebake daily; bootstrap rsyncs store content on every provision anyway, snapshot or not.
  - The inputs hash does NOT cover local_qlib:latest (bootstrap only ships docker images to workers that lack one) — after rebuilding that image, force `--snapshot bake`. Documented in both runbooks.
  - GpuBackend.launch needed NO change for default-on behavior: the pipeline's own argparse default is the plumbing. gpu_pipeline.build_options(argv) is the testable seam — parse the argv a backend builds and assert on the resulting PipelineOptions.
  - run_pipeline's finally-block additions must check bootstrapped/keep_worker flags — bake calls `gpu_worker.sh snapshot`, which power-cycles the droplet; never bake an unbootstrapped or kept worker.
---
## 2026-08-15 18:20 UTC - US-023
- Point-in-time ADV universe builder mode in data/make_universe.py: new `mode` per-universe config key (data/config.yaml us_liquid now `mode: pit`; everything else defaults last_window) + `--mode {pit,last_window}` CLI override + make_universe(mode=) kwarg. PIT semantics: membership re-evaluated on each month's FIRST trading day (month_start_indices over the store calendar) from trailing adv_window-day ADV (raw dollar volume = stored close*volume) and that day's raw price (close/factor); one-period entry/exit hysteresis = a state flip needs the opposite signal on 2 CONSECUTIVE evaluations (member's span starts at the confirming eval date, ends the trading day before a confirmed exit); pre-listing evals are skipped, post-delist evals fail, and every span end clamps to the ticker's own all.txt end. Output via new write_instrument_rows (SYMBOL\tstart\tend, sorted, multi-row-per-symbol legal); legacy write_instruments_file is now a wrapper over it, legacy filter path byte-identical. PIT on a filterless universe refuses loudly. Drive-by: make_factor_source.read_universe_symbols dedups (span files would have produced duplicate symbols in daily_pv).
- Files changed: data/make_universe.py, data/config.yaml, data/make_factor_source.py, data/CLAUDE.md, tests/test_make_universe.py (+11 tests), scripts/ralph/prd.json.
- Verified: make check green (ruff OK, pyright 0 errors, 1427 passed / 3 skipped). E2E against the LIVE store via a /tmp mirror (symlinked features+calendars, copied all.txt — real instruments/us_liquid.txt deliberately untouched; on-box rollout is US-024): 589 tickers / 720 spans / 3 filtered in 5.1s; span-count distribution 514x1, 45x2, ... 1x7 (RRX); ALL 581 current live members retain PIT membership; PIT additionally admits 8 historically-liquid names (ARBK/BITF/BTBT/CAN/GREE/HIVE/PD/SERV — crypto-miner era liquidity, exactly the bias being fixed); late entrants exist (CACI/TDY/SAIC enter 2017-01-03; HUBS/KEYS 2017→today — US-024 spot-check candidates); zero span-end-past-ticker-end violations; AAPL = full store span 2015-01-02→2026-08-13.
- **Learnings for future iterations:**
  - The store actually starts 2015-01-02 (not 2016). First-eval partial-window tolerance (legacy liquidity_stats parity: nanmean over up-to-window bars) means long-liquid names get membership from the very first calendar day — no artificial month-1 lag.
  - Every universe-file consumer deduplicates symbols EXCEPT make_factor_source.read_universe_symbols (fixed here) — checked: gpu_pipeline.resolve_instrument_hash (hash_instruments dedups), promote_fetched.read_tickers (set), promotion._universe_tickers (set), orchestrator/universe._refuse_all_us (set). data/refresh.py read_universes still returns dup-carrying lists and its carry-across re-derives spans from all.txt — that's exactly US-024's first AC (multi-span preservation through refresh), don't regenerate us_liquid on-box before it lands.
  - PIT runtime on the full 660-ticker store is ~5s (3 bins per ticker, numpy) — cheap enough to regenerate whenever.
  - Scratch-store trick for read-only E2E: mkdir /tmp/x/instruments; symlink features/ and calendars/ from the live store; cp all.txt. make_universe writes only instruments/<name>.txt, so the live store never changes.
  - US-024 note: PIT admits MORE symbols than last_window (589 vs 581) because once-liquid names get their historical windows — the universe hash WILL change, and instrument_hash parity vs the incumbent (pinned frozen universe) will mismatch by design.
## 2026-08-15 21:40 UTC - US-024
- PIT universe rollout. Carry-across fix: data/refresh.py read_universes now returns FULL (symbol, start, end) rows (malformed lines raise RefreshError); new refresh_universe_spans advances a span's end to the ticker's new end ONLY when it tracked the ticker's pre-refresh last bar (open span), carries closed spans (PIT exits) verbatim — byte-identical to old behavior for legacy full-span files; build_store extra_instruments takes rows and writes them verbatim (span semantics live in refresh.py). extend_store passes rows through untouched (existing tickers never change in an extend).
- On-box rollout: instruments/us_liquid.txt regenerated PIT (589 tickers / 720 spans / 3 filtered: DGHI, SLNH, WIT). Spot-check recorded: HUBS and CACI enter 2017-01-03 (store starts 2015-01-02); SAIC has 6 spans, TDY 3. us_liquid_promoted_30.txt untouched (mtime 2026-08-14 + new test).
- Bias magnitude (docs/decisions.md 2026-08-15): e05ad9b4 exact-weights re-predict via confirmation_returns(force_repredict=True) on last-window vs PIT universe, window 2025-01-03→2026-08-13 (403 trading days), net: ARR 83.1%→67.2% (−15.9pp), IR 2.011→1.759, MDD −38.2%→−38.0%; reproduction 0.999997 / 0.999868; runtime 128s / 106s docker + seconds of sim. c9587797 not usable (degenerate snapshot incident 2026-08-15) — re-run there once repaired.
- Files changed: data/refresh.py, data/build_store.py, data/CLAUDE.md, docs/decisions.md, tests/test_refresh.py (+3 tests, 3 updated), tests/test_make_universe.py (+1 test), scripts/ralph/prd.json.
- Verified: make check green (1431 passed / 3 skipped). New tests: multi-span PIT file through a refresh cycle (closed verbatim, open extended, exited-ticker span kept), extend_store span pass-through, malformed-line rejection, promoted-file untouched by regeneration. E2E on the LIVE store: regeneration + real double docker re-predict comparison above.
- **Learnings for future iterations:**
  - Two-universe backtest comparisons are cheap: copy the workspace to /tmp (32M), sed the `market: &market <name>` anchor in all conf_*.yaml, run confirmation_returns(force_repredict=True) — scores are per-instrument (universe change leaves common-instrument spearman ~1.0), so membership is the ONLY thing that differs.
  - e05ad9b4's snapshot env pins test_start=2025-01-01 — a re-predict can never produce pred days before that; window comparisons on it are bounded below by 2025.
  - The comparison universe (us_liquid_lastwin.txt) was DELETED from the store afterwards — any instruments/*.txt left behind is carried across every nightly refresh forever. Regenerate on demand with --mode last_window.
  - us_liquid's instrument list + hash changed with the rollout: gate parity will (correctly) mismatch pre-rollout incumbents vs post-rollout candidates. First post-rollout run establishes the new hash.
  - The nightly rdq-data-refresh (04:30 ET Mon-Fri) runs data/refresh.py from THIS working tree — the span-preserving carry-across must be committed before the next weekday refresh or PIT spans get flattened (committed here; next refresh is Mon 2026-08-17).
---
