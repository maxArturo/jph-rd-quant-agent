# Ralph Progress Log
Started: (not yet started)
---

## Codebase Patterns
- Python-only repo (>=3.10). "Typecheck passes" means the Python typechecker (pyright), not TypeScript.
- Quality gate: `make check` = ruff + pyright + pytest (configured in US-001; use it before every commit).
- Credentials: never in code or .env — bare HTTP calls through the OneCLI proxy (HTTPS_PROXY + CA cert); management API at http://127.0.0.1:10254.
- Paper trading only: no rdq-exec-live identity, no code path targeting api.alpaca.markets.
- Proxied calls from scripts: `onecli run --agent <identifier> -- curl ...`; banner on stderr, command stdout clean. See ops/CLAUDE.md for onecli CLI gotchas (UUID vs identifier, set-secrets replaces list).
- All services bind 127.0.0.1; human-facing UIs exposed via tailnet-only `tailscale serve` only.
- LLM calls go through LiteLLM under `onecli run` (placeholder keys OK — proxy overrides headers). claude-sonnet-5 rejects temperature!=1; JSON mode may return ```json fences. See research/CLAUDE.md.

## 2026-07-07 20:20 - US-001
- Implemented repo scaffold: orchestrator/, execution/, research/, data/, ops/, docs/reference/, tests/ (packages get __init__.py; non-package dirs get .gitkeep)
- pyproject.toml: requires-python >=3.10; dev extras pytest/pyright/ruff; tool config for pytest (with `live` marker for proxy smoke tests), pyright (standard mode, venv=.venv), ruff (E,F,W,I,UP,B; py310; line 100); explicit setuptools package include (flat layout breaks auto-discovery)
- README.md: standalone constraint (no nanoclaw; OneCLI at http://127.0.0.1:10254 only external service), links to PLAN.md and tasks/prd-rdagent-q-trading.md
- Makefile: `make check` = ruff check + pyright + pytest; `make venv` bootstraps
- tests/test_scaffold.py verifies directory layout and README constraints
- .gitignore for .venv, caches, future state.sqlite
- Verified: `make check` runs end-to-end and exits 0 (ruff "All checks passed", pyright "0 errors", pytest "2 passed")
- **Learnings for future iterations:**
  - Box had no make/shellcheck/python3-venv; passwordless sudo works — installed via `sudo apt-get install -y make shellcheck python3.12-venv`. shellcheck is now available for US-003.
  - Flat-layout repos need explicit `[tool.setuptools.packages.find] include=[...]` or editable install fails on "Multiple top-level packages" (plans/ dir trips auto-discovery)
  - Venv lives at .venv; Makefile calls tools via .venv/bin/ directly, no activation needed
  - pyright 1.1.411 downloaded its node runtime on first run without issue (network OK)
  - pytest exits 5 on zero collected tests — always keep at least one real test
---
## 2026-07-07 20:35 - US-002
- Pinned microsoft/RD-Agent to main HEAD 4f9ecb005881cddc08df0124a2e894c018007679 (2026-05-06; 28 commits past v0.8.0 release, unmoved for ~2 months = current stable). SHA in research/PINNED_COMMIT.
- research/install.sh (shellcheck-clean) installs `rdagent @ git+...@<SHA>` into .venv, validates the SHA format, verifies import. Deliberately NOT a pyproject dep (heavy tree: litellm/streamlit/docker).
- docs/decisions.md created with the pin rationale + rebase procedure; README dev section references install.sh.
- tests/test_rdagent_pin.py: SHA format, `import rdagent` succeeds, and pip's direct_url.json commit_id matches the pin.
- Verified: install.sh ran end-to-end; `pip check` clean; import exits 0; direct_url.json commit_id == pin; `make check` green (5 passed).
- **Learnings for future iterations:**
  - rdagent installs as version 0.8.1.dev28 (setuptools-scm) and `rdagent.__file__` is None (namespace-ish) — check `.venv/.../rdagent-*.dist-info/direct_url.json` for the true installed commit.
  - GitHub API reachable from this box (used to date commits); git ls-remote works for upstream SHAs.
  - pip resolved the git install near-instantly — wheel/cache friendly; rerunning install.sh is cheap.
  - rdagent's deps coexist cleanly with our dev tools in one venv (pip check clean; pyright still 0 errors, no slowdown).
---
## 2026-07-07 20:50 - US-003
- ops/setup_onecli.sh: registers rdq-orchestrator, rdq-research, rdq-exec-paper (idempotent — skips existing identifiers; NO rdq-exec-live, and it hard-refuses to assign any secret with the live Alpaca host pattern). Assigns vault secrets by hostPattern per the PLAN.md identity table; warns per missing vault secret and lists them at the end.
- ops/check_onecli.sh: one bare proxied request per (identity, service) — Anthropic /v1/models, Notion /v1/users/me, Alpaca paper /v2/account, OpenAI /v1/models, FMP /stable/search-symbol. PASS/FAIL per check; exits 1 listing missing secret assignments. Live-isolation proof: rdq-exec-paper vs https://api.alpaca.markets/v2/account must return 401/403 (2xx = hard fail).
- ops/CLAUDE.md added with onecli CLI gotchas for future ops scripts.
- Verified end-to-end (recorded outputs):
  - setup run 1: created all 3 identities, assigned 3/2/2 secrets; run 2: "exists:" for all three, same assignments (idempotent), exit 0 both times.
  - check run: PASS Anthropic x2 (HTTP 200), Alpaca paper x2 (HTTP 200), FMP (HTTP 200); PASS live isolation (HTTP 401 as required); FAIL Notion + OpenAI ("secret not in vault" — those keys are vaulted in later stories US-004/US-025); exit 1 listing exactly those two missing assignments. Failure behavior is per spec.
  - shellcheck clean on both scripts; make check green (ruff pass, pyright 0 errors, 5 pytest passed).
- **Learnings for future iterations:**
  - OneCLI on this box is v2.2.0; management API reachable at http://127.0.0.1:10254; jq 1.7 + curl + shellcheck preinstalled.
  - Vaulted today: Anthropic (api.anthropic.com), Alpaca paper key+secret (paper-api.alpaca.markets, two separate secrets — assign BOTH), FMP query-param key (financialmodelingprep.com). NOT vaulted yet: Notion (api.notion.com), OpenAI embeddings (api.openai.com) — US-004/US-025 must vault these (web UI) before their smoke tests pass, then rerun setup_onecli.sh to assign.
  - `onecli run` banner goes to stderr; capture stdout only for `%{http_code}`. Anthropic GET /v1/models 400s without `anthropic-version: 2023-06-01`.
  - `onecli agents secrets/set-secrets` want the agent UUID, not the identifier; set-secrets REPLACES the whole list.
  - Live Alpaca host with no injected creds returns 401 — the isolation proof works today without any live secret existing in the vault.
---
## 2026-07-07 (manual, between iterations) - Embedding provider switched OpenAI -> Voyage AI
- Decision recorded in docs/decisions.md: EMBEDDING_MODEL=voyage/voyage-3.5-lite (host api.voyageai.com, key VOYAGE_API_KEY). No OpenAI APIs anywhere in the stack.
- Updated: PLAN.md, prd.json US-004 acceptance criteria, ops/setup_onecli.sh (rdq-research hosts), ops/check_onecli.sh (Voyage probe = minimal POST /v1/embeddings; probe() now takes an optional POST body as 5th CHECKS field).
- Supersedes earlier notes referring to "OpenAI embeddings (api.openai.com)": vault the VOYAGE key instead, then rerun setup_onecli.sh.
- **Learnings for future iterations:**
  - Voyage has no GET endpoint; auth checks must POST a 1-token embedding request.
  - US-004 must use voyage/voyage-3.5-lite via LiteLLM (native voyage/ provider - no OPENAI_API_BASE needed) and placeholder ANTHROPIC_API_KEY + VOYAGE_API_KEY in research/.env.example.
---
## 2026-07-08 (manual, between iterations) - Human blockers cleared for US-004, US-006, US-025/026
Operator provisioned three things; PRD notes + docs/decisions.md updated to match. State verified empirically against the live OneCLI gateway.
- **Voyage AI key vaulted** (host api.voyageai.com, confirmed via `onecli secrets list`). US-004 unblocked. FIRST STEP for US-004: rerun `ops/setup_onecli.sh` so rdq-research gets the Voyage secret assigned (it was skipped while unvaulted), then `ops/check_onecli.sh` passes the Voyage probe.
- **Slack app installed; tokens in repo-root .env** as SLACK_OAUTH_TOKEN (xoxb- bot) and SLACK_SOCKET_TOKEN (xapp- app-level). US-006 token source is DECIDED = .env fallback (do NOT proxy Slack through OneCLI). Added `.env` and `research/.env` to .gitignore — they were UNTRACKED-BUT-NOT-IGNORED (would have been committed). Keep them ignored; the zero-raw-creds metric covers committed files only.
- **Notion auth works via OneCLI connector** (integration "JPH NanoClaw Connection"), NOT a vaulted secret: bare proxied GET https://api.notion.com/v1/users/me as rdq-orchestrator returns HTTP 200 (verified). It does not appear in `onecli secrets list`, so setup_onecli.sh's "no vault secret for api.notion.com" WARN is a false alarm now — ignore it, don't vault a Notion key. Parent page: "Automated AI Quant Investment" id 3979b1a4-36cf-8046-baa5-cc14c1ca7665.
- **Learnings for future iterations:**
  - OneCLI can inject auth for a host via a *connector integration* that never shows in `onecli secrets list`. Prove auth by making a real proxied request (200 vs 401), not by grepping the secrets list.
  - A valid Notion integration token != page access. GET the parent page: 404 object_not_found means the page isn't shared with the integration yet (Notion UI: page -> ... -> Connections). Parent page is now SHARED — GET /v1/pages/<id> returns 200 (verified 2026-07-08). No human blocker remains for Notion.
  - Slack tokens intentionally bypass OneCLI (chat-scoped, PLAN.md-sanctioned exception). Read them from .env; never add them to the vault.
---
## 2026-07-08 19:45 - US-004
- LLM backend probe: Claude via LiteLLM CONFIRMED end-to-end through the OneCLI proxy; no fallback provider needed. Outcome + gotchas recorded in docs/decisions.md.
- Files: research/.env.example (CHAT_MODEL=anthropic/claude-sonnet-5, EMBEDDING_MODEL=voyage/voyage-3.5-lite, placeholder keys, zero OpenAI vars), research/probe_llm.py (JSON-mode hypothesis prompt validated against a jsonschema + one Voyage embedding call; exit 0/1), tests/test_probe_llm.py (18 tests total in repo now; helpers + .env.example contract), research/CLAUDE.md (new), docs/decisions.md (probe outcome entry).
- Pre-step per story notes: reran ops/setup_onecli.sh — rdq-research now has 3 secrets (Anthropic, Voyage, FMP); ops/check_onecli.sh PASSes the Voyage probe (HTTP 200). Only remaining check FAIL is the known-false-alarm Notion vault check (connector-injected auth, see US-026 notes).
- Verified: `onecli run --agent rdq-research -- .venv/bin/python research/probe_llm.py` → "PASS chat [anthropic/claude-sonnet-5]: schema-valid hypothesis (confidence=0.62)" + "PASS embedding [voyage/voyage-3.5-lite]: 1024-dim vector", exit 0. make check green (ruff pass, pyright 0 errors, 18 pytest passed).
- **Learnings for future iterations:**
  - LiteLLM 1.91.0 raises UnsupportedParamsError for temperature!=1 on claude-sonnet-5 (treats it as a reasoning model) — omit temperature or set litellm.drop_params=True. Will matter when configuring RD-Agent itself (US-017).
  - Anthropic JSON mode via LiteLLM can still return ```json-fenced output; parse tolerantly (research/probe_llm.extract_json_object) and budget max_tokens (a truncated reply = invalid JSON).
  - Proxy header injection OVERRIDES client-sent auth headers — placeholder ANTHROPIC_API_KEY/VOYAGE_API_KEY values work fine through onecli run.
  - `onecli run` injects HTTPS_PROXY + SSL_CERT_FILE/REQUESTS_CA_BUNDLE/CURL_CA_BUNDLE etc.; httpx (LiteLLM) needs no extra wiring.
  - Voyage voyage-3.5-lite returns 1024-dim vectors.
  - `import litellm` takes seconds — import it lazily inside functions so offline tests/pyright stay fast.
  - ops/check_onecli.sh short-circuits to FAIL when a host has no vault secret — it never probes connector-injected hosts like api.notion.com. If US-026 wants that check green, teach it to probe regardless of vault state (decisions.md already documents the connector).
  - make check runs ALL pytest markers incl. `live` — any future live-marked test should self-skip (e.g. unless RDQ_LIVE_TESTS=1) or make check gets slow/flaky and burns tokens.
---
## 2026-07-08 20:00 - US-005
- ops/run_vanilla_factor.sh: default mode execs `onecli run --agent rdq-research -- rdagent fin_factor --loop_n 1` with LOG_TRACE_PATH (timestamped) + WORKSPACE_PATH exported (defaults under ~/rdq-runs/vanilla_factor/, env-overridable); sources research/.env (set -a). --check mode runs `rdagent health_check --no-check-env` then hard-asserts sudo-less docker, port 19899 free, onecli gateway up + rdq-research registered. Header documents the slow first run (cn_data download + local_qlib:latest build, hours) and the completion criterion (qlib_res.csv with non-null IC in the run workspace).
- PREREQ FIX: `rdagent` CLI was import-broken (ImportError MCPServerStreamableHTTP) — rdagent leaves pydantic-ai-slim unpinned and pip had resolved 2.5.1 (2.x renamed the MCP classes). research/install.sh now pins pydantic-ai-slim[mcp,openai,prefect]==1.107.0 after the rdagent install and verifies `from rdagent.app.cli import app`. pip check clean. Recorded in docs/decisions.md.
- health_check env leg is skipped BY DESIGN: upstream env_check only knows DeepSeek/OpenAI env layouts and crashes (UnboundLocalError) on our Anthropic+Voyage layout; LLM path covered by research/probe_llm.py (US-004). Recorded in docs/decisions.md.
- Files: ops/run_vanilla_factor.sh (new), research/install.sh (pin + CLI import check), research/.env.example (+BACKEND, path-var note; additive, US-004 tests still pass), docs/decisions.md (2 entries), tests/test_run_vanilla_factor.py (7 tests: contract, header docs, --help, unknown-arg, shellcheck, CLI-import regression guard), research/CLAUDE.md (pin gotcha). Created machine-local research/.env from example (gitignored, placeholders only).
- Verified: `ops/run_vanilla_factor.sh --check` exit 0 — health_check: "The docker status is normal" (hello-world pulled+ran) + "Port 19899 is not occupied"; hard asserts: PASS docker sudo-less, PASS port 19899 free, PASS onecli gateway + rdq-research registered. install.sh rerun end-to-end OK ("rdagent CLI import OK"). shellcheck clean on both scripts. make check green (ruff pass, pyright 0 errors, 25 pytest passed).
- NOT done (out of iteration scope, documented in PRD notes): the multi-hour full fin_factor run. First person to launch it: budget hours, watch ~/.qlib/qlib_data/cn_data and `docker images | grep local_qlib`.
- **Learnings for future iterations:**
  - rdagent CLI import breaks with pydantic-ai-slim 2.x — always run research/install.sh (it pins 1.107.0) rather than raw pip installing rdagent; test_rdagent_cli_importable guards this in make check.
  - `rdagent health_check` NEVER exits nonzero on docker/port failures (only logs) — wrappers must assert independently. Its env leg crashes on non-OpenAI/DeepSeek layouts; use --no-check-env.
  - rdagent env var names confirmed from source: LOG_TRACE_PATH (LogSettings env_prefix LOG_ + trace_path), WORKSPACE_PATH (RDAgentSettings). BACKEND default is already rdagent.oai.backend.LiteLLMAPIBackend.
  - Docker on this box: sudo-less, network pulls work (hello-world pulled during health_check).
  - research/.env now exists on this box (copy of .env.example, placeholders only, gitignored) — run wrappers source it.
  - GOTCHA: every rdagent invocation (even --help/health_check) writes a CWD-relative log/<timestamp>/ trace dir and selector.log. Now gitignored (/log/, selector.log) — but avoid `git add -A` blindly after running rdagent from repo root.
---
## 2026-07-08 20:20 - US-006
- Slack Bolt Socket Mode skeleton: orchestrator/config.py (SlackConfig; process env overrides repo-root .env; ConfigError names the missing var + where to set it; validates xoxb-/xapp- prefixes) and orchestrator/app.py (create_app registers a message handler; replies in-thread via thread_ts = event.thread_ts or event.ts; ignores other channels, subtypes, and bot_id messages; main() starts SocketModeHandler). slack-bolt>=1.20 added to pyproject deps (installed 1.29.0).
- Token-source decision was already recorded in docs/decisions.md (2026-07-08 ".env fallback" entry) — criterion satisfied, no new entry needed.
- tests/test_slack_app.py: 15 tests — config loading (file parse, env override, per-var missing errors, prefix swap rejection) + routing through the REAL Bolt dispatcher (App.dispatch with socket_mode BoltRequest): channel msg -> reply threads on its ts; thread msg -> reply targets thread_ts; other-channel/bot_id/subtype ignored; plus direct handle_message unit tests.
- Verified: make check green (ruff pass, pyright 0 errors, 40 pytest passed). `python -m orchestrator.app` without SLACK_CHANNEL_ID exits 1 with the actionable ConfigError. LIVE smoke: with the real .env tokens + dummy channel id, Socket Mode session established — "A new session has been established", "⚡️ Bolt app is running!" (15s run, then killed). Both tokens work.
- **Learnings for future iterations:**
  - Bolt >=1.15 builds a NEW real WebClient per request in App._init_context — App(client=mock) alone is not enough for tests; also monkeypatch slack_bolt.app.app.WebClient (AFTER App() construction — the constructor isinstance-checks that symbol). Full recipe in orchestrator/CLAUDE.md.
  - MagicMock(spec=WebClient) lacks instance attrs Bolt reads (base_url, timeout, ssl, proxy, headers, logger, retry_handlers) — set them on the mock.
  - process_before_response=True makes App.dispatch() run listeners synchronously (no test races); leave it False in production once handlers get slow (Slack retries events not acked in ~3s) — relevant for US-009's Claude calls (use lazy listeners or ack-then-work).
  - Handler MUST skip subtype/bot_id events or the bot loops on its own replies.
  - Live-Slack usability still needs the human: SLACK_CHANNEL_ID in .env, bot invited to #quant-research, scopes chat:write + channels:history + reactions:write (+files:write later).
---
## 2026-07-08 20:05 - US-007
- orchestrator/state.py: StateStore with all four tables (directives, runs, promoted_strategy, pending_interactions), idempotent migration run on every construction, typed frozen-dataclass rows, and CRUD helpers: create/get directive (latest per thread), create/get/list/update-status/delete run, set/get promoted strategy (upsert on CHECK(id=1) single row), add/list/resolve pending interactions.
- Constraints designed for downstream stories: runs.thread_ts is the PK — duplicate create_run raises DuplicateRunError carrying the existing row (US-020 rejection message); pending_interactions.interaction_key is UNIQUE — add returns None on duplicate (US-021 poller dedup); promoted_strategy can never hold two rows (US-033).
- Files: orchestrator/state.py (new), tests/test_state.py (20 tests: idempotent migration, fetch-by-thread incl. latest-directive-wins, duplicate run rejection, status transitions, single-row promotion replacement asserted via raw COUNT(*), interaction dedup/resolve/thread filter, full restart-survival test), orchestrator/CLAUDE.md (state-layer usage pattern).
- Verified: make check green (ruff pass, pyright 0 errors, 60 pytest passed — 20 new). Default-path smoke: StateStore() created orchestrator/state.sqlite, directive written, second StateStore() on the same file (simulated restart) migrated idempotently and returned the directive; file confirmed gitignored (git status clean), then removed.
- **Learnings for future iterations:**
  - StateStore opens a short-lived sqlite3 connection per method — share ONE instance across Bolt handlers and pollers; never cache a connection across threads.
  - sqlite3.Row is not a typing Mapping — `Dataclass(**row)` fails pyright; use explicit _x_from_row constructors.
  - `with closing(sqlite3.connect(...)) as conn, conn:` gives commit-on-success/rollback-on-error AND close; sqlite3's own context manager does not close.
  - cur.lastrowid is Optional under pyright — assert non-None after INSERT.
  - Uniqueness/dedup belongs in the schema (PK/UNIQUE + IntegrityError catch), not in check-then-insert code — it stays correct across process restarts and threads.
---
## 2026-07-08 20:40 - US-008
- orchestrator/llm.py: ModelRouter centralizing the tier policy — judgment() streams claude-fable-5 via client.beta.messages.stream with betas=["server-side-fallback-2026-06-01"] + fallbacks=[{"model": "claude-opus-4-8"}], NO thinking parameter (fable-5 rejects any explicit thinking config), stop_reason=="refusal" checked BEFORE content is read (raises RefusalError carrying stop_details). utility() = claude-haiku-4-5 via messages.create (also refusal-guarded). judgment_tool_loop() executes registered ToolSpec handlers, feeds all tool_result blocks back in ONE user message (parallel tool use), loops until stop_reason != tool_use; handler exceptions/unknown tools become is_error tool_results (fed to model, not raised); ToolLoopLimitError after max_iterations.
- Files: orchestrator/llm.py (new), tests/test_llm.py (13 tests: model/beta/fallback selection per path, thinking omitted, refusal branch — RefusalMessage.content property raises if touched, proving the order of checks — tool loop happy path/unknown tool/handler exception/parallel calls/iteration limit/refusal propagation, plus a grep test enforcing model IDs appear only in orchestrator/llm.py), pyproject.toml (+anthropic>=0.116), orchestrator/CLAUDE.md (router usage + fake-client test recipe).
- Verified: make check green (ruff pass, pyright 0 errors, 73 pytest passed — 13 new). LIVE smoke via `onecli run --agent rdq-orchestrator`: utility -> haiku 'pong' end_turn; judgment -> claude-fable-5 'pong' end_turn (fallback beta accepted — vaulted Anthropic key HAS fable-5 access, org retention compatible); judgment_tool_loop -> real tool round-trip computed 17+25='42' end_turn.
- **Learnings for future iterations:**
  - anthropic SDK 0.116.0 installed; client.beta.messages.stream accepts betas= and fallbacks= kwargs directly and the live API accepted them through the proxy.
  - claude-fable-5 works through the OneCLI proxy with the placeholder-key pattern (proxy header injection), same as LiteLLM. Thinking is always on for fable-5 — omit the param entirely; give judgment calls max_tokens headroom (thinking counts against it; 2048 was fine for a trivial prompt).
  - To assert "X is checked before Y is read" in tests, make Y a property that raises — cleaner than mock call-order assertions (see RefusalMessage in tests/test_llm.py).
  - ModelRouter takes client=Any so MagicMock/fakes pass pyright standard mode; keep `import anthropic` lazy inside __init__ so offline tests never pay the import.
  - US-009 should build its conversational core on ModelRouter.judgment_tool_loop (save_directive becomes a ToolSpec) — do NOT instantiate Anthropic clients elsewhere; the grep test will fail the build if model IDs leak outside llm.py.
---
## 2026-07-08 20:15 - US-009
- Conversational core: orchestrator/prompts.py (SYSTEM_PROMPT — quant desk PM persona: honest reporting, never trades without explicit operator approval; directive_context() renders the saved directive back into the system prompt) + orchestrator/conversation.py (ConversationCore over ModelRouter.judgment_tool_loop; save_directive ToolSpec persists {objective, universe_hint, constraints} via StateStore.create_directive and posts a mrkdwn summary in-thread; RefusalError/LLMError are caught and reported in-thread, and the failed user turn is popped so the transcript stays consistent; per-thread history bounded at 40 messages).
- Restart model: in-memory history is best-effort; the DURABLE context is the directive, reloaded from SQLite into the system prompt on every call — a restart loses chit-chat, never the directive.
- app.py rewired: handle_message/create_app now take a conversation (MessageResponder Protocol — structural, so tests pass a plain stub); main() builds ConversationCore(StateStore(), ModelRouter()); skeleton ack removed. US-006 tests updated to a FakeConversation stub (routing assertions unchanged, plus assertions the core receives (thread_ts, text)).
- Files: orchestrator/prompts.py (new), orchestrator/conversation.py (new), orchestrator/app.py, tests/test_conversation.py (9 new tests: persona keywords, idea->directive->echo with exact summary text, optional-fields None, empty-objective rejected via is_error with nothing persisted, history accumulation, per-thread isolation, restart context reload asserting the system prompt carries the objective, no-context-before-save, refusal rollback), tests/test_slack_app.py (stub injection), orchestrator/CLAUDE.md.
- Verified: make check green (ruff pass, pyright 0 errors, 82 pytest passed — 9 new). LIVE smoke `onecli run --agent rdq-orchestrator -- python /tmp/smoke_us009.py`: real fable-5 called save_directive on a momentum idea, summary + confirmation posted via fake say, Directive row #1 persisted with all three fields and survived a fresh-StateStore "restart". In-channel live test still blocked on the human (SLACK_CHANNEL_ID absent from .env, bot not yet invited).
- **Learnings for future iterations:**
  - Per-message tools whose handlers need thread_ts/say: build the ToolSpec inside the handle call with a closure — do NOT register thread-agnostic tools and thread state through inputs.
  - tests/ is a package: reuse FakeClient/message/tool_use_block/RefusalMessage from tests.test_llm instead of re-mocking Anthropic.
  - Use a Protocol (MessageResponder) for app.py's dependency on the core — plain stub classes then pass pyright without inheriting anything.
  - fable-5 follows the save_directive tool contract well live: one call, sensible field split, and it echoed the honesty/no-trading ground rules unprompted in its confirmation.
  - judgment_tool_loop copies the convo internally and returns only the final message — the tool exchange is not in your history; persist durable facts (the directive) in SQLite and re-inject via system prompt instead of trying to keep tool turns in history.
  - On RefusalError/LLMError, pop the just-appended user turn: Anthropic merges consecutive same-role messages, but a rolled-back transcript keeps retries clean.
---
## 2026-07-08 20:25 - US-010
- ops/rdq-orchestrator.service (systemd user unit): runs `onecli run --agent rdq-orchestrator -- .venv/bin/python -m orchestrator.app`, Restart=always + RestartSec=5, StartLimitIntervalSec=0 (in [Unit] — never parks failed during outages), WorkingDirectory/paths via %h, WantedBy=default.target, header docs (install/start/logs). Critical addition: Environment NO_PROXY=slack.com + no_proxy=slack.com — onecli run injects HTTPS_PROXY process-wide, which would route Slack through the OneCLI proxy against the docs/decisions.md rule; onecli preserves pre-set NO_PROXY and urllib suffix-matches it (covers wss-primary.slack.com/files.slack.com).
- ops/install_services.sh: ln -sfn units from ops/ into ~/.config/systemd/user/ + daemon-reload; idempotent; errors on missing unit files; UNITS array is the extension point for US-018/036/041 units.
- tests/test_services.py (10 tests): unit contract (onecli identity, Restart=always, NO_PROXY exemption, [Install]), systemd-analyze --user verify (skips if unavailable), install script shellcheck + UNITS-entries-exist-in-ops/ drift guard.
- Verified live: systemd-analyze verify OK; install script linked + reloaded; `systemctl --user status` recorded: "Active: active (running)", Main PID python -m orchestrator.app under onecli, journal shows "A new session has been established" + "⚡️ Bolt app is running!", NRestarts=0 over 25s+. Restart=always also organically proven: initial start crash-looped (restart counter hit 4) and systemd kept resurrecting it. Service then STOPPED deliberately (placeholder channel id — see below). make check green (ruff pass, pyright 0 errors, 92 pytest passed — 10 new).
- INCIDENT (self-inflicted, fixed): appended SLACK_CHANNEL_ID placeholder to .env with `printf >> .env`, but .env had NO trailing newline — the comment (containing an em-dash) concatenated onto the SLACK_OAUTH_TOKEN line, so the token value carried "…# PLACEHOLDER — …" and every Slack call died with UnicodeEncodeError ('latin-1' can't encode —) in http.client putheader. Repaired .env (proper lines + trailing newline). Debug trick that found it: monkeypatch http.client.HTTPConnection.putheader to print non-latin-1 header values.
- OPERATOR HANDOFF: service is installed but stopped; .env SLACK_CHANNEL_ID=C0000000000 is a placeholder. Set the real #quant-research id, invite the bot, then `systemctl --user enable --now rdq-orchestrator.service`.
- **Learnings for future iterations:**
  - Before appending to .env (or any dotfile), check the trailing newline (`tail -c1 file | xxd`) — and note parse_env_file does NOT strip inline `#` comments from values.
  - onecli run PRESERVES pre-set NO_PROXY (verified empirically) — the unit-level Environment= exemption is the sanctioned way to keep Slack off the proxy while Anthropic goes through it.
  - systemctl --user / systemd-analyze --user need XDG_RUNTIME_DIR=/run/user/$(id -u) from agent shells (it's unset here); user units use WantedBy=default.target; StartLimitIntervalSec belongs in [Unit].
  - systemd user session on this box is healthy (`is-system-running` → running) and hosts other nanoclaw services; `Loaded: ... linked` is the expected state for ln -sfn-installed units.
  - journalctl --user -u rdq-orchestrator.service is where bot logs go; grep -v tokens when pasting.
## 2026-07-08 20:45 - US-011
- data/fmp.py: FmpClient for the FMP /stable API through the OneCLI proxy — get_eod_bars(symbol, start, end) hitting /historical-price-eod/full?symbol=&from=&to=, get_splits (/splits), get_dividends (/dividends). Bare HTTPS: zero apikey in code/env/params (proxy injects for rdq-research). Typed frozen dataclasses (EodBar, Split with .ratio, Dividend); all lists returned ascending by date.
- Error handling: 429 retried honoring Retry-After (numeric) with exponential fallback (2*2^attempt) up to max_retries, then FmpRateLimitError; 401/403 raise FmpAuthError telling the operator exactly what to do (run under `onecli run --agent rdq-research`, vault+assign via ops/setup_onecli.sh, verify with check_onecli.sh); non-list JSON payloads (FMP's error-object shape) raise FmpError.
- Files: data/fmp.py (new), tests/test_fmp.py (15 unit + 1 live), data/CLAUDE.md (new), pyproject.toml (+requests>=2.31 — was only transitively present via rdagent).
- Verified: make check green (ruff pass, pyright 0 errors, 107 passed / 1 skipped). LIVE smoke through proxy: `RDQ_LIVE_TESTS=1 onecli run --agent rdq-research -- pytest -m live tests/test_fmp.py` → 1 passed (AAPL 2026-05-01..31 returned >15 ascending in-window bars, all closes >0).
- **Learnings for future iterations:**
  - Live-marked tests now follow the self-skip pattern: @pytest.mark.live + skipif RDQ_LIVE_TESTS!=1 (make check stays offline-green). Copy this for US-028's Alpaca smoke.
  - Testability via injection, not monkeypatching: FmpClient(session=FakeSession, sleep=list.append) — reuse FakeSession/FakeResponse from tests/test_fmp.py for other HTTP clients (US-025 Notion, US-028 Alpaca).
  - FMP /stable rows come newest-first — client sorts ascending; downstream code can rely on order.
  - FMP closes are RAW/unadjusted; US-012 must build adjustment factors from get_splits + get_dividends (Split.ratio = numerator/denominator, e.g. 10.0 for NVDA 2024-06-10).
  - requests was already in the venv via rdagent but NOT a declared dep — declared now. Check pyproject deps whenever importing something "already available".
---
## 2026-07-08 20:55 - US-012
- data/adjust.py: adjustment_factors(bars, splits, dividends) -> dict[date, float] and adjusted_closes(...) -> ascending (date, adj_close). Backward adjustment: factor 1.0 on the window's last bar; walking backwards, crossing an ex-date multiplies the running factor — split by 1/ratio (10:1 -> 0.1 step), dividend by (prev_close - D)/prev_close where prev_close is the last bar close strictly before the ex-date (proportional). Split+dividend on adjacent/same dates compound multiplicatively.
- Data-quality guards raise AdjustmentError: duplicate bar dates, non-positive split ratio, negative dividend, dividend >= prev close. Events on/before the first bar or AFTER the last bar are ignored (FMP /dividends lists announced future ex-dates; adjusting today's prices by them would be wrong).
- Files: data/adjust.py (new), tests/test_adjust.py (18 tests: no-event ticker all-1.0, 10:1 split 0.1 step, compounding splits, reverse split, proportional dividend, prev-close-strictly-before semantics across a non-trading ex-date, combined split+dividend, out-of-window events ignored, unsorted input, error guards, NVDA 2024-06-10 fixture — raw closes cliff ~90% across the split while adjusted max daily move <20%), data/CLAUDE.md (adjustment conventions).
- Verified: make check green (ruff pass, pyright 0 errors, 126 passed / 1 skipped — 18 new). Direct drive observed: NVDA fixture with split + a 0.01 dividend printed factor 0.099992 pre-split (0.1 x 0.999918 dividend factor), 1.0 on the last bar, adjusted series smooth across 06-10 (120.88 -> 121.79).
- **Learnings for future iterations:**
  - Descending-walk trick: iterate bars newest-first and consume events with date > bar.date before assigning the bar's factor — the current bar is then exactly the dividend's prev_close bar, no separate lookup needed.
  - FMP /dividends can include FUTURE ex-dates (declared, not yet effective) — any consumer of get_dividends must window events to the bar range or stores get silently mis-adjusted.
  - Ex-dates can fall on non-bar days (halts/weekends for the fixture granularity): "strictly before ex-date" must be by comparison, not by index/adjacency.
  - US-013: adjusted price = raw * factor; Qlib volume convention = raw volume / factor; keep raw closes + factor column so incremental refresh can re-derive.
---
## 2026-07-08 20:45 - US-013
- data/build_store.py: Qlib US bin-store builder. Pipeline: ticker list -> FMP fetch (bars+splits+dividends, checkpointed per ticker) -> data/adjust factors -> qlib dump_bin layout (calendars/day.txt, instruments/all.txt, features/<sym>/<field>.day.bin). Fields: open/high/low/close ADJUSTED (raw*factor), volume raw/factor, plus a factor field so raw is recoverable. CLI: --tickers/--tickers-file --start --end --output (default ~/.qlib/qlib_data/us_data) --checkpoint-dir --fresh.
- Resumability: checkpoints at <output>.checkpoint/<SYM>.json record the (start,end) window; same-window reruns skip fetched tickers, window change/corrupt file refetches. Atomicity: store written to <target>.tmp, validate_store() (calendar ordering, instruments coverage, bin index bounds, NO NaN close/factor inside a ticker's span), then swap via <target>.old; any failure rmtree's the tmp and leaves the old store untouched.
- Files: data/build_store.py (new), tests/test_build_store.py (14 tests: crash-midlist resume with exact refetch list + no dup/missing instruments, window-mismatch refetch, bin format vs hand-read np.fromfile, split adjusts prices/volume/factor, failed build leaves existing store byte-identical + no .tmp/.old, gapped ticker fails validation and never swaps, rebuild swap, missing-file validation, CLI exit codes, qlib smoke), pyproject.toml (+numpy, +pyqlib), data/CLAUDE.md (bin format + swap/checkpoint conventions).
- Verified: make check green (ruff pass, pyright 0 errors, 140 passed / 1 skipped — 14 new). Acceptance smoke in-suite: 5-ticker fixture store, qlib.init(region="us") + D.features(["AAPL"], OHLCV) -> 5 rows, zero NaN. LIVE end-to-end: `onecli run --agent rdq-research -- python -m data.build_store --tickers AAPL,MSFT,NVDA --start 2026-05-01 --end 2026-06-30 --output /tmp/...` -> "store built (3 tickers)", qlib read-back 41 bars/ticker, 0 NaN closes, sane AAPL prices (~$280 June 2026).
- **Learnings for future iterations:**
  - pyqlib 0.9.7 installs clean on py3.12 (wheels; only gym built from source), pip check clean, rdagent still imports. It is now a DECLARED dep — `make venv` style installs get qlib without research/install.sh.
  - Qlib bin format (from qlib FileFeatureStorage source, not docs): float32 LE array, element 0 = calendar index of first value; feature paths are LOWERCASE symbol dirs; instruments file is TAB-separated; D.features instruments arg stays uppercase.
  - qlib.init(provider_uri=..., region="us") works offline on a local store; import qlib takes seconds — keep it lazy/inside the one test that needs it.
  - US-014 (make_universe): write instruments/<name>.txt rows as SYMBOL\tstart\tend matching all.txt spans; US-015 (factor source h5): read via qlib D.features on this store.
  - Union-calendar alignment means a ticker missing a day inside its own [first,last] span produces NaN -> validation hard-fails. If a legit halt ever trips this, the fix is an explicit allowlist, not relaxing the guard.
---
## 2026-07-08 21:00 - US-014
- data/make_universe.py: writes instruments/<name>.txt into an existing Qlib store. Built-ins from data/config.yaml: us_liquid (min_adv_usd $20M, min_price $5, adv_window 20; with no --tickers it considers ALL store tickers) and sp500 (tickers_file -> data/sp500_tickers.txt, a committed 504-symbol snapshot fetched live from FMP /stable/sp500-constituent through the proxy on 2026-07-08; refresh one-liner documented in config.yaml). Custom names take --tickers/--tickers-file (CLI overrides config source). Filter rejections print per-ticker reasons; empty-after-filter is an error; names validated ([a-z][a-z0-9_]*, 'all' reserved).
- Gap handling per spec: any requested ticker absent from instruments/all.txt -> ERROR with the full gap list on stderr, exit 1, nothing written.
- Filter math: stored close*volume == RAW dollar volume (adjustment factors cancel: close=raw*f, volume=raw/f) and last raw price = close/factor — no de-adjustment pass needed; unit-tested across a 2:1 split.
- Files: data/make_universe.py (new), data/config.yaml (new), data/sp500_tickers.txt (new), tests/test_make_universe.py (14 tests), pyproject.toml (+pyyaml), data/CLAUDE.md (universe conventions).
- Verified: make check green (ruff pass, pyright 0 errors, 157 passed / 1 skipped — 14 new). LIVE end-to-end on a real proxy-built 3-ticker store (/tmp/rdq_univ_store): us_liquid default wrote 3 tickers exit 0; custom ai_pair wrote 2; gap case (AVGO,TSM absent) exit 1 with gap list; sp500 vs tiny store exit 1 listing 501 gaps. qlib.init + D.instruments(market="ai_pair") resolved the file: 2 instruments, 82 rows, 0 NaN closes.
- **Learnings for future iterations:**
  - Qlib's `market:` param is just the instruments filename — D.instruments(market="ai_pair") reads instruments/ai_pair.txt. US-016/US-023 template renders need only set market: <universe name>.
  - FMP /stable/sp500-constituent works through the proxy (200, current membership — SpaceX added 2026-07-07). Snapshot committed; regenerate with the jq one-liner in config.yaml when benchmarking matters.
  - Shell gotcha: PIPESTATUS is clobbered by ANY subsequent command incl. `echo` — capture it on the line immediately after the pipeline or use a redirect + $?.
  - pyyaml was in the venv (via deps) but undeclared — now declared. pyright bundles typeshed stubs for yaml, no types-PyYAML install needed.
  - config.yaml tickers_file paths resolve relative to the config file's directory (keeps data/ relocatable); tests point --config at a tmp yaml to test thresholds without touching the real config.
## 2026-07-08 21:10 - US-015
- data/make_factor_source.py: universe -> factor coder source data. Reads the store bins DIRECTLY with numpy (no qlib import) and reproduces upstream rdagent factor_data_template/generate.py output exactly: daily_pv_all.h5 + daily_pv_debug.h5, key="data", MultiIndex (datetime, instrument), float32 columns $open/$close/$high/$low/$volume/$factor, rows only inside each instrument's own span. Parity proven by a test comparing against qlib D.features(D.instruments(market=...)).swaplevel().sort_index() on the fixture store (assert_frame_equal).
- Consumable layout beyond the AC (informed by upstream rdagent/components/coder/factor_coder consumption code): the coder links ALL files in FACTOR_CoSTEER_DATA_FOLDER(_DEBUG) into each factor workspace and prompts the LLM with the DEBUG folder's files by NAME — so both folders need the SAME filename. Generator therefore also writes <output>/data_folder/ and <output>/data_folder_debug/ each holding daily_pv.h5 + README.md; US-017 sets FACTOR_CoSTEER_DATA_FOLDER=<output>/data_folder and ..._DEBUG=<output>/data_folder_debug.
- Refusal per AC: missing instruments/<universe>.txt -> FactorSourceError ("create it first with data/make_universe.py"), CLI exit 1, nothing written. Debug subset = last --debug-days trading days x first --debug-instruments sorted symbols (defaults 504 x 100, mirroring upstream's ~2y x 100).
- Files: data/make_factor_source.py (new), tests/test_make_factor_source.py (12 tests: h5 MultiIndex/columns contract on fixture store, universe scoping, debug trimming, nonpositive-limit rejection, consumable folders mirror h5s, regeneration overwrites stale output, missing-universe refusal (API + CLI), calendar-less store, missing feature file, CLI happy path, qlib parity), data/CLAUDE.md (factor source conventions).
- Verified: make check green (ruff pass, pyright 0 errors, 169 passed / 1 skipped — 12 new). LIVE on the real proxy-built store (/tmp/rdq_univ_store from US-014): us_liquid -> 123 rows x 6 cols, 3 instruments, 2026-05-01..2026-06-30, 0 NaN, all float32; debug trimmed to 20 days x 2 instruments; missing universe exit 1 with actionable error; read-back via pd.read_hdf confirmed layout.
- **Learnings for future iterations:**
  - RD-Agent factor coder data contract (from source, .venv rdagent/components/coder/factor_coder/factor.py + scenarios/qlib/experiment/utils.py): FACTOR_CoSTEER_DATA_FOLDER and _DEBUG each need daily_pv.h5 + README.md; get_data_folder_intro() builds the prompt from the DEBUG folder's files. Env prefix is FACTOR_CoSTEER_ (mixed case, pydantic env_prefix).
  - pd.DataFrame.to_hdf APPENDS to an existing h5 file even with a fixed key — unlink before rewriting or regenerated files silently grow (test_regeneration_overwrites_stale_output guards).
  - pandas-stubs gotchas under pyright: DataFrame boolean masking returns Series|DataFrame (use .loc[np.asarray(mask)]), DataFrame.any() chains poorly (use .to_numpy().any()), read_hdf returns object (assert isinstance).
  - tests/test_build_store.py fixtures (DAYS, five_ticker_client, build_from_fmp) compose cleanly for any store-consuming test — build a real fixture store in ~0.1s rather than mocking store reads.
  - qlib.init can be called repeatedly in one pytest process with different provider_uris (test_build_store and test_make_factor_source both init; no conflict).
  - pytables (tables 3.11.1) was already in the venv via rdagent's tree; pandas to_hdf/read_hdf work without new declared deps (h5 read path is exercised in make check, so a venv without it would fail loudly).
---
## 2026-07-08 21:35 - US-016
- research/us_templates/factor_template/ + model_template/: copies of the pinned upstream qlib workspace template folders. All 5 conf_*.yaml patched: provider_uri ~/.qlib/qlib_data/us_data, region us, market &market us_liquid, benchmark &benchmark SPY, limit_threshold REMOVED (qlib region-us default None; trade_unit defaults to 1), open_cost/close_cost 0.0005 (5bps/side spread+slippage proxy; Alpaca commission-free), min_cost 0. read_exp_res.py + per-dir README.md byte-identical upstream copies (workspace runs `python read_exp_res.py`).
- research/app_tpl/: partial prompt overrides loaded via APP_TPL — scenarios/qlib/experiment/prompts.yaml (qlib_factor/model_experiment_setting: CSI300 row -> "US stocks (us_liquid universe)") and scenarios/qlib/factor_experiment_loader/prompts.yaml (factor_viability/relevance/duplicate_system: "China A-share market" -> "the US equity market"). Extracted verbatim from upstream then phrase-patched (generated, not hand-typed). classify_system_chinese NOT overridden — dead code (only classify_system is referenced in pdf_loader.py).
- docs/decisions.md: full grep audit table (all A-share language locations + handling), SPY-not-^GSPC rationale (benchmark resolves as a store instrument; SPY flows through the FMP pipeline — STORE BUILDS MUST INCLUDE SPY), cost/limit_threshold decisions, APP_TPL mechanics, US-017 consumption note.
- Files: research/us_templates/** (new, +top-level README with rebase procedure), research/app_tpl/** (new), tests/test_us_templates.py (16 tests), docs/decisions.md, research/CLAUDE.md, pyproject.toml (+jinja2 dep; ruff/pyright exclude research/us_templates).
- Verified: make check green (ruff pass, pyright 0 errors, 185 passed / 1 skipped — 16 new). Live end-to-end: RD_AGENT_SETTINGS.app_tpl=<abs research/app_tpl> drove rdagent's real load_content — production-style ".prompts:" URIs returned the US overrides, non-overridden key fell through to upstream ("Wall Street"), unset app_tpl returned upstream CSI300. Upstream-untouched proven by hashing every installed rdagent file against pip's RECORD (test_pinned_rdagent_install_unmodified).
- **Learnings for future iterations:**
  - APP_TPL (RDAgentSettings.app_tpl, NO env prefix) prepends <app_tpl>/<upstream-relative-path> to prompt lookups; absolute paths work (pathlib Path/abs-str). Missing keys in an override file fall through to upstream (KeyError -> continue) — keep override files minimal.
  - APP_TPL does NOT cover workspace template folders: QlibFactorExperiment/QlibModelExperiment hardcode Path(__file__).parent/"factor_template". US-017 hook = QLIB_QUANT_* env-configurable class paths in rdagent/app/qlib_rd_loop/conf.py (scen, *_hypothesis2experiment, ...) pointing at tiny subclasses that pass research/us_templates paths. Also QLIB_QUANT_TRAIN_START etc. map to the same settings class.
  - Benchmark = SPY (must be in the us_data store); US-017's store/check must ensure SPY is fetched alongside the universe.
  - pip's RECORD hashes (importlib.metadata dist.files[].hash, sha256 urlsafe-b64-nopad) give a free "upstream unmodified" proof — no network, ~1s for the whole rdagent tree.
  - yaml.safe_load parses unquoted ISO dates to datetime.date — compare against date objects when asserting rendered qlib configs.
  - rdagent.scenarios.qlib.experiment is a namespace package (__file__ None); resolve upstream paths via rdagent.utils.agent.tpl.PROJ_PATH.
  - Committed upstream file copies must be excluded from ruff/pyright (pyproject) since tests enforce byte-identity — never autofix research/us_templates.
  - Grep-audit needles can appear in your own explanatory comments — the no-A-share-text test taught the override header to say "CN-market" instead.
---
## 2026-07-08 22:05 - US-017
- ops/run_us_quant.sh: US fin_quant wrapper. wire_env exports: date segments under ALL THREE prefixes QLIB_QUANT_/QLIB_FACTOR_/QLIB_MODEL_ (RDQ_TRAIN_START.. knobs; defaults train 2016-2022, valid 2023-2024, test 2025-01-01..2026-06-30), FACTOR_CoSTEER_DATA_FOLDER(_DEBUG) -> ~/rdq-data/factor_source/us_liquid/data_folder(_debug), APP_TPL -> research/app_tpl, QLIB_QUANT_FACTOR/MODEL_HYPOTHESIS2EXPERIMENT -> research.us_quant.US* classes, LOG_TRACE_PATH (timestamped)/WORKSPACE_PATH under ~/rdq-runs/us_quant. --loop_n/--all_duration passthrough (default --loop_n 1); launches `onecli run --agent rdq-research -- rdagent fin_quant`. Header documents the full-run completion criterion (qlib_res.csv non-NaN IC, |ARR|<200%, MDD<0, mlruns/**/pred.pkl indexed by US tickers).
- research/us_quant.py: the template-redirect hook APP_TPL can't provide — USQlibFactorHypothesis2Experiment/USQlibModelHypothesis2Experiment override convert_response to re-inject research/us_templates over the new experiment's workspace AND the fresh baseline QlibFactorExperiment(sub_tasks=[]) (file_dict + on-disk both replaced; US template filenames mirror upstream exactly so replacement is total).
- Files: ops/run_us_quant.sh (new), research/us_quant.py (new), tests/test_us_quant_hooks.py (6 tests: env-var name seam via QuantBasePropSetting+import_class, filename parity vs upstream, factor/model convert_response repoint with no cn_data/csi300 leakage, in-place replace incl. on-disk, non-experiment objects ignored), tests/test_run_us_quant.py (11 tests: contract greps, header criterion, help/unknown-arg/missing-value, shellcheck, --check pass on fake layout + failures naming missing factor source/SPY/unordered dates), research/CLAUDE.md.
- Live deployment prep done as part of verification: REAL us_data store built through the proxy at ~/.qlib/qlib_data/us_data — 31 tickers (30 liquid megacaps + SPY benchmark), 2015-01-01..2026-06-30; us_liquid universe = the 30 stocks (SPY deliberately excluded from the tradable universe); factor source generated at ~/rdq-data/factor_source/us_liquid.
- Verified: `ops/run_us_quant.sh --check` exit 0 live (all 8 PASS lines: dates ordered, rdagent+.env, store+universe+SPY, factor source, APP_TPL+templates, hook classes import, docker, onecli+rdq-research). Negative paths covered in pytest. make check green (ruff pass, pyright 0 errors, 202 passed / 1 skipped — 17 new).
- NOT done (matches US-005 precedent): the multi-hour full fin_quant run. First launcher: expect local_qlib:latest docker build on first run; store/universe/factor source are already in place.
- **Learnings for future iterations:**
  - rdagent's FactorExperiment/ModelExperiment (components.coder.*) are BARE ALIASES of Experiment (`FactorExperiment = Experiment`) — isinstance cannot discriminate factor vs model experiments. Match concrete Qlib classes; quant_experiment.py defines its OWN QlibFactorExperiment/QlibModelExperiment distinct from factor_experiment.py/model_experiment.py's — cover both tuples.
  - DATE GOTCHA: factor_runner/model_runner re-instantiate FactorBasePropSetting()/ModelBasePropSetting() inside develop() and pass those dates as jinja env into qrun — QLIB_QUANT_* only reaches the scenario prompt. Any fin_quant driver (US-019/020) must export the same dates under all three prefixes or backtests silently use CN defaults (2008-2020).
  - inject_code_from_folder overwrites same-named files in BOTH file_dict and the on-disk workspace — total template replacement works because US-016 kept upstream filenames. tests/test_us_quant_hooks.py has a filename-parity guard.
  - QTDockerEnv mounts ~/.qlib rw into the container (/root/.qlib), so provider_uri ~/.qlib/qlib_data/us_data resolves inside docker; enable_gpu auto-disables when no GPU (upstream comment confirms).
  - Hypothesis2Experiment subclass test recipe: Hypothesis(6 str args) + SimpleNamespace(hist=[]) trace + hand-written response JSON drives the REAL convert_response offline; set RD_AGENT_SETTINGS.workspace_path to tmp_path first (experiment construction writes workspaces immediately).
  - The us_data store on this box is now real and reusable (31 tickers, 11.5y). SPY lives in the store for the benchmark but NOT in us_liquid — keep benchmark instruments out of tradable universes (US-023 set_universe should do the same).
---
## 2026-07-08 21:45 - US-018
- ops/rdq-research.service (systemd user unit): runs the server_ui control API on 127.0.0.1:19899 via `onecli run --agent rdq-research`, Restart=always/RestartSec=5/StartLimitIntervalSec=0, NO_PROXY=127.0.0.1,localhost,100.64.0.0/10 (loopback control traffic + tailnet peers skip the OneCLI proxy), UI_TRACE_FOLDER/UI_STATIC_PATH -> ~/rdq-runs/server_ui/{traces,static} (state outside the repo). Unit comments document NOT-exposed-via-tailscale-serve (PLAN.md port table: flask-cors advisories, stays dark; only `rdagent ui` :19900 is ever exposed, tailnet-only, in US-042).
- KEY FINDING: upstream `rdagent server_ui` (rdagent/log/server/app.py main()) binds Flask to host="0.0.0.0" — violates the 127.0.0.1 rule, and the pinned tree can't be patched (RECORD-hash test). research/server_ui.py is a loopback replica of main(): same _load_existing_traces + app.run sequence with host=127.0.0.1, plus it mkdir-p's the trace/static dirs; --host/--port CLI.
- Files: ops/rdq-research.service (new), research/server_ui.py (new), tests/test_server_ui.py (7 tests: app.run kwargs loopback, dir creation, load-before-serve ordering, argparse, subprocess end-to-end — real server on an ephemeral port answers GET /test 200 AND ss shows the listener bound to 127.0.0.1 not wildcard), tests/test_services.py (+8: TestResearchUnit contract greps incl. exact NO_PROXY line + "NOT exposed via tailscale serve" comment, verify helper refactor, install-script drift), ops/install_services.sh (UNITS += rdq-research.service), .gitignore (+/git_ignore_folder/), research/CLAUDE.md.
- Verified live (recorded): systemd-analyze --user verify OK; install script linked both units; `systemctl --user enable --now rdq-research.service` -> "Active: active (running)", journal shows "gateway connected" + "Serving Flask app 'rdagent.log.server.app'". curl http://127.0.0.1:19899/ -> HTTP 404 (no static index — an HTTP response per AC), /test -> HTTP 200 {"msgs":{},"pointers":{}}. ss -ltnH: LISTEN 127.0.0.1:19899 only. `tailscale serve status`: no 19899 mapping (only pre-existing nanoclaw 3100/443). Service left ENABLED + RUNNING for US-019/020. make check green (ruff pass, pyright 0 errors, 216 passed / 1 skipped — 15 new).
- **Learnings for future iterations:**
  - UI_ env vars (UI_TRACE_FOLDER, UI_STATIC_PATH, env_prefix UI_) are read at IMPORT time of rdagent.log.ui.conf and default CWD-relative (./git_ignore_folder/...) — set env before any rdagent.log import; monkeypatch module attrs (server_app.log_folder_path) in tests instead of env.
  - server_ui's Flask app has GET /test returning task/pointer state as JSON — cheap health/liveness probe for US-019's client and US-042's health.sh (GET / is 404 unless the static frontend is built).
  - POST /trace derives log_trace_path from UI_TRACE_FOLDER/<scenario>/<trace_name> — US-019's artifact locator should resolve under ~/rdq-runs/server_ui/traces, NOT the LOG_TRACE_PATH convention the CLI wrappers use.
  - Flask app.run kwargs are a clean monkeypatch seam (raising=False since run is a bound method); asserting the exact kwargs dict beats parsing ss output for offline tests.
  - systemd Environment= lines DO expand %h specifiers (verified: UI_TRACE_FOLDER landed as /home/nanoclaw/rdq-runs/...).
  - server_ui idles at ~36MB RSS with instant startup (Flask only, no torch/qlib imports until a loop starts) — safe to keep always-on.
---
## 2026-07-08 22:30 - US-019
- orchestrator/rdagent_client.py: typed client for server_ui speaking the REAL upstream protocol (the PRD's endpoint sketch was wrong; full mapping table added to docs/decisions.md). start_run(directive, universe, interaction=True) = POST /upload (scenario "Finance Whole Pipeline", loops/all_duration passthrough) THEN pre-seeds the two blocking init interactions via /user_interaction/submit: (1) {"user_instruction": directive + universe constraint}, (2) default base features (ALPHA20, lazy rdagent import, injectable for tests). pending() = POST /trace poll (all+reset) filtered to tag user_interaction.request, classified into kinds (init_params/base_features/hypothesis/feedback) with stable PendingInteraction.key for US-021's SQLite dedup. submit() answers the oldest unanswered interaction (FIFO IPC queue). stop() = POST /control. resume() sends action=resume+path and maps upstream's 400 "Only 'stop'" to UnsupportedActionError (US-024 adds server-side resume in research/server_ui.py). status() detects the END message; health() = GET /test. locate_artifacts(trace_dir) unpickles **/runner result/**/*.pkl newest-first and returns RunArtifacts(workspace_path, qlib_res.csv, ret.pkl|None, source_pkl).
- Files: orchestrator/rdagent_client.py (new), tests/test_rdagent_client.py (21 tests vs a threaded-Flask StubServerUi replicating upstream semantics incl. drain-one-pending-per-poll; artifact resolution on fixture log dirs: newest-wins, corrupt-pkl fallback, ret.pkl optional, error paths), docs/decisions.md (US-019 protocol entry), orchestrator/CLAUDE.md (client usage).
- Verified: make check green (ruff pass, pyright 0 errors, 237 passed / 1 skipped — 21 new). LIVE against the running rdq-research.service: health()=True, messages/pending on a fresh trace id = [], status finished=False, stop(unknown) -> RdAgentServerError "No running process for given id", resume() -> UnsupportedActionError with the US-024 pointer. No real run started (fin_quant is multi-hour); artifact locator verified on fixtures only — no real trace with a finished loop exists yet.
- **Learnings for future iterations:**
  - Upstream server_ui protocol (pinned rdagent/log/server/app.py): POST /upload STARTS runs; POST /trace is the message poll AND drains at most ONE pending user-interaction request per call; POST /receive is the subprocess logger's ingestion endpoint (not for us); /control only supports "stop". Don't trust the PRD's endpoint names for later stories — read the upstream source first.
  - fin_quant via server ALWAYS gets interaction queues and blocks at startup on init-params then base-features; responses are queued FIFO and independently of requests, so they can be submitted BEFORE the request appears. start_run exploits this to seed the directive without waiting for the subprocess to boot.
  - submit() cannot address a specific request — it answers whatever the run blocks on next (FIFO). US-021's poller must submit in-order and dedup posted requests via PendingInteraction.key (answered requests remain in the message stream forever).
  - CRITICAL for US-020: rdq-research.service has NO US env wiring (dates under 3 prefixes, FACTOR_CoSTEER_DATA_FOLDER(_DEBUG), APP_TPL, QLIB_QUANT_* hook classes) — a start_run today launches fin_quant with CN defaults. Add Environment=/EnvironmentFile= to the unit (mirror ops/run_us_quant.sh wire_env) before wiring start_research.
  - Trace dirs for server-started runs: ~/rdq-runs/server_ui/traces/<scenario>/<trace_name>; artifacts (qlib_res.csv, ret.pkl) live in the EXPERIMENT WORKSPACE, linked only through pickled 'runner result' objects — the /trace JSON stream never carries workspace paths.
  - StubServerUi (tests/test_rdagent_client.py) is a reusable threaded-Flask stub of server_ui — US-020/021/024 tests should reuse it. werkzeug make_server("127.0.0.1", 0, app) + daemon thread gives real-HTTP tests with ephemeral ports; flask imports free via rdagent's tree.
  - SimpleNamespace pickles by reference to types.SimpleNamespace — perfect for fixture pkls that production code reads duck-typed (no rdagent import needed to unpickle in tests).
---
## 2026-07-08 22:15 - US-020
- start_research tool: new ToolSpec in orchestrator/conversation.py registered alongside save_directive. Flow: require a saved directive (else is_error telling the model to save_directive first) -> reject if the thread already has a run (message names status + session_path) -> RdAgentClient.start_run(directive_instruction(directive), "us_liquid") -> record thread_ts <-> session_path (= str(client.trace_dir(trace_id))) in the runs table -> post a mrkdwn start notice in-thread. Race guard: if create_run raises DuplicateRunError after the launch, the orphan run is stopped via client.stop() before rejecting.
- directive_instruction() = objective + optional "Constraints: ..." line; universe_hint deliberately excluded (the enforced universe is passed separately; hint->universe mapping is US-023). DEFAULT_UNIVERSE="us_liquid" (only universe wired end-to-end today).
- ResearchLauncher protocol (start_run/trace_dir/stop) so tests stub the client without pyright complaints; ConversationCore(rdagent=...) injectable, defaults to a real RdAgentClient.
- RdAgentClient.trace_id_of(session_path) added — inverse of trace_dir(), so US-021's poller and US-024's stop/resume can recover the trace id from the stored session_path.
- PREREQ from US-019 notes FIXED: ops/rdq-research.service now wires the full US run env (dates under QLIB_QUANT_/QLIB_FACTOR_/QLIB_MODEL_, FACTOR_CoSTEER_DATA_FOLDER(_DEBUG), APP_TPL, QLIB_QUANT_*_HYPOTHESIS2EXPERIMENT hook classes, WORKSPACE_PATH=~/rdq-runs/server_ui/workspace, EnvironmentFile=research/.env for CHAT_MODEL etc. — non-optional so a missing file fails loudly instead of silently falling back to OpenAI/CN defaults). Without this, /upload-spawned fin_quant runs would backtest csi300 2008-2020.
- prompts.py: SYSTEM_PROMPT now instructs the model to call start_research only on an explicit operator ask and to relay duplicate-run rejections.
- Files: orchestrator/conversation.py, orchestrator/prompts.py, orchestrator/rdagent_client.py, orchestrator/CLAUDE.md, ops/rdq-research.service, ops/CLAUDE.md, tests/test_conversation.py (5 new), tests/test_rdagent_client.py (2 new), tests/test_services.py (2 new incl. a date-sync test vs run_us_quant.sh wire_env defaults), scripts/ralph/prd.json.
- Verified: make check green (ruff pass, pyright 0 errors, 246 passed / 1 skipped). systemd-analyze verify OK; service daemon-reloaded + restarted, active (running), GET /test -> 200, and /proc/<MainPID>/environ shows all US vars + CHAT_MODEL/EMBEDDING_MODEL/BACKEND from research/.env. LIVE smoke through the OneCLI proxy (real fable-5, real tmp StateStore, stubbed launcher): "please start the research run now" -> model called start_research, run row written (status running, universe us_liquid, session under ~/rdq-runs/server_ui/traces/), start notice posted; "start the research again" -> rejected, model relayed the active-run pointer. No real fin_quant run started (multi-hour; deliberate, consistent with US-017/019).
- **Learnings for future iterations:**
  - systemd per-unit Environment= lines do NOT show in `systemctl --user show-environment`; verify with `tr '\0' '\n' < /proc/$(systemctl --user show -p MainPID --value <unit>)/environ`.
  - EnvironmentFile handles research/.env's comment lines fine; made it non-optional (no `-` prefix) so a missing env file fails the unit loudly — spawned runs otherwise fall back to rdagent's OpenAI defaults and burn a run discovering it.
  - Tool handlers that raise ValueError produce is_error tool_results the model relays verbatim-ish — putting the actionable pointer (session_path, status) IN the exception message is what makes the Slack reply useful.
  - Duplicate-start needs BOTH the cheap pre-check (don't launch, then fail) and the DuplicateRunError catch (start race) — and the race path must stop() the just-launched run or it leaks a headless fin_quant process.
  - US-021 must NOT re-answer init_params/base_features interactions: start_run pre-seeds them, and pending() surfaces them anyway (dedup via PendingInteraction.key; skip those kinds).
  - runs.session_path convention: str(RdAgentClient.trace_dir(trace_id)); recover with trace_id_of(). US-024's stop_run: trace_id_of(run.session_path) -> client.stop().
---
## 2026-07-08 22:30 - US-021
- orchestrator/poller.py: HypothesisPoller — daemon polling thread (start/stop idempotent, survives any poll exception) checks every runs-table row with status 'running', maps session_path -> trace_id via trace_id_of(), and posts each NEW hypothesis interaction to its owning thread as Block Kit Approve/Edit/Reject buttons (button value = pending_interactions row id). Persist-then-post: row inserted first (UNIQUE interaction_key = schema dedup), Slack-post failure deletes the row so the next poll retries; submit failure never resolves a row (stays actionable, error said in-thread).
- Action mapping onto the upstream queue protocol (RDLoop._interact_hypo rebuilds via type(hypo)(**res_dict)): Approve = content unchanged; Edit = in-thread text round-trip (row -> 'editing', prompt posted; the operator's NEXT thread message is consumed by consume_edit_reply BEFORE the conversation core and submitted as {**content, "hypothesis": text}); Reject = same constructor keys with the hypothesis text replaced by an operator-rejection instruction — upstream has NO regenerate/skip action, the iteration still runs but the trace steers the next proposal (decision + rationale in docs/decisions.md).
- Liveness extras beyond the AC: feedback interactions auto-acked (submitted unchanged, row 'auto_approved') or every run deadlocks after its first experiment; FIFO guard — per run, processing stops at the first hypothesis still pending/editing so nothing jumps the answer queue (responses answer the OLDEST blocked request).
- state.py: +get_pending_interaction(id), +get_pending_interaction_by_key(key), +delete_pending_interaction(id). app.py: InteractionHandler protocol, create_app(interactions=...) registers hypo_approve/hypo_edit/hypo_reject action listeners + edit-reply interception in handle_message; main() shares one WebClient between Bolt and the poller and starts the poller before Socket Mode.
- Files: orchestrator/poller.py (new), orchestrator/state.py, orchestrator/app.py, tests/test_poller.py (21 new tests), tests/test_slack_app.py (make_app grew interactions param), docs/decisions.md, orchestrator/CLAUDE.md, scripts/ralph/prd.json.
- Verified: make check green (ruff pass, pyright 0 errors, 267 passed / 1 skipped — 21 new). Restart-persistence AC test: row survives fresh StateStore+poller, not reposted, still actionable. Bolt routing tested with REAL block_actions dispatches through App.dispatch. End-to-end smoke (/tmp/smoke_us021.py) over real HTTP against StubServerUi with the REAL RdAgentClient + real SQLite: start_run -> post+dedup -> approve (dict unchanged on the wire) -> restart -> edit round-trip -> reject (instruction submitted) -> feedback auto-ack: ALL PASS. Poller thread start/stop sanity-checked live.
- **Learnings for future iterations:**
  - Bolt block_actions payloads dispatch as BoltRequest(body=json.dumps(payload), mode="socket_mode") with NO event_callback envelope (interactive payloads ARE the body); Bolt injects ack/action/say and matches app.action(action_id) against actions[0].action_id.
  - pyright does NOT carry `if x is not None` narrowing into closures — alias to a local (handler = interactions) before defining listener functions that capture it.
  - Upstream _interact_hypo catches TypeError and falls back to the ORIGINAL hypothesis — a malformed submit payload silently un-does an edit/reject; always echo back the exact constructor key set.
  - US-022 hooks: poller already walks all running runs each tick — completion detection (client.status().finished -> update_run_status + summary post) belongs in the same poll_once loop; RunHandle.interaction flag is where auto-approve-without-operator would key off.
  - US-024 note: request_edit/'editing' rows survive stop_run — stop/resume tools should resolve or re-post stale editing rows for stopped runs.
## 2026-07-08 22:40 - US-022
- orchestrator/summary.py: load_metrics (qlib_res.csv = pandas-Series csv, NaN/non-numeric dropped), format_summary (IC/ICIR/Rank IC/ARR/IR/MDD/Sharpe; missing -> "n/a"; ARR/MDD as %, rest 4dp), compute_sharpe (annualized Sharpe of ret.pkl daily net return (return-cost), sqrt(252)), render_equity_curve (matplotlib Agg, strategy-net-of-cost + benchmark cumulative-sum curves -> PNG bytes). Metric-key mapping uses qlib's REAL recorder keys: SigAnaRecord logs IC/ICIR/Rank IC verbatim; ARR/IR/MDD are 1day.excess_return_with_cost.{annualized_return,information_ratio,max_drawdown}.
- KEY DEVIATION (recorded in docs/decisions.md): qlib logs NO Sharpe metric, so it cannot be "parsed from qlib_res.csv" — derived from ret.pkl instead (csv sharpe-named key wins if one ever appears; "n/a" when neither source exists).
- orchestrator/poller.py: _poll_run now checks client.status() first; finished runs skip interaction polling and go through _handle_completion — locate artifacts (injectable `locate=` param, default rdagent_client.locate_artifacts on run.session_path), post headline (:checkered_flag:/:octagonal_sign:/:x: by end_code) + metrics summary, upload equity_curve.png via files_upload_v2, THEN update_run_status to terminal_status(): 0/None->completed, -1->stopped, else->failed. Ordering is load-bearing: artifacts parsed/rendered before any post (corrupt/missing artifacts degrade to honest text, never a repost loop); status flip last so a transient Slack failure leaves the run 'running' and the next tick retries. InteractionClient protocol grew status(); SlackPoster grew files_upload_v2.
- Files: orchestrator/summary.py (new), orchestrator/poller.py, tests/test_summary.py (13 new tests incl. fixture builders write_qlib_res_csv/write_ret_pkl reused by test_poller), tests/test_poller.py (+9 completion tests; StubRdAgent grew status_by_trace, FakeSlack grew uploads), pyproject.toml (+matplotlib>=3.8, +pandas>=2.0 — both were venv-present but undeclared), docs/decisions.md, orchestrator/CLAUDE.md, scripts/ralph/prd.json.
- Verified: make check green (ruff pass, pyright 0 errors, 289 passed / 1 skipped — 22 new). End-to-end smoke (/tmp/smoke_us022.py) over real HTTP: StubServerUi + real RdAgentClient + real StateStore + real locate_artifacts on a fixture trace (SimpleNamespace runner-result pkl -> workspace with fixture qlib_res.csv + 120-day ret.pkl) + real matplotlib render -> summary with all 7 metrics posted in-thread, 78KB PNG uploaded (visually inspected: strategy vs benchmark curves, legend, dates), run row 'completed', second poll posted nothing. No real fin_quant run existed to test against (multi-hour; consistent with US-017/019/020 precedent).
- **Learnings for future iterations:**
  - qlib metric keys in qlib_res.csv (from qlib source, not docs): SigAnaRecord -> "IC"/"ICIR"/"Rank IC"/"Rank ICIR"; PortAnaRecord -> "1day.excess_return_{with,without}_cost.{mean,std,annualized_return,information_ratio,max_drawdown}". No Sharpe anywhere.
  - ret.pkl = qlib report_normal_1day DataFrame: columns account/return/total_turnover/turnover/cost/total_cost/value/cash/bench, trading-day index. qlib accumulates returns by SUMMATION not product (risk_analysis docstring) — charts should cumsum for consistency with the ARR/MDD numbers.
  - matplotlib: select the Agg backend BEFORE importing pyplot, keep both imports lazy inside the render function (module import stays fast for tests/pyright). fig.savefig(BytesIO, format="png") -> bytes feeds slack files_upload_v2(file=<bytes>) directly.
  - Completion-handler ordering pattern: parse/render everything BEFORE the first Slack call, terminal state flip LAST. Splits failures correctly: deterministic artifact problems -> one honest message; transient Slack problems -> full retry next tick (worst case a duplicate summary, never a lost one).
  - upstream END end_code semantics (rdagent/log/server/app.py): 0 = process completed, -1 = stopped via /control, other = subprocess died; error_msg is human text ("RD-Agent process has completed.").
  - matplotlib and pandas were venv-present but undeclared deps (recurring theme — check pyproject when importing anything "already available").
  - US-024 note: stopped runs now get terminal status 'stopped' automatically when the poller sees the END(-1) message — stop_run only needs to call client.stop(); the poller closes out the row and tells the thread.
---
## 2026-07-08 23:00 - US-023
- orchestrator/universe.py: UniverseService. propose(name, tickers) = validation only, NO data work: normalizes/dedups tickers, refuses built-in/reserved names (us_liquid/sp500/all, read from data/config.yaml via resolve_config so it tracks the config), refuses all-US proposals (ticker set ⊇ instruments/us_liquid.txt or ⊇ all.txt -> "use the built-in us_liquid"), warns below min_size (default 30, configurable) suggesting padding with liquid sector peers. materialize(name, tickers) = post-confirmation data work: gap check vs store spans (UniverseGapError listing every missing symbol; nothing written), data.make_universe (instruments file), data.make_factor_source (~/rdq-data/factor_source/<name>/{data_folder,data_folder_debug}), render_universe_templates (~/rdq-data/templates/<name>: US-016 template copies with the `market: &market us_liquid` anchor line rewritten to the custom name; hard-fails if the anchor drifted; other files byte-identical; regeneration wipes stale copies).
- conversation.py: set_universe ToolSpec (posts proposal + warnings in-thread, persists to the new universes table status 'proposed', rejects when the thread already has a run) and confirm_universe ToolSpec (materializes, flips row to 'confirmed', posts the ready notice) — model instructed (prompts.py) to call confirm_universe only after explicit operator confirmation. start_research now: unconfirmed proposal -> hard error; confirmed -> start_run(directive, <name>) and run row records universe name + tickers.
- state.py: universes table (thread_ts PK, upsert resets to 'proposed'); runs.universe_tickers JSON column — added to _SCHEMA for fresh DBs AND retrofitted to legacy DBs via guarded ALTER TABLE in migrate() (PRAGMA table_info check).
- Files: orchestrator/universe.py (new), orchestrator/conversation.py, orchestrator/state.py, orchestrator/prompts.py, orchestrator/CLAUDE.md, docs/decisions.md, tests/test_universe.py (21 new), tests/test_state.py (+6), scripts/ralph/prd.json.
- Verified: make check green (ruff pass, pyright 0 errors, 316 passed / 1 skipped — 27 new). AC tests: gap reporting, min-size warning, all-US refusal, template render with custom market all covered on real fixture stores + the real committed us_templates. LIVE smoke (/tmp/smoke_us023.py via onecli, real fable-5, REAL ~/.qlib us_data store, tmp roots): model called save_directive + set_universe (relayed the 3-ticker breadth warning and suggested peers unprompted), operator confirm -> confirm_universe materialized instruments/ai_semis.txt + factor source h5s + templates with market: ai_semis, start_research launched with universe ai_semis and run row carried ('NVDA','AMD','AVGO'). Smoke instruments file removed from the real store afterwards.
- **Learnings for future iterations:**
  - KNOWN GAP recorded in docs/decisions.md: rdq-research.service env pins FACTOR_CoSTEER_DATA_FOLDER + us_quant templates to us_liquid and server_ui spawns all runs from one env — custom universes reach a server-started run ONLY via the seeded instruction today. Per-run env needs a research/server_ui.py change (same file US-024 extends for resume); the artifacts are rendered now so that becomes pure wiring.
  - Adding a column to an existing SQLite table can't ride CREATE IF NOT EXISTS — put it in _SCHEMA (fresh DBs) plus a guarded ALTER in migrate() keyed on PRAGMA table_info (legacy DBs). Test with a hand-built legacy schema.
  - judgment_tool_loop turns ANY handler exception into an is_error tool_result, so domain errors (UniverseRefusalError/UniverseGapError) need no wrapping — put the operator-actionable text (gap list, us_liquid pointer) in the exception message and the model relays it.
  - Two-step tools with an operator gate: persist the proposal in SQLite (not model memory) and let the confirm tool read it back — survives restarts and keeps the confirm schema empty. Enforce the gate in start_research (refuse unconfirmed proposals) so a forgotten confirm can't silently fall back to the default universe.
  - tests/test_make_universe.build_fixture_store + tests.test_conversation helpers (RecordingSay/StubLauncher/start_research_script) compose cleanly for new tool tests — no new fixtures needed.
  - Live-smoke observation: fable-5 chained save_directive -> set_universe in ONE turn from a single message, and volunteered peer-padding suggestions off the warning text — warning strings ARE the UX, write them for the model to relay.
---
## 2026-07-09 00:40 - US-024
- stop_run / resume_run ToolSpecs in orchestrator/conversation.py. stop_run: /control stop via trace_id_of(session_path) -> cancels the thread's unanswered pending/editing interaction rows (status 'cancelled'; dead IPC queues, resumed runs re-propose under fresh keys) -> run row running->'stopped' -> stop notice in-thread. resume_run: /control resume from the STORED session_path with the saved directive re-seeded -> run row ->'running', which is what re-activates the poller (it polls status='running' rows). Both refuse with actionable errors (no run / wrong status / no directive).
- SERVER EXTENSION (the piece upstream lacks): research/server_ui.py install_resume_control() wraps the registered Flask view app.view_functions["control_process"] at RUNTIME (pinned tree untouched, RECORD-hash test still green). action=resume relaunches the scenario target from the dumped __session__ checkpoints (RDLoop.load via main(path=...), fin_quant/fin_factor/fin_model only) under the SAME trace id; guards: refuse while alive, session-exists check, path containment under UI_TRACE_FOLDER; old task messages carried over MINUS 'END' tags (a stale END would read as instantly-finished and the poller would close the resumed run on the first tick). Non-resume actions delegate to upstream unchanged.
- RdAgentClient.resume() grew directive=/universe= kwargs: a resumed interactive run re-runs _interact_init_params (blocks on init-params + base-features again), and the poller deliberately skips those kinds — so resume re-seeds both answers exactly like start_run. Resume without a directive would deadlock the run; the tool therefore requires a saved directive.
- Files: research/server_ui.py, orchestrator/rdagent_client.py, orchestrator/conversation.py, orchestrator/prompts.py, orchestrator/CLAUDE.md, research/CLAUDE.md, docs/decisions.md, tests/test_conversation.py (+8), tests/test_rdagent_client.py (+2, 2 updated), tests/test_server_ui.py (+7 incl. subprocess assert that main() installs the extension), scripts/ralph/prd.json.
- Verified: make check green (ruff pass, pyright 0 errors, 333 passed / 1 skipped — 17 new). LIVE: rdq-research.service restarted (extension deployed to the running control plane); probes recorded — resume unknown-scenario -> "cannot resume trace id...", missing session -> "no session to resume: ...", path escape -> "must live under the trace folder", stop/unknown-action delegate to upstream verbatim. End-to-end smoke via onecli (real fable-5, real RdAgentClient over real HTTP against resume-capable StubServerUi, real SQLite): "stop the run" -> /control stop + row 'stopped'; "resume that run" -> /control resume with stored path + 2 re-seeded init answers + row 'running'. ALL PASS. No real fin_quant resume executed (multi-hour; consistent with US-017/019/020 precedent).
- **Learnings for future iterations:**
  - Changing upstream server_ui endpoint behavior WITHOUT touching the pinned tree: wrap app.view_functions["<endpoint>"] at runtime (research/server_ui.py install_resume_control is the template). Read module globals (log_folder_path, rdagent_processes, RDAgentTask) through the module object at call time so tests can monkeypatch; guard with an app.config flag for idempotence.
  - RDLoop session resume mechanics: every fin target's main() takes path=; RDLoop.load(trace_dir, checkout=True) picks the latest __session__/<loop>/<step> dump and truncates later logs. The trace dir itself is a valid resume path — runs.session_path needs no extra bookkeeping.
  - A resumed interactive run re-asks init-params + base-features (main() re-runs _interact_init_params on the fresh IPC queues). Anything that resumes MUST re-seed both or the run hangs invisibly (poller skips init kinds by design).
  - When re-registering a task under an existing trace id, strip 'END' messages from carried-over history — status() scans for the END tag and the poller closes out any 'finished' run it polls.
  - Smoke scripts that hit LOOPBACK HTTP under `onecli run` need NO_PROXY=127.0.0.1,localhost pre-set (same trick as the systemd units) — the injected HTTPS_PROXY otherwise routes 127.0.0.1 through the gateway and the request dies with ProxyError.
  - tests/test_conversation.py error-path pattern: assert client.stream_calls[1]["messages"][2]["content"][0]["is_error"] is True and grep the content — reused cleanly for all 5 new rejection tests.
---
## 2026-07-09 01:00 - US-025
- orchestrator/notion_client.py: NotionClient — create_page (POST /v1/pages, optional children), query_db (POST /v1/databases/{id}/query with filter/sorts, paginates has_more/next_cursor into one list), update_page (PATCH /v1/pages/{id}, properties and/or archived; ValueError on neither). Every request sends ONLY Notion-Version: 2022-06-28 — no Authorization header anywhere (OneCLI connector injects the token for rdq-orchestrator; a source-grep test enforces the invariant).
- Retry design: 429 honors Retry-After (exponential fallback); 409/500/502/503/504 are transient (Notion 409 conflict_error = concurrent save, retry) — both bounded by max_retries then NotionRateLimitError/NotionError with the body's code+message. 401/403 -> NotionAuthError spelling out the connector-not-vault situation + the sharing check. Read-after-write eventual consistency = query_db_until(db_id, predicate, filter=) — re-queries with bounded exponential backoff (consistency_retries) then NotionConsistencyError.
- Files: orchestrator/notion_client.py (new), tests/test_notion_client.py (20 new tests: all three methods incl. pagination/filter passthrough/version header, 429/409/exhaustion/auth/error-detail paths, query_db_until satisfied/exhausted/filter, no-Authorization sweep + source grep), orchestrator/CLAUDE.md.
- Verified: make check green (ruff pass, pyright 0 errors, 353 passed / 1 skipped — 20 new). LIVE through the proxy (onecli run --agent rdq-orchestrator): client _request GET /v1/users/me -> bot user 'JPH NanoClaw Connection'; GET parent page 3979b1a4... -> 'Automated AI Quant Investment'; then full write round-trip with the REAL methods — create_page under the parent page returned a page id, update_page(archived=True) trashed it. All PASS.
- **Learnings for future iterations:**
  - Notion API responses/errors verified live: users/me works with connector-injected auth through the client's own session path (headers merge fine — requests merges per-call headers with the proxy's injected Authorization at the gateway, not client-side).
  - create_page under a PAGE parent needs the "title" property key (not a named DB property); DB-parented pages (US-026/027) use the database's property names.
  - Session fake for write clients: record .request(method, url, json=, headers=) — the GET-only FakeSession in test_fmp.py doesn't generalize; test_notion_client.py's version is the template for US-028 Alpaca.
  - Archived pages go to trash and update_page returns archived:true — cheap reversible live-write smoke pattern for any Notion story.
  - US-026: query_db_until is the idempotency primitive (create DB -> query by title until visible); pass consistency_retries/consistency_backoff at construction, not per call.
---
## 2026-07-09 01:55 - US-026
- ops/bootstrap_notion.py: creates the five Notion DBs (Research Ideas, Hypothesis Log, Backtest Results, Decision Log, Trade Ledger) under the operator's parent page (--parent-page-id, defaults to 3979b1a4-36cf-8046-baa5-cc14c1ca7665) and writes their ids into orchestrator/config.yaml under notion.databases (merging with any existing yaml). Idempotent: NotionClient.list_child_databases() maps child_database blocks by title under the parent — existing titles are reused, never recreated. Research Ideas is created FIRST; Hypothesis Log/Backtest Results/Decision Log carry single_property relations pointing at its id.
- docs/reference/notion-schema.md: full property schemas + the one-writer-per-DB convention table (conversation core -> Research Ideas, poller -> Hypothesis Log + Backtest Results, operator tools -> Decision Log, rebalancer -> Trade Ledger). A test cross-checks database_properties() property names against the doc's tables so code and doc can't drift.
- NotionClient gained create_database (POST /v1/databases) and list_child_databases (GET /v1/blocks/{id}/children, paginated via query string — _request signature unchanged). ops/ became a real package (ops/__init__.py + "ops*" in pyproject packages.find) so tests import it; reran pip install -e .
- Files: ops/bootstrap_notion.py, ops/__init__.py, docs/reference/notion-schema.md, orchestrator/notion_client.py, orchestrator/config.yaml (new, committed — ids are not secrets), tests/test_bootstrap_notion.py (10 new tests), pyproject.toml, ops/CLAUDE.md, orchestrator/CLAUDE.md.
- Verified: make check green (ruff pass, pyright 0 errors, 363 passed / 1 skipped). LIVE through the proxy (onecli run --agent rdq-orchestrator): first run created all 5 DBs; rerun printed 'exists' for all 5 (idempotency proven against real Notion); query_db on each of the five ids returned 200/0 rows. IDs: research_ideas 3989b1a4-36cf-81bc-87e9-c0ac0ea1cc2c, hypothesis_log 3989b1a4-36cf-81a8-a866-c9fd16c3b104, backtest_results 3989b1a4-36cf-81a3-b1dd-ce1ce20f535a, decision_log 3989b1a4-36cf-8145-9aa1-cdb3ed98c643, trade_ledger 3989b1a4-36cf-8184-9d9d-d6dfdcadbb54.
- **Learnings for future iterations:**
  - Detect-by-title = GET /v1/blocks/{page_id}/children filtered to type=="child_database" (block id == database id). No search API needed, and it scopes naturally to the parent page.
  - Relation property creation payload (version 2022-06-28): {"relation": {"database_id": ..., "single_property": {}}} — single_property avoids a synced back-ref property appearing on Research Ideas.
  - New DBs are queryable immediately via their id (query_db right after create returned 200) — query_db_until is only needed when finding PAGES by content after a write, not for id-addressed reads.
  - US-027 must load db ids from orchestrator/config.yaml (yaml.safe_load -> notion.databases.<key>); property names/types must match docs/reference/notion-schema.md exactly (Notion 400s validation_error on unknown property names).
  - yaml.safe_dump drops comments — write_config re-emits a fixed header; don't hand-annotate config.yaml expecting notes to survive a bootstrap rerun.
---
## 2026-07-09 - US-027
- Research runs recorded in Notion end-to-end: new orchestrator/notion_recorder.py (NotionRecorder) is the single Notion write funnel, wired into ConversationCore and HypothesisPoller (both take an optional recorder=; app.py main() builds it from config.yaml + a chat_getPermalink closure and degrades to disabled with a warning when config ids are missing).
- Lifecycle points: save_directive -> Research Ideas page (Idea title=objective, Raw Idea=first thread message, Directive rendered objective/hint/constraints, Universe, Status proposed, Thread permalink, Thread TS); start_research -> Status researching (+Universe); stop_run -> Status stopped + open hypothesis rows recorded 'cancelled'; resume_run -> researching; run END -> terminal status on the idea page. Poller: posted hypothesis -> Hypothesis Log row (Action pending, Interaction Key, Idea relation), approve/reject/edit -> Action updates (edit carries Operator Input); each FEEDBACK auto-ack -> Backtest Results row (metrics from qlib_res.csv via summary.METRIC_SPECS, Sharpe from csv-key-or-ret.pkl, SOTA=bool(feedback decision), Workspace, Universe, Idea relation).
- Files: orchestrator/notion_recorder.py (new), orchestrator/state.py (notion_pages table + set/get_notion_page), orchestrator/conversation.py + orchestrator/poller.py (hooks), orchestrator/app.py (wiring), tests/test_notion_recorder.py (22 tests), docs/reference/notion-schema.md (writer table -> NotionRecorder funnel), orchestrator/CLAUDE.md.
- Verified: make check green (ruff pass, pyright 0 errors, 387 passed / 1 skipped). LIVE smoke through the proxy (onecli run --agent rdq-orchestrator, /tmp/rdq_us027_smoke.py): idea + hypothesis + backtest pages created in the REAL databases, Status=researching / Action=edited+Operator Input / IC+SOTA+Sharpe verified by read-after-write queries (Thread TS / Interaction Key / Workspace filters), Idea relation intact, all smoke pages archived after.
- **Learnings for future iterations:**
  - NotionRecorder is best-effort BY DESIGN (log-and-swallow): call sites never try/except it, tests must assert on FakeSession.calls payloads (silent failure is invisible to callers). record_* return None on failure — the live smoke asserts non-None to detect problems.
  - Backtest Results rows are written at FEEDBACK auto-ack time, not run END: one feedback interaction = one completed experiment, its 'decision' field is the SOTA verdict, and the 'runner result' pkl (hence workspace metrics) already exists by then. Run END only moves the idea page Status.
  - notion_pages SQLite table maps (kind, key) -> page_id ('idea' by thread_ts, 'hypothesis' by interaction_key) so restarts keep updating the same pages; new tables ride CREATE IF NOT EXISTS with zero migration work.
  - Notion property-name contract: recorder payload keys must match docs/reference/notion-schema.md exactly (real API 400s on unknown properties — this is why the live smoke matters); metric property names reuse summary.METRIC_SPECS display labels.
  - Real Notion accepted select options (proposed/researching/edited...) auto-created on write as notion-schema.md promised — no schema migration needed for new statuses.
  - Notion page ids come back dashed from create but query filters/relations compare fine; normalize dashes only when comparing ids from different sources.
  - US-033 (promotion/Decision Log): extend NotionRecorder with a record_decision method (decision_log db id already in NotionDatabases) rather than adding a second write path.
---
## 2026-07-09 - US-028
- Alpaca paper client: execution/alpaca_client.py AlpacaClient with get_account, get_positions, list_orders (status/limit/symbols filters), place_order (market/limit, string wire form for qty/limit_price, client_order_id passthrough), cancel_order (204 -> None). Typed frozen dataclasses Account/Position/Order.
- Paper-only enforced in code: default base URL is paper-api.alpaca.markets; constructor raises ValueError on the live host (api.alpaca.markets appears only in the refusal guard). No APCA header anywhere — source-grep test asserts no `headers=` in the module; OneCLI proxy injects both paper secrets under rdq-exec-paper.
- Errors: 401/403 -> AlpacaAuthError naming rdq-exec-paper + ops/setup_onecli.sh + check script; 429 retried honoring Retry-After (exponential fallback, AlpacaRateLimitError on exhaustion); 5xx NEVER retried (deliberate: a 500 on POST /v2/orders is ambiguous — retry could double-submit); order params validated client-side before any HTTP.
- Files: execution/alpaca_client.py (new), tests/test_alpaca_client.py (28 unit + 1 live), execution/CLAUDE.md (new), scripts/ralph/prd.json.
- Verified: 28 unit tests pass; LIVE smoke `RDQ_LIVE_TESTS=1 onecli run --agent rdq-exec-paper -- pytest -m live` PASSED — get_account returned account id 3de6db59-fdc6..., status ACTIVE, equity 100000.0, currency USD. make check green (ruff pass, pyright 0 errors, 415 passed / 2 skipped).
- **Learnings for future iterations:**
  - Alpaca v2 returns numeric fields as STRINGS ("equity": "100000.25") — always parse; position qty is SIGNED (negative for shorts) alongside a side field; notional orders have qty: null; DELETE /v2/orders/{id} returns 204 no-body.
  - rdq-exec-paper identity + both paper secrets were already assigned by US-003's setup script — the live smoke needed zero new provisioning.
  - Money-touching HTTP clients must NOT blanket-retry 5xx: POST /v2/orders on a 500 is ambiguous (order may exist). Idempotency for the rebalancer (US-032/034) should come from client_order_id, not retries.
  - The paper account is fresh: $100k equity, ACTIVE, flat. US-034's first dry-run has a clean slate.
---
## 2026-07-09 01:35 - US-029
- execution/signal.py: signal extraction pipeline pred.pkl -> equal-weight TargetBook. topk_dropout_holdings() replicates qlib TopkDropoutStrategy.generate_trade_decision selection (method_buy=top, method_sell=bottom) line-for-line: rank held, admit at most n_drop+topk-len(held) candidates, drop bottom-n_drop of the combined ranking (sell-high-buy-low guard preserved), buy to the topk budget; held names missing from pred rank last (NaN) and drop first. Two DOCUMENTED deviations: ties rank alphabetically (upstream tie order is unstable-sort luck — unacceptable for a money-touching book) and degenerate slices clamped (upstream comb[-0:] would sell the WHOLE book at n_drop=0; today[:negative] would nonsense-buy on an over-held book).
- Config per AC: load_strategy_params(workspace) reads topk/n_drop from the workspace's own qlib conf*.yaml (port_analysis_config.strategy.kwargs) — jinja placeholders survive in real workspaces (qrun renders at runtime) so it renders with jinja2.Undefined before yaml.safe_load; multiple confs must agree or it refuses. Explicit --topk/--n-drop override. pred locator: newest-mtime mlruns/**/pred.pkl (matches read_exp_res.py "latest recorder" intent, no mlflow import). Freshness: latest cross-section date >= last calendar entry on/before as_of (default calendar ~/.qlib/qlib_data/us_data/calendars/day.txt) else SignalError "stale".
- No-partial-book policy: every failure (missing/stale/empty/all-NaN pred, dup holdings/instruments, empty selection) raises SignalError before a TargetBook exists; CLI (python -m execution.signal) prints JSON on success, ERROR to stderr + exit 1 on SignalError.
- Files: execution/signal.py (new), tests/test_signal.py (36 new tests incl. a 100-case parity sweep against the VERBATIM transcribed upstream selection lines, drop rule, tie handling at topk and drop boundaries, missing-from-pred drop-first, n_drop=0 guard, over-held clamp, real us_templates conf load -> (50,5), staleness/missing aborts, CLI happy/stale/missing/override), execution/CLAUDE.md (signal conventions for US-034), scripts/ralph/prd.json.
- Verified: make check green (ruff pass, pyright 0 errors, 451 passed / 2 skipped — 36 new). CLI driven end-to-end on disk against the REAL us_data calendar + real conf_baseline.yaml copy: fresh pred (2026-06-30 == last real trading day) with --topk 3 --n-drop 1 dropped worst-held TSLA for best-new AAPL, weights 1/3 each, exit 0; conf-sourced params run returned topk=50/n_drop=5 with all 5 names at 0.2; stale pred (2026-06-26) exit 1 with the actionable refresh message; missing pred exit 1. Fixtures cleaned up.
- **Learnings for future iterations:**
  - qlib TopkDropoutStrategy semantics quirks (from source): comb[-n_drop:] with n_drop=0 slices the WHOLE list (would sell everything); buy slice today[:len(sell)+topk-len(last)] goes negative on an over-held book. Replicate the algorithm but clamp degenerate slices — and know ties are sort-algorithm-arbitrary upstream, so deterministic replication must DEFINE a tiebreak (alphabetical here).
  - Deterministic tie recipe for pandas ranking: .sort_index(kind="stable") then .sort_values(ascending=False, kind="stable", na_position="last") — equal scores come out alphabetical, NaN last.
  - Parity-test pattern: transcribe the upstream lines verbatim into the test and diff outputs over seeded randomized inputs (100 books here) — much cheaper than standing up qlib Exchange infra, and it pins the exact selection semantics against upstream drift.
  - Workspace conf yamls are NOT plain YAML (jinja braces break safe_load) — render with jinja2 Environment(undefined=Undefined) first; the default(x, true) filters handle undefined vars.
  - pyright + pandas: Timestamp.to_pydatetime() unions NaTType — guard pd.isna() then cast(dt.datetime, ...); Index.get_level_values().max() needs an Any alias before pd.Timestamp().
  - US-034 contract: extract_targets(workspace, current_holdings, as_of=..., params=...) -> TargetBook{pred_date, weights (sum 1.0), params, pred_path}; treat SignalError as abort-without-trading. Weights are 1/len(holdings) (fully invested), NOT 1/topk — US-030's max-position gate is the backstop when the book is narrow.
---
