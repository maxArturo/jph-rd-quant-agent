# Ralph Progress Log
Started: (not yet started)
---

## Codebase Patterns
- Python-only repo (>=3.10). "Typecheck passes" means the Python typechecker (pyright), not TypeScript.
- Quality gate: `make check` = ruff + pyright + pytest (use it before every commit).
- Credentials: never in code or .env — bare HTTP calls through the OneCLI proxy (HTTPS_PROXY + CA cert); management API at http://127.0.0.1:10254.
- Live trading (THIS run adds it): `api.alpaca.markets` is reachable only via `AlpacaClient(..., allow_live=True)` from the live rebalancer under the `rdq-exec-live` identity. The orchestrator identity must NEVER be able to place live orders. Until the operator vaults live keys (their final manual step, after all stories pass), nothing can authenticate to the live host — every story must verify with fakes/fixtures.
- Paper behavior is regression-frozen: with SLACK_LIVE_CHANNEL_ID unset, existing tests must pass UNMODIFIED.
- Proxied calls from scripts: `onecli run --agent <identifier> -- curl ...`; banner on stderr, command stdout clean. See ops/CLAUDE.md for onecli CLI gotchas (UUID vs identifier, set-secrets replaces list).
- All services bind 127.0.0.1; human-facing UIs exposed via tailnet-only `tailscale serve` only.
- LLM calls go through LiteLLM under `onecli run` (placeholder keys OK — proxy overrides headers). claude-sonnet-5 rejects temperature!=1; JSON mode may return ```json fences. See research/CLAUDE.md.
- PRD source for this run: tasks/prd-live-trading.md (US-050..US-060 there map onto the finer-grained stories in prd.json).
- NEVER run the real test-suite path that provisions GPU droplets: ConversationCore.gpu stays None-default; only app.py wires the real GpuBackend (2026-08-06 incident).

## 2026-08-12 12:45 - US-001
- AlpacaClient live-host support behind explicit `allow_live` opt-in:
  - `allow_live: bool = False` keyword added to `AlpacaClient.__init__`; live host (api.alpaca.markets) still raises ValueError without it (message keeps the "paper-only" phrase the existing test greps for, and now names allow_live=True + who may pass it).
  - AlpacaAuthError guidance is host-aware: `_onecli_identity`/`_secrets_host` chosen at construction (live host -> rdq-exec-live + api.alpaca.markets; everything else incl. test stubs -> rdq-exec-paper + paper host). No hardcoded "paper" in the message f-string.
  - Module + class docstrings rewritten for opt-in semantics; execution/CLAUDE.md first bullet rewritten (LIVE IS OPT-IN, only live rebalance/reconcile/flatten may pass the flag, orchestrator never).
  - New tests: TestLiveOptIn (live accepted with flag, paper works with/without flag, default unchanged, live requests hit live host, live auth error names rdq-exec-live and contains no "paper" substring) + explicit allow_live=False refusal test. All existing tests pass unmodified.
- Files changed: execution/alpaca_client.py, execution/CLAUDE.md, tests/test_alpaca_client.py; plus two pre-existing gate breakages fixed: execution/pred_refresh.py:511 (E501 from the US-049 merge) and execution/pred_refresh_predict.py:60 (torch is docker-only -> pyright: ignore[reportMissingImports]).
- Verified: `make check` fully green (ruff, pyright, 1054 passed / 3 skipped); manually drove refusal, allow_live construction, and identity selection via a python snippet (observed refusal message, live base_url + rdq-exec-live, paper default + rdq-exec-paper).
- **Learnings for future iterations:**
  - This worktree's .venv was bootstrapped incomplete: rdagent missing (run `bash research/install.sh` — installs pinned commit + pydantic-ai-slim 1.107.0) and git-ignored env files absent. Copy `/home/nanoclaw/rd-agent-q/research/.env` and `/home/nanoclaw/rd-agent-q/.env` into the worktree or tests/test_run_us_quant.py --check tests fail.
  - `make check` was ALREADY red on the merge base (E501 in pred_refresh.py, missing torch import for pyright) — don't assume a clean gate at iteration start; both are fixed as of this commit.
  - tests/test_alpaca_client.py greps source: no "APCA-API-KEY-ID" (outside the one docstring phrase) and no "headers=" anywhere in execution/alpaca_client.py — never add either.
  - test_live_host_is_refused matches "paper-only" in the ValueError — keep that phrase if the refusal message changes again.
  - Client auth guidance derives identity/secrets-host at construction time (self._onecli_identity / self._secrets_host) — reuse those fields if later stories need host-aware wording.
---

## 2026-08-12 14:05 - US-002
- Live guardrail config, all data+paths, zero code forks:
  - execution/limits.live.json (500 / 10 / 60 / 60) — loads via the existing order_gate.load_limits; new constant order_gate.LIVE_LIMITS_PATH.
  - execution/breaker.live.json (5000 / 5) — loads via load_breaker_config; new constant breaker.LIVE_CONFIG_PATH.
  - execution/allocation.py (new module): LiveAllocation dataclass + load_live_allocation() over execution/allocation.live.json (required key live_equity_allocation_pct=10; refuses missing/unknown keys, non-numbers incl. bool, and pct outside 0<pct<=100; AllocationConfigError).
  - Live breaker state constants breaker.LIVE_STATE_DIR / LIVE_HALT_FILE / LIVE_HWM_FILE under ~/rdq-data/breaker-live/ — distinct from paper's ~/rdq-data/breaker/.
  - Docstrings updated (breaker.py, order_gate.py) + execution/CLAUDE.md bullet on the live-guardrails-are-data-not-forks pattern.
- Tests: TestLiveBreaker in tests/test_breaker.py (committed live config values, live-vs-paper path distinctness, halt independence both directions, separate HWM files + live drawdown trip not consulting paper's peak, corrupt live HWM -> BreakerStateError "refusing to trade"); test_committed_live_limits_file_loads in tests/test_order_gate.py; new tests/test_allocation.py (12 cases: committed load, float coercion, 100-boundary, missing file/key, bad JSON, non-object, unknown key, non-numeric incl. True/False/str, out-of-range 0/-5/100.001/250).
- Files changed: execution/{limits.live.json,breaker.live.json,allocation.live.json,allocation.py,breaker.py,order_gate.py,CLAUDE.md}, tests/{test_allocation.py,test_breaker.py,test_order_gate.py}.
- Verified: make check fully green (ruff, pyright, 1077 passed / 3 skipped); manual smoke drove all three loaders + printed live state paths (observed BreakerConfig(5000, 5), Limits(500, 10, 60, 60), LiveAllocation(10.0), ~/rdq-data/breaker-live/{halt,high_water_mark.json}).
- **Learnings for future iterations:**
  - Breaker/gate loaders are already fully parameterized — live variants of guardrails need ONLY new json files + path constants; never fork the classes (AC explicitly forbids it).
  - Loader strictness idiom repeated 3x now (unknown keys refused -> missing keys refused -> per-key type/range check, bool excluded via isinstance(value, bool) FIRST since bool is an int subclass); copy that shape for any future config file.
  - Committed-config tests assert exact live numbers (limits 500/10/60/60, breaker 5000/5, allocation 10) — deliberate: operator edits to the committed files must trip a test so guardrail changes are reviewed. Update tests when the PRD numbers change.
  - NaN can't sneak through allocation range check: 0 < nan <= 100 is False -> refused.
  - US-013 wiring note: rebalance --live must scale equity BEFORE diff (allocation pct), pass real buying_power to cap_buys_to_buying_power, and construct Breaker(load_breaker_config(LIVE_CONFIG_PATH), LIVE_HALT_FILE, LIVE_HWM_FILE).
---
## 2026-08-12 15:10 - US-003
- Live promoted-strategy slot, fully independent of paper:
  - orchestrator/state.py: new `promoted_strategy_live` table in _SCHEMA (same single-row shape, `CHECK (id = 1)`); paper `promoted_strategy` table untouched. Rides `CREATE TABLE IF NOT EXISTS` — no guarded ALTER needed for a whole new table (migrate() adds it to pre-live DBs automatically; test drops the table and reopens to prove the retrofit).
  - StateStore methods: `set_promoted_strategy_live(workspace_path, config)` (upsert, returns PromotedStrategy), `get_promoted_strategy_live()`, `clear_promoted_strategy_live()` (demote; delete is a no-op when already empty). Config dict carries paper keys + `live_equity_allocation_pct` for audit — the store stays shape-agnostic like paper's; US-009's promotion backend is responsible for populating the pct.
  - execution/promoted.py: `load_promoted_strategy_live(db_path)` mirroring paper refusals exactly (DB absent — never created; no live row — even when paper IS promoted; workspace dir gone) with live-specific messages + `LIVE_PROMOTE_HINT` ("promote-to-live message in the live Slack channel"). Module docstring covers both loaders.
- Tests: test_state.py live-slot section (empty/set-get incl. pct round-trip/replace-keeps-single-row/clear/both-direction paper-live independence/pre-live DB retrofit); test_promotion.py live-loader section (no-DB, paper-promoted-but-live-empty refusal, workspace-gone, live-vs-paper slot selection, paper loader ignores live-only promotion).
- Files changed: orchestrator/state.py, execution/promoted.py, orchestrator/CLAUDE.md, execution/CLAUDE.md, tests/test_state.py, tests/test_promotion.py.
- Verified: make check fully green (ruff, pyright, 1089 passed / 3 skipped, +12 new); manual smoke drove the full lifecycle in a tmp DB (empty-slot refusal with live hint -> promote-to-live -> live loader returns live ws + pct 10 while paper loader returns paper ws -> demote -> refusal again, paper untouched -> absent-DB refusal without creating the file).
- **Learnings for future iterations:**
  - Adding a whole NEW table needs no migrate() retrofit code — `CREATE TABLE IF NOT EXISTS` in _SCHEMA covers old DBs (guarded ALTERs are only for new COLUMNS on existing tables). US-005's runs.channel_id WILL need the guarded-ALTER pattern.
  - PromotedStrategy dataclass is slot-agnostic — reuse it for live; don't create a LivePromotedStrategy type.
  - `StateStore(db_path)` migrates on construction, so the promoted.py loaders check `db_path.is_file()` BEFORE constructing the store (that's what keeps "never creates the DB" true). Preserve that order in any new loader.
  - test_promotion.py's `store` fixture pins module-level DBS config; plain `StateStore(tmp_path / ...)` tests that don't need Notion can skip it entirely (the new live-loader tests do).
  - US-009/US-010 wiring notes: live promotion backend should call set_promoted_strategy_live with the paper config keys + live_equity_allocation_pct (from execution.allocation.load_live_allocation), demote calls clear_promoted_strategy_live; both write Decision Log entries.
---
## 2026-08-12 16:05 - US-004
- SLACK_LIVE_CHANNEL_ID config plumbing:
  - SlackConfig gains `live_channel_id: str | None = None` (frozen dataclass, default keeps equality with pre-live constructions so existing tests pass unmodified).
  - load_slack_config reads SLACK_LIVE_CHANNEL_ID with the same env-over-file precedence as the required keys; absent, empty, or whitespace-only -> None (`(env or file or "").strip() or None`).
  - Validation: when set and equal to SLACK_CHANNEL_ID, ConfigError naming BOTH vars ("live channel ... can never be the paper research channel").
  - Docs: no .env.example in this repo — the config.py module docstring IS the config doc (added the var + "setting it ARMS live features" wording); orchestrator/CLAUDE.md Slack-env bullet updated too.
- Files changed: orchestrator/config.py, orchestrator/CLAUDE.md, tests/test_slack_app.py.
- Verified: make check fully green (ruff, pyright, 1095 passed / 3 skipped, +6 new); manual snippet drove absent/empty/set/process-override/equal-channels through load_slack_config against real tmp .env files (observed None/None/C2LIVE/CENV/ConfigError naming both vars).
- **Learnings for future iterations:**
  - There is NO .env.example in this repo; env vars are documented in orchestrator/config.py's module docstring (Required/Optional lists) — put new-var docs there.
  - Adding an optional field WITH a default to the frozen SlackConfig keeps every existing `SlackConfig(...)`/equality test green — follow that shape for future optional config.
  - US-006 wiring note: app.py's actionable check should accept `config.live_channel_id` when not None; tests/test_slack_app.py CONFIG constant is paper-only — build a second CONFIG_LIVE with live_channel_id set for dual-channel tests.
---
## 2026-08-12 17:00 - US-005
- runs table records originating channel:
  - orchestrator/state.py: `channel_id TEXT` on runs (in _SCHEMA for fresh DBs + guarded ALTER in migrate() for pre-US-005 DBs — the runs-column pattern predicted in the US-003 notes). Run dataclass gains `channel_id: str | None = None`; create_run accepts/persists it; `_run_from_row` reads it.
  - New accessor `StateStore.run_channel(thread_ts, paper_channel_id)` — returns the run's stored channel, falling back to the paper channel for NULL columns (pre-migration rows) AND unknown threads (never raises; callers are background posters).
  - ConversationCore ctor gains `channel_id: str | None = None` (stored as self._channel_id, stamped onto the run row in start_research); app.py main() wires `channel_id=config.channel_id`, so every paper run now records the paper channel explicitly. None default keeps all existing core tests/constructions green.
- Files changed: orchestrator/state.py, orchestrator/conversation.py, orchestrator/app.py, orchestrator/CLAUDE.md, tests/test_state.py, tests/test_conversation.py.
- Verified: make check fully green (ruff, pyright, 1099 passed / 3 skipped, +4 new); manual smoke built a pre-US-005 DB by hand (no channel_id column), opened it via StateStore twice (migration + idempotent reopen) and observed legacy row -> None/C_PAPER fallback, new row -> C_LIVE stored+returned, unknown thread -> C_PAPER, PRAGMA shows channel_id appended.
- **Learnings for future iterations:**
  - Guarded-ALTER migration puts the new column at the END of legacy tables while _SCHEMA puts it mid-table on fresh DBs — irrelevant because _run_from_row reads by name, but don't write INSERTs with positional VALUES against runs in new code/tests without naming columns (existing legacy-DB tests do positional inserts against the OLD schema on a raw connection, which is fine).
  - make_core in tests/test_conversation.py now takes channel_id= — use it for US-006 dual-channel core tests.
  - US-006/007 wiring notes: per-message channel should eventually override the ctor default (ctor channel is the US-005 stepping stone); background posters (poller/gpu) must resolve destinations via store.run_channel(run.thread_ts, config.channel_id) — the accessor is deliberately total (no KeyError) so poster loops can't crash on a deleted run.
---
## 2026-08-12 18:20 - US-006
- Dual-channel message routing + system prompt:
  - app.py: `_is_actionable_user_message`/`handle_message` gain `live_channel_id=None`; the channel check is now membership in {paper, live} (live only when set); every other filter (subtype, bot_id, trusted-bot mention gate, thread derivation) untouched. create_app wires `config.live_channel_id`; main() logs when the live channel is armed. Module docstring covers both channels.
  - Channel transport WITHOUT breaking any protocol/fake: the per-message channel rides Bolt's `Say` (Bolt binds `say.channel` to the event's channel). New `_say_channel(say)` helpers in conversation.py and poller.py return the channel only when it is a non-empty str (MagicMock/RecordingSay -> None -> exactly today's behavior). ConversationCore.handle_message gains optional `channel=` (derived from say when omitted); start_research stamps `channel or self._channel_id` onto the run row, so live-channel runs record the live channel (US-007 routes home via store.run_channel).
  - Collision keying (AC 3): poller.consume_edit_reply gains optional `channel=` (say-derived) and returns False when the message's channel differs from `store.run_channel(thread_ts, paper)` — a live-channel message can never be eaten as a paper thread's edit text. ConversationCore._awaiting_hypothesis(thread_ts, channel) refuses spoken approve/reject when the run belongs to another channel.
  - prompts.py: "live trading is out of scope" removed; new paragraph describes both channels (live = REAL-MONEY Alpaca account) and independent paper/live promotion slots; ground rule keeps the "never trade"/"explicit approval" phrases the persona test greps.
- Tests: test_slack_app.py dual-channel section (CONFIG_LIVE + make_app(config=...): live message actionable + reply lands in live channel via real Bolt dispatch, paper still handled, unknown channel ignored, live channel ignored when unconfigured, bot/subtype filtered in live channel, handle_message unit with live_channel_id); test_conversation.py (prompt coverage, ChannelSay stamping over ctor default, RecordingSay fallback, cross-channel approve refused / owning-channel approve allowed); test_poller.py (edit reply ignored from foreign channel, consumed from owning channel).
- Files changed: orchestrator/{app.py,conversation.py,poller.py,prompts.py,CLAUDE.md}, tests/{test_slack_app.py,test_conversation.py,test_poller.py}.
- Verified: make check fully green (ruff, pyright, 1112 passed / 3 skipped, +13 new); manual smoke proved the integration seam the tests compose: real `slack_bolt` Say(channel="C0LIVECHAN") -> both _say_channel helpers return it; Say(channel=None) and MagicMock -> None (guards skipped). Bolt binding say to the event channel is exercised by the dispatch tests (reply kwargs channel == live channel).
- **Learnings for future iterations:**
  - The paper-freeze constraint ("existing tests pass unmodified") rules out widening MessageResponder/InteractionHandler protocols — FakeConversation/FakeInteractions define exact 3-arg signatures and pyright checks them structurally. The say-attribute transport (`say.channel`, a real Bolt Say ctor param) threads the channel through WITHOUT any call-signature change; implementations may still add optional trailing kwargs (consume_edit_reply's channel=) and keep satisfying the protocol.
  - `_say_channel` must isinstance-check str: `getattr(MagicMock(), "channel")` is a truthy Mock and would silently activate channel guards in tests.
  - make_app in test_slack_app.py now takes `config: SlackConfig = CONFIG` (use CONFIG_LIVE for dual-channel tests); test_poller.py imports make_app/CHANNEL from it.
  - `add_pending_interaction` works without a run row (older tests rely on it) — channel guards must treat "no run" as "no owner info" and allow, or half the suite breaks.
  - US-007 wiring notes: poller digests/completion/buttons still post to self._channel_id — swap those `channel=self._channel_id` sites to `store.run_channel(run.thread_ts, self._channel_id)`; completion messages for live-channel runs must NOT attach the paper Promote button.
  - US-008/US-010 wiring: ConversationCore will need `live_channel_id` in the ctor; per-message channel already arrives in handle_message — pass it into new live tools the way _start_research_tool/_awaiting_hypothesis take it.
---
## 2026-08-12 19:30 - US-007
- Channel-aware background posters:
  - poller.py `_run_home(run)` = `store.run_channel(run.thread_ts, self._channel_id)`; every run-scoped post now uses it: `_post_hypothesis` (buttons), `_narrate` (auto-approve narration, feedback verdicts, budget/identical-error notices), `_handle_completion` (summary post + `files_upload_v2` chart). Paper fallback preserved: NULL/legacy rows and unknown threads resolve to the ctor channel, so pre-migration behavior is byte-for-byte (all existing poller tests pass unmodified).
  - Completion promote rule: promotable statuses with artifacts get the Block Kit Promote button ONLY when the run's home == the paper channel; otherwise (live-channel run) the summary text appends `LIVE_PROMOTABLE_NOTE` ("This run is promotable — say *promote to live* in this channel..."), no blocks. Failed runs get neither, as before.
  - Button handlers needed NO code change: approve/reject/request_edit respond via Bolt's `say`, which is bound to the clicked message's channel (buttons only exist in the run's home channel). Proven by a real Bolt block_actions dispatch test with a live-channel container.
- Tests (8 new): buttons post to live channel; approve round-trip resolves records for a live-channel run; edit round-trip keyed to live channel (paper-channel collision message NOT consumed, live message consumed); real Bolt dispatch replies in clicked channel; autonomous narration posts home; live completion -> live channel, no blocks, note present, chart upload to live channel; live FAILED run gets no note; paper run with STORED paper channel keeps the exact "Promote to paper trading" button. dispatch_action helper gained `channel: str = CHANNEL` (default keeps existing callers byte-identical).
- Files changed: orchestrator/poller.py, orchestrator/CLAUDE.md, tests/test_poller.py.
- Verified: make check fully green (ruff, pyright, 1120 passed / 3 skipped, +8 new); manual smoke drove a STOPPED (end_code -1) live-channel run through poll_once -> observed post+upload channel C0LIVECHAN, no blocks, promotable note as text tail, run row flipped to 'stopped'.
- **Learnings for future iterations:**
  - Bolt's `say` in block_actions handlers is bound to the CONTAINER's channel — button responses route correctly for free; only proactive `chat_postMessage`/`files_upload_v2` calls need explicit `_run_home` resolution. When adding any new poller post, use `_run_home(run)`, never `self._channel_id` directly.
  - `store.run_channel` is one extra SQLite read per post — fine at poller cadence; don't cache it on the run object (Run rows are re-fetched every poll anyway).
  - The live-vs-paper decision in `_handle_completion` is `channel == self._channel_id` (home == paper ctor channel), NOT "channel_id column is set" — paper runs stamped with the paper channel since US-005 must keep the button.
  - test_poller.py's dispatch_action now takes `channel=`; test_slack_app exports CONFIG_LIVE/LIVE_CHANNEL for dual-channel tests (import them, don't redefine).
  - US-010 wiring note: LIVE_PROMOTABLE_NOTE names the spoken phrase "promote to live" — keep the promote_to_live tool's trigger phrasing consistent with it.
---
## 2026-08-12 20:40 - US-008
- halt_live_trading / resume_live_trading on the live breaker paths:
  - ConversationCore ctor gains `live_channel_id` + `live_breaker` (TradingBreaker); the live breaker is default-constructed on breaker.LIVE_* paths ONLY when the live channel is armed, so paper-only deployments never touch ~/rdq-data/breaker-live/. app.py wires config.live_channel_id.
  - Tools registered only when live_channel_id is set (paper-only cores never expose them). Channel gating asymmetric: `_require_live_channel` demands channel == live_channel_id (unknown/say-without-channel refuses — real money needs positive ID); `_refuse_paper_tool_in_live_channel` makes paper halt_trading/resume_trading refuse ONLY a positively-identified live channel (unknown stays permissive = paper freeze). Refusal messages point at the other channel/tool pair in both directions.
  - format_live_trading_halted/format_live_trading_resumed say "LIVE ... real-money account" (:rotating_light:), distinct from paper's wording; handlers mirror paper semantics (already-halted keeps note, not-halted resume errors) + Decision Log rows (types halt_live/resume_live).
  - prompts.py halt bullet extended: live tools are the real-money account's SEPARATE kill switch, each pair channel-bound.
- Files changed: orchestrator/{conversation.py,app.py,prompts.py,CLAUDE.md}, tests/test_trading_halt.py.
- Verified: make check fully green (ruff, pyright, 1134 passed / 3 skipped, +14 new); manual smoke drove live halt from live channel (live halt file written, paper's absent), live halt refused from paper channel AND from unknown channel, paper halt refused from live channel, live resume (paper halt untouched), and a paper-only core exposing no live tools.
- **Learnings for future iterations:**
  - Live-only tools pattern is now established in conversation.py: registration gate on self._live_channel_id + _require_live_channel(channel, tool) (returns the live breaker) + _refuse_paper_tool_in_live_channel for the paper twin. US-010's promote_to_live/demote_live should reuse all three.
  - Paper tool builders now take `channel` (_halt_trading_tool/_resume_trading_tool are 3-arg) — any new paper tool with a live twin needs the channel param threaded the same way.
  - tests/test_trading_halt.py has make_live_core/make_live_breaker/live_halt_script/live_resume_script + tool_names()/tool_error() helpers; ChannelSay lives in tests.test_conversation, CHANNEL/LIVE_CHANNEL in tests.test_slack_app.
  - HALT_TRADING_SCHEMA/RESUME_TRADING_SCHEMA are reused verbatim by the live tools (same reason-string / no-input shapes) — don't fork schemas that don't differ.
  - Notion Decision Log "Type" select auto-creates new options (halt_live/resume_live needed no schema change); recorder tests assert the exact select name.
---
## 2026-08-12 21:50 - US-009
- Live promotion backend (LivePromotion in orchestrator/promotion.py):
  - Refactor: PromotionFlow._candidate split — run-row lookup + status check stay in _candidate, the provenance derivation (locate artifacts, topk/n_drop, conf-market universe, instruments tickers, metrics/sharpe) is now public candidate_from_run(run), the single path shared by paper and live promotion (AC: no re-derived logic).
  - LivePromotion(store, flow, recorder=, load_allocation=, snapshot=): promote(reference=, thread_ts=, trigger_permalink=) resolves explicit ref (exact thread_ts, or >=6-char fragment of session_path; ambiguity/no-match refuse with run listings) -> current thread's run -> paper slot copied IN FULL (config dict verbatim + live_equity_allocation_pct; workspace-dir existence checked; metrics degrade-load from workspace qlib_res.csv). Non-promotable status refused naming the status. All refusals = LivePromotionError raised BEFORE any write.
  - Snapshot rules (differ from paper deliberately): complete conf+env+params set NEVER touched (operator-pinned market preserved); incomplete set regenerated via snapshot_pred_refresh with a warning in result.warnings when a conf existed (promote_fetched rule); snapshot FAILURE refuses (paper warns-and-promotes — real money must stay refreshable).
  - Success: set_promoted_strategy_live + Decision Log row (type promote_live; details carry workspace, source, universe + ticker count, allocation pct, metrics, replaced, Triggered by: <permalink>). demote() clears the slot (refuses when empty) + demote_live row stating the next live rebalance will abort.
  - LivePromotionResult carries everything US-010's armed summary needs (promoted row, source, metrics/sharpe, universe_label/label_mismatch, warnings, replaced).
- Files changed: orchestrator/promotion.py, orchestrator/CLAUDE.md, tests/test_live_promotion.py (new, 21 tests), scripts/ralph/prd.json.
- Verified: make check fully green (ruff, pyright, 1155 passed / 3 skipped, +21); manual smoke drove the full lifecycle in a tmp DB (no-candidate refusal -> direct-run promote with snapshot call + live slot pinned incl pct 10.0 while paper slot stays None -> unknown-ref refusal listing promotable runs -> demote returns the row and empties the slot -> empty-demote refusal).
- **Learnings for future iterations:**
  - PromotionFlow.candidate_from_run(run) is the reusable provenance derivation — US-010+ must never re-derive market/tickers; construct LivePromotion over a PromotionFlow (tests: make_live in tests/test_live_promotion.py mirrors make_flow).
  - Decision Log has NO permalink property — permalinks go into the Details rich_text. app.py's existing _permalink closure is paper-channel-bound (chat_getPermalink(channel=config.channel_id)); US-010 must resolve the live-channel permalink itself and pass trigger_permalink=.
  - record_decision(thread_ts=) links the Idea relation only when an idea page mapping exists — safe to pass the source run's thread unconditionally.
  - The Notion Type select auto-creates promote_live/demote_live (same as halt_live) — no bootstrap change needed.
  - test fixture gotcha: promotable_artifacts writes instruments/ next to the workspace under tmp_path — a second run in the same store can reuse the same artifacts stub via locate=lambda.
  - US-010 wiring: tool passes channel-checked (reuse _require_live_channel), calls live.promote(reference=<optional user ref>, thread_ts=thread, trigger_permalink=<resolved>), posts armed summary from LivePromotionResult (+ live limits/breaker values it reads itself), and must ALSO check the live breaker halt before promoting (backend deliberately does not — it's the tool's refusal path per US-010 AC).
---
## 2026-08-12 23:00 - US-010
- promote_to_live / demote_live Slack tools — one message, no confirmation:
  - ConversationCore ctor gains `live_promotions` (LivePromotionManager protocol over US-009's LivePromotion) + `permalink` ((channel, message_ts) -> url resolver). Tools registered only when live_channel_id AND live_promotions are both wired; channel-gated via the existing _require_live_channel (paper/unknown channel refuses).
  - Arm path: guardrails FIRST (_live_guardrails loads limits.live.json + breaker.live.json — unusable config refuses with nothing written, and the numbers feed the summary), live-halt check (tool-side by design; backend doesn't own it), then backend.promote(reference=, thread_ts=, trigger_permalink=) and format_live_armed posted immediately — workspace id, source (paper copy vs direct run), METRIC_SPECS metrics + Sharpe, universe + ticker count + label-mismatch call-out, allocation pct, live limits/breaker numbers, replaced line, snapshot warnings, LIVE_REBALANCE_SCHEDULE ("Mon–Fri 08:10 America/New_York (rdq-rebalance-live.timer)").
  - demote_live clears via backend.demote and posts format_live_demoted (what was demoted + next live rebalance aborts; paper slot unaffected). Backend LivePromotionError refusals surface as error tool_results (judgment_tool_loop catches ALL handler exceptions — no conversion needed).
  - Permalink is best-effort (_trigger_permalink swallows resolver failures -> None); app.py wires a channel-aware chat_getPermalink closure + constructs LivePromotion(store, promotions, recorder=) only when config.live_channel_id is set. prompts.py: live promotion is ONE step, never ask confirmation first, armed summary IS the confirmation.
- Files changed: orchestrator/{conversation.py,app.py,prompts.py,CLAUDE.md}, tests/test_live_promote_tools.py (new, 19 tests), scripts/ralph/prd.json.
- Verified: make check fully green (ruff, pyright, 1174 passed / 3 skipped, +19); manual smoke drove the REAL LivePromotion backend through the tools in a tmp store (paper slot + complete-snapshot workspace -> promote_to_live pinned the live slot with pct 10.0 and posted the full armed summary -> demote_live emptied the slot, paper slot untouched, notices as formatted).
- **Learnings for future iterations:**
  - judgment_tool_loop catches ALL handler exceptions (not just ValueError) into is_error tool_results — backend error types (LivePromotionError) can propagate from tool handlers as refusals without wrapping.
  - _live_guardrails reads order_gate.LIVE_LIMITS_PATH / breaker.LIVE_CONFIG_PATH at CALL time via module attrs — tests monkeypatch those module constants to simulate missing/malformed live config.
  - The armed-summary numbers come from the committed live config files, so the formatter tests assert $500/$5,000/5%/10% — they'll trip (deliberately) if the operator's guardrail numbers change.
  - ConversationCore now has a general (channel, message_ts) permalink resolver (`self._permalink` / `_trigger_permalink`) — reuse it for any future channel-aware audit line instead of adding another closure to app.py.
  - LIVE_REBALANCE_SCHEDULE (conversation.py) states the live timer cadence — US-019 must keep rdq-rebalance-live.timer at Mon-Fri 08:10 America/New_York or update the constant with it.
  - US-012 wiring note: check_live_* tools should follow the same registration shape (live_channel_id + their own dependency wired) and _require_live_channel gating.
---
## 2026-08-13 00:10 - US-011
- Live Notion databases: bootstrap, config keys, schema doc:
  - orchestrator/notion_client.py: new get_page (GET /v1/pages/{id}), search_pages (POST /v1/search, page filter, paginated), append_children (PATCH /v1/blocks/{id}/children), list_children (raw paginated block children); list_child_databases refactored over list_children (byte-identical requests, existing tests unmodified).
  - ops/bootstrap_notion.py --live: ensure_live_page adopts by EXACT title via search (Notion search is fuzzy — the paper page matches the live query and must be skipped), else creates the sibling under the PAPER PAGE'S OWN PARENT with a red real-money callout (LIVE_PAGE_INTRO); workspace-parented paper page -> LiveBootstrapError pointing at go-live task D (operator creates+shares the page, rerun adopts); adopted page missing a callout gets it appended. bootstrap_live then ensures Trade Ledger (Live)/Account Snapshots (Live) with schemas COPIED from the paper twins (live_database_properties — no second spec, no relations). LIVE_CONFIG_KEYS kept separate from CONFIG_KEYS (paper loop iterates CONFIG_KEYS). main --live prints the page line and returns 1 with nothing written on LiveBootstrapError.
  - write_config now MERGES into notion.databases (dict update) instead of replacing, + optional live_parent_page_id — paper-only reruns preserve live ids and vice versa; existing write_config tests pass unmodified.
  - orchestrator/notion_recorder.py: NotionDatabases gains trade_ledger_live/account_snapshots_live (str | None = None); load_notion_databases requires only defaultless fields (dataclasses.MISSING check) and populates optional ones when present — the committed paper-only config.yaml keeps loading.
  - docs/reference/notion-schema.md: LIVE page intro paragraph, writer-table rows for both (Live) DBs (sole writer = live rebalancer --live, US-014), shared-Decision-Log note, ## Trade Ledger (Live) / ## Account Snapshots (Live) sections stating schema identity with paper.
- Files changed: orchestrator/{notion_client.py,notion_recorder.py,CLAUDE.md}, ops/{bootstrap_notion.py,CLAUDE.md}, docs/reference/notion-schema.md, tests/{test_bootstrap_notion.py,test_notion_client.py,test_notion_recorder.py}, scripts/ralph/prd.json.
- Verified: make check fully green (ruff, pyright, 1188 passed / 3 skipped, +14 new); manual smoke drove main() three times over FakeSessions: fresh --live (page + 2 DBs created, config written), idempotent --live rerun (all "exists", zero POST/PATCH creates), paper-only rerun (live ids + live_parent_page_id preserved), then load_notion_databases on both the smoke config (live ids populated) and the committed repo config (live fields None).
- **Learnings for future iterations:**
  - Notion search (POST /v1/search) is the adopt-by-title mechanism for pages whose parents can't be listed (workspace-level); its matching is FUZZY, so always compare exact titles on the results (the paper page matches the LIVE query).
  - The public Notion API cannot create workspace-level pages — parent must be page_id/database_id. Sibling creation = get_page(paper).parent, refuse when it's not a page.
  - write_config semantics changed from replace to merge for notion.databases — future stories can add config keys the same way without clobbering; ALL_CONFIG_KEYS maps every known title -> key.
  - NotionDatabases optionality pattern: new ids added with `str | None = None` default + loader's `f.default is MISSING` required-check keeps old configs loading; use it for any future DB.
  - US-014 wiring: live ledger/snapshot writers should take databases.trade_ledger_live/account_snapshots_live and REFUSE --live when either is None (mirror the missing-Trade-Ledger refusal); the recorder's Decision Log path is unchanged and shared.
---
## 2026-08-13 01:20 - US-012
- Live read-only status tools (check_live_account / check_live_orders / check_live_pnl):
  - orchestrator/live_status.py (new): `LiveStatusReader` over NotionClient — latest_snapshot()/snapshot_history(limit)/recent_orders(limit) query the Account Snapshots (Live) / Trade Ledger (Live) databases (sorts: Date desc / created_time desc) and parse rows into LiveSnapshot/LiveOrder dataclasses. Unset db ids (bootstrap --live not run) read as empty — the tools answer "no live data yet" gracefully; a real Notion outage raises and surfaces as an error tool_result like paper broker errors. NotionClient.query_db gained optional `max_results=` (stop paginating once N rows are in hand; default None keeps ALL-rows behavior, existing tests unmodified).
  - conversation.py: LiveStatusSource protocol + `live_status=` ctor param; tools registered only when live_channel_id AND live_status are wired, gated by `_require_live_channel` (which gained a `paper_hint=` param so read-tool refusals point at check_account/check_orders/check_pnl instead of the kill-switch line). check_live_account reports the live slot (workspace, universe, allocation pct), names BOTH workspaces when paper and live slots differ ("paper slot: same strategy as live" when equal), live halt/breaker state via new _live_trading_state_line, and the latest snapshot (equity/cash/positions/outcome/prev-day P/L/breaker note). Paper check_account/check_orders/check_pnl builders now take `channel` and refuse a positively-identified live channel via _refuse_paper_tool_in_live_channel (guard messages generalized from kill-switch-specific wording; no test asserted the old sentences). prompts.py read-visibility bullet extended with the live trio + channel-boundness.
  - app.py: LiveStatusReader wired when config.live_channel_id is set (databases=None-safe — live ids missing simply mean no data); docs/decisions.md records the US-053 read-path decision: option (b) Notion/state reads, orchestrator identity gains NO live host access (rationale: structural greppable boundary vs proxy config, no new credential paths, honest staleness).
- Tests: tests/test_live_status_tools.py (25 new: registration gates, account report incl. differing/equal slots + halt + no-data, orders formatting + limit clamp + no-data, pnl totals + no-data, channel gates in all four directions incl. unknown-channel refusal and paper-freeze check, sparse-row formatters, LiveStatusReader over real NotionClient+FakeSession incl. query shapes, and a source-grep test pinning that orchestrator/*.py never contains the live host or allow_live); 2 new query_db max_results tests in test_notion_client.py.
- Files changed: orchestrator/{live_status.py,conversation.py,notion_client.py,app.py,prompts.py,CLAUDE.md}, docs/decisions.md, tests/{test_live_status_tools.py,test_notion_client.py}, scripts/ralph/prd.json.
- Verified: make check fully green (ruff, pyright, 1215 passed / 3 skipped, +27 new); manual smoke drove the REAL reader+core end-to-end over FakeSession Notion payloads (observed: full account report with both workspaces + pct + snapshot numbers and the exact query json; ledger row formatting; pnl period total across two snapshot days; graceful no-data answer with is_error absent; cross-channel refusals both directions with zero Notion/broker calls).
- **Learnings for future iterations:**
  - `_require_live_channel` now takes `paper_hint=` — new live tools should pass a hint naming their paper twins; the default stays the kill-switch line. `_refuse_paper_tool_in_live_channel`'s message is generic ("is for the PAPER account ... use {live_tool}").
  - The read-path boundary is now a TEST: tests/test_live_status_tools.py greps every orchestrator/*.py for "api.alpaca.markets" (after stripping the paper- prefix form) and "allow_live". Never mention either in orchestrator code or comments — write "the live host" instead.
  - Notion "latest N rows" idiom: query_db(db, sorts=[...], page_size=N, max_results=N). Snapshots sort by the Date property; ledger sorts by created_time timestamp (record_final-created rows can lack Submitted At).
  - Notion rich_text/title parsing must handle BOTH item shapes: API responses carry "plain_text", writer-side fixtures often only "text": {"content"} — live_status._plain_text falls back.
  - test_live_status_tools.py exports StubLiveStatus, SNAPSHOT/ORDERS/HISTORY fixtures, and snapshot_page()/ledger_page() Notion payload builders — reuse for US-014's live-summary tests if they need read-back shapes.
  - US-014 wiring note: live ledger/snapshot writers must keep the exact property names AccountSnapshotLog/TradeLedger use today — LiveStatusReader parses those names; a renamed property silently reads as None, so extend tests in both directions if the schema ever changes.
---
## 2026-08-13 02:30 - US-013
- rebalance.py --live: core switch, everything flips together on one flag:
  - run_rebalance gains `live: bool = False` + `allocation: LiveAllocation | None = None`. live=True: promoted loader -> load_promoted_strategy_live (never the paper row); limits default -> load_limits(order_gate_mod.LIVE_LIMITS_PATH); breaker default -> Breaker(load_breaker_config(breaker_mod.LIVE_CONFIG_PATH), LIVE_HALT_FILE, LIVE_HWM_FILE); diff equity scaled via dataclasses.replace(account, equity=equity*pct/100) (allocation.live.json at call time when not injected); submit_orders gains id_prefix ("rdq-live" in live mode) + account_label (submit-failure message names the right account; paper string byte-identical). Gate/breaker/buying-power cap keep the REAL account snapshot — only the diff is scaled. AllocationConfigError added to _ABORT_ERRORS.
  - slack_notifier(live=False): live=True posts to SLACK_LIVE_CHANNEL_ID and raises ConfigError when unset. main(): --live flag; client = AlpacaClient(LIVE_BASE_URL, allow_live=True) (new public constant in alpaca_client.py); Notion wiring SKIPPED in live mode (ledger/snapshots None — US-014 routes to the Live databases; live fills must never hit the paper ledger).
- Files changed: execution/{rebalance.py,alpaca_client.py,CLAUDE.md}, tests/test_rebalance_live.py (new, 11 tests), scripts/ralph/prd.json.
- Verified: make check fully green (ruff, pyright, 1226 passed / 3 skipped, +11); manual smoke drove run_rebalance(live=True) end-to-end over FakeBroker through the REAL client on the live host: plan showed scaled 25 AAPL/12 MSFT (unscaled would be 250/125), ids rdq-live-2026-07-09-buy-*, exit 0, daily summary posted to the injected (live-channel) notify, HWM recorded at REAL $100k equity.
- **Learnings for future iterations:**
  - `ruff format` is NOT part of make check (it would reformat pre-existing code) — the gate is ruff check + pyright + pytest; don't run format on this repo.
  - Live-mode defaults resolve via call-time module attrs (order_gate_mod/breaker_mod/allocation_mod aliases inside rebalance.py) — tests monkeypatch execution.breaker.LIVE_HALT_FILE etc. to tmp paths so nothing ever touches ~/rdq-data/breaker-live/. Injected limits=/breaker=/allocation= skip the files entirely (tests/test_rebalance_live.py run_live helper).
  - Order of checks matters for live tests: the gate runs BEFORE the breaker, so committed limits.live.json ($500/order) rejects the scaled $5,025 order first — halt-file tests must inject permissive limits to reach the breaker.
  - slack_notifier does its imports at CALL time — monkeypatch slack_sdk.WebClient and orchestrator.config.load_slack_config module attrs to test it without tokens.
  - FakeBroker routes by path only, so any host works: AlpacaClient(LIVE_BASE_URL, session=broker.session, allow_live=True) drives the live-host client through the same fake (live_client helper in tests/test_rebalance_live.py).
  - Gate exposure is checked against the REAL account equity (live limits' 10%-of-equity means 10% of the real account), and the breaker HWM tracks REAL equity — deliberate; only diff sizing is scaled. US-021's integration test should assert the same split.
  - US-014 wiring: main()'s live branch currently forces ledger/snapshots to None with a comment — replace that branch with Trade Ledger (Live)/Account Snapshots (Live) wiring + the missing-live-keys refusal; run_rebalance itself needs no change (it takes ledger/snapshots params).
---
## 2026-08-13 03:40 - US-014
- Live rebalance reporting: live ledger, live snapshots, live-channel daily summary:
  - main() --live now wires the SAME TradeLedger/AccountSnapshotLog classes at the LIVE database ids (notion.databases.trade_ledger_live / account_snapshots_live) — replacing US-013's ledger=None stopgap — plus a LiveStatusReader pointed at the PAPER Account Snapshots db (new run_rebalance param paper_snapshots=) for the comparison line. Refusals: missing live ids -> exit 1 naming the config keys + "run ops/bootstrap_notion.py --live" (no escape hatch); --live --no-notion -> exit 1 ("--no-notion is a paper-only escape hatch"); RecorderConfigError in live mode drops the misleading --no-notion hint.
  - format_daily_summary gains allocation_pct (not-None => header says "daily LIVE rebalance summary" + "live allocation: N% of equity (sizing against $X)" line) and paper_vs_live (appended last). Both default None => paper output byte-identical (all paper/live suites pass unmodified except the one US-013 main()-wiring test that asserted the stopgap).
  - paper_vs_live_pnl_line(paper_snapshots, client, as_of): latest paper snapshot row must carry Date == as_of, else omit; live side = previous_day_pnl over a fresh get_portfolio_history fetch (AlpacaError degrades to "n/a"); paper read failure prints a stderr WARNING and omits. Resolved lazily per summary path via the _live_summary_kwargs closure in run_rebalance (dry runs never fetch; the stale-paper-row test proves no history fetch happens by not routing the endpoint).
- Files changed: execution/rebalance.py, execution/CLAUDE.md, orchestrator/live_status.py (docstring: reader is schema-generic, paper reuse is read-only), tests/test_rebalance_live.py, scripts/ralph/prd.json.
- Verified: make check fully green (ruff, pyright, 1234 passed / 3 skipped, +8 net new); manual smoke drove run_rebalance(live=True) with REAL TradeLedger/AccountSnapshotLog over a fake Notion session + FakeBroker with history route: observed 2 POSTs to db-ledger-live (rdq-live orders), 2 record_final PATCHes, 1 POST to db-snap-live, and the posted summary carrying LIVE header, "live allocation: 10% of equity (sizing against $10,000.00)", breaker line, and "paper vs live (2026-07-09 snapshots): paper day P/L $-321.00 (-0.32%), live day P/L $+1,250.50 (+1.25%)"; also drove the --live --no-notion refusal from the real CLI (exit 1).
- **Learnings for future iterations:**
  - format_daily_summary keeps paper byte-identical via None defaults — any future live-only summary content must ride optional params defaulting to None/(), never a live flag that changes shared lines.
  - orchestrator.live_status.LiveStatusReader is schema-generic: point snapshots_db= at ANY Account Snapshots-shaped db. US-014 reads the paper db with it; reads don't violate one-writer-per-DB.
  - The `**_live_summary_kwargs()` closure pattern: live summary extras resolve lazily AT POST TIME (network calls only on the one summary path that runs); pyright needs the `summary_day = as_of` local because closures see param declared types, not narrowing.
  - tests/test_rebalance_live.py now exports StubPaperSnapshots/live_paper_snapshot/all_databases/RecordingRecorder/main_wiring — US-021's integration test should reuse all_databases + the main_wiring fixture shape; tests.test_account_log.route_history(broker) adds the portfolio-history route FakeBroker lacks by default (unrouted path = AssertionError, which is how you prove a fetch never happened).
  - When main() wiring changes, monkeypatch the module-top symbols rebalance_mod.TradeLedger/AccountSnapshotLog and call-time orchestrator.live_status.LiveStatusReader / orchestrator.notion_recorder.load_notion_databases — NotionClient() constructs offline-safe, leave it real.
  - US-015 wiring note: reconcile --live should read Trade Ledger (Live) via databases.trade_ledger_live (same missing-key refusal shape as main()'s); flatten needs no Notion.
---
## 2026-08-13 04:40 - US-015
- reconcile.py and flatten.py --live:
  - ops/reconcile.py: --live flag in main() switches BOTH the ledger source (databases.trade_ledger_live; None -> ReconcileError "run ops/bootstrap_notion.py --live first" -> existing exit-2 catch, mirroring rebalance main()'s missing-live-ids refusal) and the broker client (AlpacaClient(LIVE_BASE_URL, allow_live=True)). run_reconcile/compare/fetch logic untouched — it was already account-agnostic (joins on Alpaca order id, so the rdq-live- client_order_id prefix is irrelevant).
  - ops/flatten.py: --live flag swaps only the client construction; cancel/close/verify sequence and exit codes untouched.
  - Paper paths still call AlpacaClient() with ZERO args, so TestCli's `lambda: ...` monkeypatch factories and every existing test pass unmodified (byte-for-byte AC).
  - Docstrings + argparse descriptions de-paper-ed (both scripts now show paper AND rdq-exec-live invocations); ops/CLAUDE.md flatten/reconcile bullets updated (--live under rdq-exec-live, reconcile's missing-key refusal).
- Files changed: ops/{reconcile.py,flatten.py,CLAUDE.md}, tests/{test_reconcile.py,test_flatten.py}, scripts/ralph/prd.json.
- Verified: make check fully green (ruff, pyright, 1241 passed / 3 skipped, +7 new: TestLiveWiring x2 — live ctor args + ledger id selection, paper defaults unchanged, missing-live-id exit 2, plus full run_reconcile/run_flatten drives over live-host clients on fake sessions); manual smoke: reconcile --live against the committed paper-only config exited 2 with the exact refusal message; flatten --help shows --live and no paper-only claim.
- **Learnings for future iterations:**
  - Existing CLI tests monkeypatch ops.<mod>.AlpacaClient with ZERO-arg lambdas — any new mode must keep the default path calling AlpacaClient() with no args (build the live client in a conditional expression, not by always passing kwargs).
  - Wiring-test idiom for these CLIs: monkeypatch module attrs (AlpacaClient -> recording factory that still constructs the real client, load_notion_databases -> lambda returning a hand-built NotionDatabases, run_reconcile -> arg recorder). NotionDatabases requires all seven paper ids positionally — see make_databases in tests/test_reconcile.py.
  - reconcile main()'s except clause already catches ReconcileError -> exit 2; raise it for any new pre-flight refusal instead of adding print/return branches.
  - US-018 note: ops/CLAUDE.md still has the old "Paper-only rule: never register rdq-exec-live" bullet near the top — that's US-018's to rewrite (setup_onecli/check_onecli), deliberately not touched here.
  - US-020 note: runbook flatten one-liner should be `onecli run --agent rdq-exec-live -- .venv/bin/python -m ops.flatten --live` (matches the new docstring).
---
