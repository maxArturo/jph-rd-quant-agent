# Ralph Progress Log
Started: Mon Aug 24 11:24:42 AM UTC 2026
---
## Codebase Patterns
- Fixture stores for data/ tests: `build_store([TickerBundle(sym, make_bars(sym), (), ())], store)` reusing `make_bars`/`DAYS` from tests/test_build_store.py; `extra_instruments={"name": [(sym, start, end), ...]}` adds universes (multi-row per symbol = PIT spans, dedup col 0 when counting).
- Store field bins are `features/<sym_lower>/<field>.day.bin` (no `$`); user-facing field names are `$`-prefixed. `data/menu.py` is the single source of truth for field inventory + semantics (CURATED_FIELDS).
- Checked-in generated docs must be deterministic vs a fixture store: render only schema-level content (no calendar dates/universe counts — those change on every daily refresh and would break the drift test daily).
- CLI module pattern here: `main(argv) -> int`, argparse, errors as `print(f"ERROR: {exc}", file=sys.stderr); return 1`, `sys.exit(main())` guard; test via `main([...])` + capsys.
- make check = ruff + pyright (standard mode, py3.10 target) + pytest; full suite ~2min.

## 2026-08-24 ~12:00 UTC - US-001
- Implemented data/menu.py: introspects the Qlib store (field-bin union across instruments, calendar range, universes with PIT-aware distinct-symbol counts) and merges CURATED_FIELDS (kind per-ticker/market-level, description, pit_note). Unknown store fields surface with an UNDOCUMENTED marker, never dropped. CLI: default human menu, --json (schema_version 1: field/kind/description/pit_note/documented), --write-doc (+ --doc-path for tests).
- Generated docs/reference/data-menu.md against the real store; drift test compares it to render_doc(fixture-store menu) — hermetic because the doc is schema-only and the fixture store carries the same canonical 6 fields.
- Files: data/menu.py (new), tests/test_menu.py (new, 10 tests), docs/reference/data-menu.md (generated), data/CLAUDE.md (menu note).
- Verified: make check green (1371 passed, 3 skipped); live CLI runs against the real store captured 592 tickers / 2015-01-02->2026-08-21 / 12 universes / all 6 fields documented; --json parses.
- **Learnings for future iterations:**
  - Real store today: exactly open/high/low/close/volume/factor bins; store at ~/.qlib/qlib_data/us_data (DEFAULT_STORE_PATH in build_store.py).
  - US-014 (news field) and US-006 (mkt fields) MUST add CURATED_FIELDS entries + rerun `python -m data.menu --write-doc` + extend the fixture store in tests/test_menu.py (plant extra bins like add_mystery_field does) or the drift test fails.
  - US-002 should inject render_menu(build_menu(...)) — it already contains field names, PIT notes, universes; wrap in try/except MenuError for the degraded "menu unavailable" path. US-003 should match data_required against DataMenu.field_names().
  - build_store's extra_instruments writes (sym, start, end) rows verbatim — handy for fixture universes.
---
## 2026-08-24 ~13:00 UTC - US-002
- Injected the data menu into the directive-crafting prompt. New `prompts.data_menu_context(store=None)`: MENU_HEADER + render_menu(build_menu(store)), rebuilt on every call (tracks daily refreshes); catches Exception -> returns MENU_UNAVAILABLE_LINE (logged), never raises. ConversationCore gained `menu_builder: Callable[[], str] | None` (digest_builder pattern — None skips so existing strict-equality prompt tests stay green); `_system_prompt` = SYSTEM_PROMPT + menu + directive_context, with a call-site try/except guard too. app.py wires `menu_builder=prompts.data_menu_context`.
- Files: orchestrator/prompts.py, orchestrator/conversation.py, orchestrator/app.py, tests/test_conversation.py (5 new tests), orchestrator/CLAUDE.md.
- Verified: make check green (1376 passed, 3 skipped). Tests assert fixture-store field names ($open/$close/$volume/$factor) land in stream_calls[...]["system"] alongside directive context, and both degraded paths (unreadable store, raising builder) produce MENU_UNAVAILABLE_LINE with the turn completing. Live run against the real store: full menu renders (~1.7k chars, 592-ticker universes listed); /tmp bogus path returns the fallback line.
- **Learnings for future iterations:**
  - Existing tests assert `system == prompts.SYSTEM_PROMPT` / startswith — any new system-prompt context MUST be an optional injected callable (None default), never unconditional, or test_system_prompt_has_no_directive_context_before_save breaks.
  - tests/test_menu.py's `fixture_store` is importable (`from tests.test_menu import fixture_store`) — reuse it for any test needing a real bin store.
  - app.py main() keeps heavy imports inside main(); ruff enforces sorted order there (`from orchestrator import prompts` sorts before `orchestrator.approvals`).
  - Fallback prompt text must not enumerate field names (acceptance: no hand-copied field lists anywhere) — MENU_UNAVAILABLE_LINE says "standard daily price/volume data" instead.
  - US-003 hook: data_required verification should call data.menu build_menu(...).field_names(); the menu text is already in the crafting prompt so the model can emit data_required from it.
---
## 2026-08-24 ~14:00 UTC - US-003
- Directive pre-flight: save_directive gained `data_required` (schema + SYSTEM_PROMPT bullet); entries are string-matched against data/menu.py field names via ConversationCore's new `field_lister=` param (real-store default `_store_field_lister`, lazy import). All present -> "Data required: ... — all present in store" line in the saved summary; any missing -> directive saved but PARKED (persisted in new directives.data_required/missing_data JSON columns, guarded ALTER migration) with ":no_entry_sign: parked — needs ingestion: <series>" posted; start_research refuses a parked directive with the same message (state-enforced from SQLite, survives restarts — proven by test). Unreadable store at save time fails the save loud (error tool_result, nothing persisted). directive_context now carries data_required + PARKED state so the model knows post-restart.
- Files: orchestrator/state.py (columns, migrate, create_directive, Directive.parked), orchestrator/conversation.py (schema, parked_directive_message, format_directive_summary line, _verify_data_required, field_lister wiring, start refusal), orchestrator/prompts.py (SYSTEM_PROMPT + directive_context), tests/test_conversation.py (4 new), tests/test_state.py (2 new), orchestrator/CLAUDE.md.
- Verified: make check green (1382 passed, 3 skipped). Migration exercised against a COPY of the real prod state.sqlite (38 legacy rows load, parked=False, round-trip OK). Live-drove both flows against the real store with scripted model turns: $mkt_brent parks + start refused + gpu.launched stays empty; $close/$volume renders the all-present line and start_research launches.
- **Learnings for future iterations:**
  - Directives PARK at save time only — after US-006/US-014 ingest new $mkt_*/$news series, a parked directive must be SAVED AGAIN to unpark (start_research does not re-verify). The parked wording lives in conversation.parked_directive_message; reuse it, never re-type it.
  - ConversationCore injectable-dependency pattern: real default resolved inside __init__ (universes/breaker/broker) vs None-skips (menu_builder/digest_builder) vs None-refuses (gpu). field_lister uses a module-level default fn (`_store_field_lister`) because pyright dislikes shadowing a param with a nested def.
  - `make check` runs `ruff check` only — NOT `ruff format` (the repo has pre-existing would-reformat files; don't reformat wholesale).
  - Multi-turn FakeClient tests: grab the latest tool_result via stream_calls[-1]["messages"][-1]["content"][0] — absolute indices break once history accumulates.
  - StateStore migration pattern for directives now mirrors runs: add column to _SCHEMA AND a guarded ALTER keyed on PRAGMA table_info(directives).
---
## 2026-08-24 ~15:00 UTC - US-004
- Formalized the FMP market-series probe: ops/probe_market_series.sh fetches BZUSD/CLUSD/HOUSD/NGUSD/RBUSD/GCUSD/DXUSD via /stable/historical-price-eod/light and the treasury curve via /stable/treasury-rates chunked <=90 calendar days, every request through `onecli run --agent rdq-research -- curl`. Non-200/empty/non-array => printed FAILURE + exit 1 (check-style `set -uo pipefail`, failures collected). Coverage reported vs NYSE trading days from the store calendar. Treasury cap asserted: single full-window request must return FEWER rows than the chunked fetch. Verifies `year10` present (needed for $mkt_y10). Env overrides RDQ_PROBE_{AGENT,START,END,CALENDAR} for tests.
- Files: ops/probe_market_series.sh (new), tests/test_probe_market_series.py (new, 7 tests incl. shellcheck), docs/decisions.md (2026-08-24 US-064 entry), ops/CLAUDE.md (stub-onecli test pattern + treasury cap note).
- Verified: make check green (1389 passed, 3 skipped). LIVE run on-box through OneCLI, all clean:
  - Window 2025-01-02 -> 2026-08-23, 410 NYSE trading days. BZUSD 462 rows (112.7%), CLUSD 462, HOUSD 461, NGUSD 460, RBUSD 461, GCUSD 461, DXUSD 468 (114.1%, has weekend rows through 2026-08-23); all first=2025-01-02.
  - treasury-rates: 410 rows, 100.0% coverage, 7 chunked requests. Cap PROVEN: single full-window (599-day) request returned 62 rows (HTTP 200, silent truncation) < 410 chunked.
- **Learnings for future iterations:**
  - Commodity EOD /light rows are {symbol, date, price, volume} — no OHLC; treasury rows are {date, month1..year30}. Commodities trade some NYSE holidays (coverage >100% vs equity calendar) and DXUSD even has weekend rows — US-006's broadcast must select/ffill onto the equity calendar, not assume 1:1 dates.
  - Treasury cap for US-005 get_treasury_rates: chunk <=90 CALENDAR days; truncation is silent (200 + trailing ~3 months) so a merged-result row-count sanity check is worthwhile.
  - Stub-onecli PATH-shim testing works cleanly for `onecli run -- curl` wrappers (stub parses -o <file> and last-arg URL, prints http code); driven by PROBE_STUB_MODE/PROBE_STUB_BAD_SYMBOL env.
  - onecli lives at ~/.local/bin (not on default non-login PATH — export before running ops scripts by hand).
  - dict.update(kw=..., **extra) collides when extra carries the same key — use a merged dict literal in test helpers with overridable defaults.
---
## 2026-08-24 ~16:00 UTC - US-005
- FMP client methods for market series: data/fmp.py gains `get_commodity_eod(symbol, start, end)` (/historical-price-eod/light -> CommodityEod rows: date/price/volume, no OHLC on that endpoint, missing volume -> 0.0) and `get_treasury_rates(start, end)` (/treasury-rates -> TreasuryCurve rows with all 12 tenors as float|None). Treasury fetch transparently chunks <=TREASURY_CHUNK_DAYS (90) calendar days per request, merges via a by-date dict (boundary dupes deduped, later chunk wins), returns ascending. Both ride the existing _get retry/backoff (429 Retry-After/exponential) and typed errors (FmpAuthError/FmpRateLimitError/FmpError). Shared `_window()` helper validates start<=end.
- Files: data/fmp.py, tests/test_fmp.py (15 new unit tests + 2 live smokes), data/CLAUDE.md (US-065 note).
- Verified: make check green (1404 passed, 5 skipped). Unit tests cover >90-day window -> 2 requests with exact chunk params (2025-01-02..2025-04-01 / 2025-04-02..2025-05-01), contiguous merged ascending result, boundary-date dedup, per-chunk size <=90 across a 366-day window, missing-tenor None, mid-chunk HTTP 500, 429 backoff on both methods. LIVE: `RDQ_LIVE_TESTS=1 onecli run --agent rdq-research -- .venv/bin/pytest tests/test_fmp.py -m live` -> 3 passed (BZUSD month of prices; treasury 2026-01-02..2026-05-01 chunked, ~80 rows, head+tail present, year10 populated).
- **Learnings for future iterations:**
  - `TreasuryCurve(date=d, **{tenor: ... for tenor in _TREASURY_TENORS})` dict-expansion into a frozen dataclass passes pyright standard mode fine.
  - FakeSession/FakeResponse + make_client from tests/test_fmp.py handle multi-request methods naturally (queued responses pop in order — queue one response per expected chunk).
  - US-006 broadcast fields: use get_commodity_eod/get_treasury_rates; align onto the store calendar with ffill (commodities have >100% NYSE coverage, DXUSD weekend rows; treasury y10 comes from TreasuryCurve.year10).
  - Live-test pattern cost: the 3 live smokes ran in <1s through the proxy — cheap to extend for future endpoint additions (news probe US-011).
---
## 2026-08-24 ~17:00 UTC - US-006
- Market series in the bin store as raw broadcast fields: build_store gained `market_series={"mkt_x": [(date, value), ...]}` — each series is written RAW into EVERY instrument's feature dir (identical per date, implicit factor 1; equity adjustment math untouched). `_market_matrix` owns the policy: ffill equity days with no observation (off-calendar weekend prints count), NaN before the series' first observation, and a >=MARKET_COVERAGE_MIN (99%) direct-coverage gate over trading days since first obs that fails loud NAMING the series (StoreValidationError). validate_store gained `market_fields=` (bin presence + bounds per instrument). `fetch_market_series(client, start, end)` maps COMMODITY_SYMBOLS (BZUSD/CLUSD/HOUSD/NGUSD/RBUSD/GCUSD/DXUSD) + treasury year10 -> the 8 MARKET_FIELDS (mkt_brent/wti/heatoil/natgas/gasoline/gold/dxy/y10), skipping None y10 rows. build_from_fmp/CLI: `--market-start` (canonical MARKET_SERIES_START=2025-01-02). data/menu.py: 8 curated market-level entries (shared _MARKET_PIT_NOTE); doc regenerated from a 14-field fixture store.
- Files: data/build_store.py, data/menu.py, docs/reference/data-menu.md, tests/test_build_store.py (7 new + qlib smoke extended), tests/test_menu.py (fixture_store now carries all MARKET_FIELDS), tests/test_conversation.py (parked-directive test's missing field: $mkt_brent -> $news_ct_1d), data/CLAUDE.md.
- Verified: make check green (1411 passed, 5 skipped). Fixture-store E2E: qlib D.features reads $mkt_gold/$mkt_y10 back identical across AAPL/NVDA with exact values; split ticker keeps RAW mkt values while $close still adjusts; ffill test (120-weekday calendar) proves holiday-ffill, Saturday-print-ffill into a missing Monday, and NaN head; 50%-coverage series fails naming mkt_dxy with no store/partials left. Live real-store menu still clean: no mkt fields listed (not ingested yet), so $mkt_* directives keep parking until the post-US-007 rebuild.
- **Learnings for future iterations:**
  - GOTCHA (US-007 must fix): data/refresh.py rebuilds via build_store WITHOUT market_series — a refresh would silently DROP $mkt_* bins. Do NOT ingest market series into the prod store until US-007 carries them through the rebuild (read them back from bins or refetch from MARKET_SERIES_START when absent).
  - Coverage gate denominator is trading days SINCE THE SERIES' FIRST OBSERVATION — that's what lets a 2025-01-02 market series coexist with the 2015-start equity calendar (pre-2025 mkt values are NaN, legal). US-007's outage-ffill (1 missing day) passes 99% easily; tiny 5-day fixtures do NOT (1 missing day = 80%) — use the weekdays() helper (~120 days) when testing ffill/coverage.
  - tests/test_menu.py fixture_store now carries all 8 mkt series — anything asserting a field is MISSING from it must use $news_ct_1d (free until US-014), not $mkt_*.
  - MarketFakeFmp + make_curve + commodity_price in tests/test_build_store.py are reusable for US-007's refresh tests (subclass adds get_commodity_eod/get_treasury_rates over FakeFmp; y10_none simulates gap days).
  - qlib.init was NOT called twice in one pytest process — the mkt read-back was folded into the existing qlib smoke test; keep it that way (second init in-process is untested here).
  - Regenerating docs/reference/data-menu.md needs a store carrying all curated fields; the real store doesn't yet. Use: build a tiny fixture store with market_series for MARKET_FIELDS, then `data.menu.main(["--store", <fixture>, "--write-doc"])`.
---
## 2026-08-24 ~18:00 UTC - US-007
- Daily refresh appends market fields (found implemented-but-uncommitted by the prior iteration; reviewed, verified, committed). data/refresh.py: `read_market_fields` (glob mkt_*.day.bin union) + `read_market_series` (overlay every symbol's bin onto the calendar — ffilled values read back as direct observations, NaN heads dropped, missing bin on any ticker = loud RefreshError corruption). `refresh_store` pulls each stored series since last_before+1 under the same yesterday-ET --end, merges, and passes `market_series=` through the temp->validate->atomic-swap rebuild (implicit factor 1 — adjustment math never sees mkt fields). `extend_store` carries them too (new tickers get broadcast bins). Per-series FMP failure degrades: stored series keeps its observations, build_store ffills the new days, warning in RefreshResult.warnings — never raises. main() prints warnings to stderr + posts ONE Slack line via execution.rebalance.slack_notifier (injectable `notify=`, --no-slack skips, notify failure itself caught -> still exit 0). `--market-start` = one-time introduction: backfills canonical MARKET_FIELDS the store lacks and forces a rebuild with zero new equity bars; total introduction outage = no-op, store byte-identical. ops/rdq-data-refresh.service: NO_PROXY=slack.com env (Slack must not transit the OneCLI proxy, decisions 2026-07-08).
- Files: data/refresh.py, tests/test_refresh.py (13 new tests), tests/test_services.py (unit NO_PROXY assertion), ops/rdq-data-refresh.service, data/CLAUDE.md (GOTCHA replaced with the carry-through contract).
- Verified: make check green (1426 passed, 5 skipped). Tests drive the full flows against fixture bin stores with read-back: append alongside equity bars (exact window asserted on the fake client), noop idempotency (byte-snapshot equal, zero market fetches), second-run noop, 250-day outage ffill (failed series flat, healthy series advances, unknown mkt_mystery carried + warned), main() outage exit-0 + Slack capture, broken-notify exit-0, --no-slack, introduction (all 8 fields, equity bins value-identical), partial/total introduction outage, extend carries fields, partial-span overlay recovery. Live: `python -m data.refresh --help` renders the new flags.
- **Learnings for future iterations:**
  - PROD STORE STILL HAS NO $mkt_* BINS. Introduction is an explicit operator step: `onecli run --agent rdq-exec-paper -- .venv/bin/python -m data.refresh --market-start 2025-01-02`. US-010's live sweep NEEDS this first (its AC says "post US-006/007 store rebuild") — do it at the start of US-010, then the nightly timer advances the series automatically.
  - DEPLOY: ops/rdq-data-refresh.service changed — after merge, `systemctl --user daemon-reload` on the box (memory: unit files are linked; compare unit start time vs commit date).
  - Stored mkt series advance only when equity bars landed (no new trading day = nowhere to put a value); introduction (--market-start) is the only path that rebuilds without new bars.
  - Refresh reads ffilled bins back as observations — an outage day, once ffilled into the store, is permanently an "observation" on later reads. Coverage gate tolerates this (denominator = days since first obs, 99%); fine for 1-day outages, don't let a multi-week gap accumulate silently (warnings post to Slack each run, so it's visible).
  - mkt_* carry-through covers ONLY the mkt_ prefix. US-014's per-ticker news_ct_1d bins will be DROPPED by refresh rebuilds unless US-015 adds its own read-back+carry (same trap US-006 left for US-007; data/CLAUDE.md documents it).
  - Unknown mkt_-prefixed bins (no FMP mapping) are carried forward-filled + warned, not fatal — safe for experimental series.
  - test_refresh.py helpers reusable: MarketWindowedFakeFmp (windowed equity+market endpoints, per-series failure injection), market_store/market_client/introduction_client, store_snapshot for byte-identity assertions.
---
## 2026-08-24 ~19:00 UTC - US-008
- market_series.h5 companion in the factor-source folders: make_factor_source now writes MARKET_H5 into BOTH data_folder/ and data_folder_debug/ (same filename per the RD-Agent linking contract). New `load_market_frame(store, symbols)` reads every mkt_* bin, overlays across universe symbols onto the store calendar (same recovery approach as refresh.read_market_series), keeps NaN before a series' first observation, and returns a plain DatetimeIndex x `$mkt_*`-columns frame — or None when the store carries no mkt bins (today's prod store), in which case no market file is written, the README stays base, and stale copies from a prior run are unlinked. Debug variant is `market.loc[isin(debug_frame dates)]` — exactly the daily_pv_debug trading-day span. README gains MARKET_README_TEXT (read snippet with key="data", one-value-per-date-shared-by-all-instruments, betas/spreads/conditioning, NEVER a cross-sectional signal alone) only when the file exists.
- Files: data/make_factor_source.py, tests/test_make_factor_source.py (5 new tests + market_store fixture), data/CLAUDE.md.
- Verified: make check green (1431 passed, 5 skipped). Tests: identical schema/columns (sorted $MARKET_FIELDS) in both folders with exact commodity_price/y10 values read back; debug window == daily_pv_debug dates (frame_equal vs full.loc); README lines in both folders; NaN head when market series starts after the calendar (market_days=DAYS[1:]); market-store->plain-store regeneration removes market_series.h5 + README section. Live CLI drive on a fixture store: rc 0, both folders list [README.md, daily_pv.h5, market_series.h5], 5x8 frame prints correct values, README tail correct.
- **Learnings for future iterations:**
  - make_factor_source consumers (orchestrator/universe.py) just call the function and point FACTOR_CoSTEER_DATA_FOLDER at the subfolders — new companion files flow through with zero orchestrator changes; US-009's sync-manifest test should assert folder CONTENTS, not a hardcoded file list.
  - market_five_ticker_client() from tests/test_build_store.py + build_from_fmp(market_start=DAYS[0]) is the one-liner for a fixture store WITH all 8 mkt fields (reuse for US-009/US-010 fixtures); MarketFakeFmp(market_days=DAYS[1:]) gets you a NaN head.
  - The real store STILL has no mkt bins (introduction pending, see US-007 notes) — a live make_factor_source run today produces no market_series.h5 by design; regenerate factor sources after the operator runs --market-start or GPU workspaces won't carry the file.
  - US-014's daily_news.h5 should copy this exact shape: load_*_frame returning None on absence, conditional README section, unlink-stale on regeneration, debug windowed by debug_frame dates.
---
## 2026-08-24 ~20:00 UTC - US-009
- Snapshot-inputs manifest covers the market-series schema: ops/gpu_snapshot.py gains `market_series_manifest(series=MARKET_FIELDS, schema_version=MARKET_MANIFEST_SCHEMA_VERSION)` (JSON: ordered series names + "market-series-v1") hashed into worker_inputs_hash as a new part (`market_manifest=` kwarg, default = the real manifest). Pre-expansion snapshots (old hash names) no longer match -> full bootstrap + auto-rebake on the next run. Sync path proof: gpu_worker.sh gains FACTOR_SOURCE_DIR (shared by bootstrap rsync, require_local_layout, and the new `sync-list` subcommand) — sync-list prints the EXACT rsync file list (same flags, dry-run into an empty temp dir) the bootstrap ships; a text test binds bootstrap's rsync to the same var with no --exclude filters so sync-list can't drift from the real sync.
- Files: ops/gpu_snapshot.py, ops/gpu_worker/gpu_worker.sh, tests/test_gpu_snapshot.py (5 new), tests/test_gpu_worker.py (2 new), ops/CLAUDE.md.
- Verified: make check green (1438 passed, 5 skipped). LIVE: `python -m ops.gpu_snapshot hash` = 5829fa6fb5c5 with the change vs c83c19079199 on stashed old code (hash moved -> stale snapshots unselectable, AC intent). `gpu_worker.sh sync-list` against the real ~/rdq-data/factor_source: 91 lines, data_folder/data_folder_debug contents (daily_pv.h5 + README.md) listed per universe; no market_series.h5 yet — correct, the prod store still lacks mkt bins (introduction pending, US-007 notes). Fixture test proves market_series.h5 appears in the list once the folders carry it.
- **Learnings for future iterations:**
  - gpu_worker.sh usage() is `sed -n '2,34p'` over its own header comment — adding a lifecycle line to the header REQUIRES bumping that range or help output truncates.
  - ops.gpu_snapshot now imports data.build_store (MARKET_FIELDS) — it runs via `.venv/bin/python -m ops.gpu_snapshot` from REPO_ROOT in gpu_worker.sh, so repo-package imports are safe there.
  - US-014 contract: extend market_series_manifest's series (or bump MARKET_MANIFEST_SCHEMA_VERSION for shape changes) when $news_ct_1d lands — that is the "manifest bumped" AC in US-014; a new companion file (daily_news.h5) needs NO sync changes (folder contents ship verbatim, sync-list test pattern ready to extend).
  - rsync dry-run file-list trick: `rsync -az --dry-run --out-format='%n' src/ $(mktemp -d)/` prints exactly what a real sync would create — cheap way to make shell sync paths testable without a remote.
  - NOTE: the live worker-inputs hash changes AGAIN when the operator runs the mkt introduction (store_schema_marker picks up the new field bins) — expect one more full bootstrap + rebake after that; not a bug.
---
## 2026-08-24 ~21:15 UTC - US-010
- Refresh-parity harness for new fields (found implemented-but-uncommitted by the prior iteration; reviewed, live-verified, committed). ops/parity_check.py: per field it (1) trains a throwaway 3-feature LGBM factor in docker qrun (workspace ~/rdq-runs/parity_check/<field>/, recreated every run; conf_baseline.yaml with jinja date placeholders so the snapshot machinery can re-render it), (2) snapshots via the REAL execution.pred_refresh.snapshot_pred_refresh (incl. the c9587797 task-signature conf match), (3) exact-weights re-predicts via ops.confirm_window._run_repredict with test_end pushed to the store calendar end, (4) computes mean per-day spearman over EVERY overlapping cross-section and passes at >= --min-spearman (0.98). Market-level fields enter the features relative to $close (raw broadcast series are cross-sectionally constant); per-ticker fields enter directly. CLI: --field '$x' | --all-mkt; PARITY PASS/FAIL/ERROR lines always name the field + measured spearman; exit 1 on any failure. Dates: 50/25/25 split of trading days from --start (default MARKET_SERIES_START) with a 5-day tail held out so PortAnaRecord never indexes off the calendar and the re-predict demonstrably EXTENDS the pred.
- Files: ops/parity_check.py (new), tests/test_parity_check.py (new, 18 tests, fake-runner E2E incl. snapshot round-trip + inverted-pred fail path), ops/CLAUDE.md.
- Verified: make check green (1460 passed, 5 skipped). LIVE on-box, real store, real docker (each field ~4.5 min):
  - Regression guard: PARITY PASS $close: spearman 1.0000 over 102 overlap day(s).
  - Sweep: PARITY PASS for all 8 — $mkt_brent, $mkt_wti, $mkt_heatoil, $mkt_natgas, $mkt_gasoline, $mkt_gold, $mkt_dxy, $mkt_y10 — each spearman 1.0000 over 102 overlap day(s); "parity: 8/8 field(s) passed", exit 0.
- **Learnings for future iterations:**
  - THE PROD STORE NOW HAS ALL 8 mkt_* BINS (the US-007 --market-start introduction was executed before this iteration; calendar ends 2026-08-21 and the nightly refresh advances the series). US-011+ can rely on it.
  - Factor sources have NOT been regenerated since the introduction — ~/rdq-data/factor_source/*/data_folder/ still lack market_series.h5, so GPU workspaces won't carry it until someone re-runs make_factor_source per universe. Do this before (or as part of) the next GPU-run-facing story, and expect the worker-inputs hash to have changed already (store_schema_marker picked up the new bins).
  - A full train->snapshot->re-predict parity cycle on us_liquid (589 tickers, ~410 days, 3 features) is ~4.5 min on the 4-core box — cheap enough to run per new field routinely (US-015 news: `--field '$news_ct_1d'`).
  - qrun inside local_qlib leaves ROOT-owned files including a nested workspace/qlib_workspace/mlruns/filelock OUTSIDE mlruns/ — in-container exit chmod must cover the WHOLE workspace; stale-workspace deletion goes through _remove_workspace (docker chmod fallback on PermissionError).
  - The conf a harness writes must keep a choose_source_conf-recognized filename (conf_baseline.yaml) AND `| default(..., true)` jinja filters so an Undefined render stays parseable (the task-signature match renders it bare).
  - Fake-runner tests need the qrun side to write BOTH the "Render the template with the context: {...}" log line (recover_context) and a task pickle that signature-matches the conf, or snapshot_pred_refresh refuses.
  - Python stdout is block-buffered when redirected to a file — a multi-field sweep prints nothing until exit; don't mistake an empty log for a hung run (docker ps shows the live rdq-parity-*/rdq-confirm-* containers).
---
