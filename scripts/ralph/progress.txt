# Ralph Progress Log
Started: Mon Aug 24 11:24:42 AM UTC 2026
---
## Codebase Patterns
- Fixture stores for data/ tests: `build_store([TickerBundle(sym, make_bars(sym), (), ())], store)` reusing `make_bars`/`DAYS` from tests/test_build_store.py; `extra_instruments={"name": [(sym, start, end), ...]}` adds universes (multi-row per symbol = PIT spans, dedup col 0 when counting).
- Store field bins are `features/<sym_lower>/<field>.day.bin` (no `$`); user-facing field names are `$`-prefixed. `data/menu.py` is the single source of truth for field inventory + semantics (CURATED_FIELDS).
- Checked-in generated docs must be deterministic vs a fixture store: render only schema-level content (no calendar dates/universe counts — those change on every daily refresh and would break the drift test daily).
- CLI module pattern here: `main(argv) -> int`, argparse, errors as `print(f"ERROR: {exc}", file=sys.stderr); return 1`, `sys.exit(main())` guard; test via `main([...])` + capsys.
- make check = ruff + pyright (standard mode, py3.10 target) + pytest; full suite ~2min.

## 2026-08-24 ~12:00 UTC - US-001
- Implemented data/menu.py: introspects the Qlib store (field-bin union across instruments, calendar range, universes with PIT-aware distinct-symbol counts) and merges CURATED_FIELDS (kind per-ticker/market-level, description, pit_note). Unknown store fields surface with an UNDOCUMENTED marker, never dropped. CLI: default human menu, --json (schema_version 1: field/kind/description/pit_note/documented), --write-doc (+ --doc-path for tests).
- Generated docs/reference/data-menu.md against the real store; drift test compares it to render_doc(fixture-store menu) — hermetic because the doc is schema-only and the fixture store carries the same canonical 6 fields.
- Files: data/menu.py (new), tests/test_menu.py (new, 10 tests), docs/reference/data-menu.md (generated), data/CLAUDE.md (menu note).
- Verified: make check green (1371 passed, 3 skipped); live CLI runs against the real store captured 592 tickers / 2015-01-02->2026-08-21 / 12 universes / all 6 fields documented; --json parses.
- **Learnings for future iterations:**
  - Real store today: exactly open/high/low/close/volume/factor bins; store at ~/.qlib/qlib_data/us_data (DEFAULT_STORE_PATH in build_store.py).
  - US-014 (news field) and US-006 (mkt fields) MUST add CURATED_FIELDS entries + rerun `python -m data.menu --write-doc` + extend the fixture store in tests/test_menu.py (plant extra bins like add_mystery_field does) or the drift test fails.
  - US-002 should inject render_menu(build_menu(...)) — it already contains field names, PIT notes, universes; wrap in try/except MenuError for the degraded "menu unavailable" path. US-003 should match data_required against DataMenu.field_names().
  - build_store's extra_instruments writes (sym, start, end) rows verbatim — handy for fixture universes.
---
## 2026-08-24 ~13:00 UTC - US-002
- Injected the data menu into the directive-crafting prompt. New `prompts.data_menu_context(store=None)`: MENU_HEADER + render_menu(build_menu(store)), rebuilt on every call (tracks daily refreshes); catches Exception -> returns MENU_UNAVAILABLE_LINE (logged), never raises. ConversationCore gained `menu_builder: Callable[[], str] | None` (digest_builder pattern — None skips so existing strict-equality prompt tests stay green); `_system_prompt` = SYSTEM_PROMPT + menu + directive_context, with a call-site try/except guard too. app.py wires `menu_builder=prompts.data_menu_context`.
- Files: orchestrator/prompts.py, orchestrator/conversation.py, orchestrator/app.py, tests/test_conversation.py (5 new tests), orchestrator/CLAUDE.md.
- Verified: make check green (1376 passed, 3 skipped). Tests assert fixture-store field names ($open/$close/$volume/$factor) land in stream_calls[...]["system"] alongside directive context, and both degraded paths (unreadable store, raising builder) produce MENU_UNAVAILABLE_LINE with the turn completing. Live run against the real store: full menu renders (~1.7k chars, 592-ticker universes listed); /tmp bogus path returns the fallback line.
- **Learnings for future iterations:**
  - Existing tests assert `system == prompts.SYSTEM_PROMPT` / startswith — any new system-prompt context MUST be an optional injected callable (None default), never unconditional, or test_system_prompt_has_no_directive_context_before_save breaks.
  - tests/test_menu.py's `fixture_store` is importable (`from tests.test_menu import fixture_store`) — reuse it for any test needing a real bin store.
  - app.py main() keeps heavy imports inside main(); ruff enforces sorted order there (`from orchestrator import prompts` sorts before `orchestrator.approvals`).
  - Fallback prompt text must not enumerate field names (acceptance: no hand-copied field lists anywhere) — MENU_UNAVAILABLE_LINE says "standard daily price/volume data" instead.
  - US-003 hook: data_required verification should call data.menu build_menu(...).field_names(); the menu text is already in the crafting prompt so the model can emit data_required from it.
---
